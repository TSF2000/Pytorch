# PyTorch 深度学习实践 · 第六课 逻辑回归 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys)  
> **集数**：第 6 集 · Logistic Regression（逻辑斯蒂回归）  
> **时长**：约 50 分钟  
> **整理日期**：2026-07-11  
> **前置**：第 5 集线性回归（`nn.Module`、五步训练循环、`MSELoss`）

---

## 一、本课定位

第 5 集预测**连续值**（回归），第 6 集解决**二分类**问题：输出 0 或 1 的概率（如是否及格、是否会恶化）。

```
第 5 集（回归）              第 6 集（二分类）
─────────────────           ─────────────────
输出：连续实数               输出：(0, 1) 概率
损失：MSELoss                损失：BCELoss
模型：Linear                 模型：Linear + Sigmoid
标签 y：2, 4, 6              标签 y：0, 0, 1
```

**虽然叫「逻辑回归」，本质是分类算法，不是回归。**

本课核心：训练循环不变，换 **Sigmoid + BCELoss + 0/1 标签**。

---

## 二、Sigmoid 函数

线性输出 \(z = wx + b\) 是 \((-\infty, +\infty)\) 的实数，无法直接表示概率。

**Sigmoid** 把实数映射到 (0, 1)：

\[
\sigma(z) = \frac{1}{1 + e^{-z}}
\]

| z 的值 | σ(z) |
|--------|------|
| z = 0 | 0.5 |
| z → +∞ | → 1 |
| z → -∞ | → 0 |

```python
y_pred = torch.sigmoid(self.linear(x))
```

> Sigmoid **没有可训练参数**，直接在 `forward()` 里调用，不用在 `__init__` 里初始化。

---

## 三、模型结构

本质 = **线性层 + Sigmoid**：

```
x → Linear(1,1) → z (实数) → Sigmoid → y_pred (0~1 概率)
```

```python
class LogisticRegressionModel(torch.nn.Module):
    def __init__(self):
        super(LogisticRegressionModel, self).__init__()
        self.linear = torch.nn.Linear(1, 1)

    def forward(self, x):
        y_pred = torch.sigmoid(self.linear(x))
        return y_pred
```

与第 5 集相比，**只改了 `forward()`**，`__init__` 里仍是 `nn.Linear(1, 1)`。

---

## 四、损失函数：BCELoss

回归用 MSE，二分类用 **BCE（Binary Cross Entropy，二元交叉熵）**：

\[
loss = - \big[ y \cdot \log(\hat{y}) + (1-y) \cdot \log(1-\hat{y}) \big]
\]

直觉：

- 真实 \(y=1\) 时，希望 \(\hat{y}\) 越接近 1，loss 越小
- 真实 \(y=0\) 时，希望 \(\hat{y}\) 越接近 0，loss 越小

```python
criterion = torch.nn.BCELoss(reduction='sum')  # 旧版：size_average=False
```

> `BCELoss` 要求输入是 **0~1 的概率**，所以 `forward` 里必须先过 Sigmoid。

### 为什么分类不用 MSE

| | MSE | BCE |
|---|-----|-----|
| 适用 | 回归（连续值） | 二分类（0/1） |
| 梯度性质 | 分类时容易饱和、收敛差 | 专为概率设计，更合理 |

---

## 五、完整代码

```python
import torch

# Step 1: 数据（y 只能是 0 或 1）
x_data = torch.Tensor([[1.0], [2.0], [3.0]])
y_data = torch.Tensor([[0], [0], [1]])

# Step 2: 模型
class LogisticRegressionModel(torch.nn.Module):
    def __init__(self):
        super(LogisticRegressionModel, self).__init__()
        self.linear = torch.nn.Linear(1, 1)

    def forward(self, x):
        return torch.sigmoid(self.linear(x))

model = LogisticRegressionModel()

# Step 3: 损失 + 优化器
criterion = torch.nn.BCELoss(reduction='sum')
optimizer = torch.optim.SGD(model.parameters(), lr=0.01)

# Step 4: 训练（与第 5 集完全相同）
for epoch in range(1000):
    y_pred = model(x_data)
    loss = criterion(y_pred, y_data)

    optimizer.zero_grad()
    loss.backward()
    optimizer.step()

print('w =', model.linear.weight.item())
print('b =', model.linear.bias.item())
```

**期望直觉**：x 越小越倾向预测 0，x 越大越倾向预测 1；loss 逐渐下降。

---

## 六、与第 5 集的对照

| 项目 | 第 5 集（线性回归） | 第 6 集（逻辑回归） |
|------|-------------------|-------------------|
| **任务** | 回归（连续值） | 二分类（0/1） |
| **标签 y** | `[[2.0],[4.0],[6.0]]` | `[[0],[0],[1]]` |
| **forward** | `return self.linear(x)` | `return torch.sigmoid(self.linear(x))` |
| **损失** | `MSELoss` | `BCELoss` |
| **训练循环** | 五步 | **完全相同** |
| **优化器** | `SGD(lr=0.01)` | `SGD(lr=0.01)` |

---

## 七、进阶：BCEWithLogitsLoss

工程上更常用 **Sigmoid + BCE 合并** 的写法，数值更稳定：

```python
class Model(torch.nn.Module):
    def forward(self, x):
        return self.linear(x)  # 输出 logits，不过 Sigmoid

criterion = torch.nn.BCEWithLogitsLoss(reduction='sum')
```

| 损失 | 模型 forward 输出 | 说明 |
|------|------------------|------|
| `BCELoss` | 过 Sigmoid 后的概率 | **课件用法** |
| `BCEWithLogitsLoss` | 原始 logits（实数） | 工程推荐，数值更稳 |

**二选一，不要重复加 Sigmoid。**

---

## 八、可视化

与第 5 集相同，可画 loss、w、b 曲线：

```python
import matplotlib.pyplot as plt

plt.plot(epoch_list, loss_list)
plt.xlabel('epoch')
plt.ylabel('loss')
plt.show()

plt.plot(epoch_list, w_list, label='w')
plt.plot(epoch_list, b_list, label='b')
plt.legend()
plt.show()
```

分类任务还可打印训练后的预测概率：

```python
print(model(x_data))  # 应分别接近 0, 0, 1
```

---

## 九、常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| BCELoss 报错 | 输入不在 (0,1) | `forward` 里加 Sigmoid |
| loss 是 NaN | 预测值恰好为 0 或 1 取 log | 检查 lr；了解 `BCEWithLogitsLoss` |
| 用了 BCEWithLogitsLoss 又加 Sigmoid | 重复激活 | 二选一 |
| 误用 MSELoss 做分类 | 损失函数选错 | 分类用 **BCELoss** |
| y 标签类型不对 | BCE 要求 float | `torch.Tensor([[0.0],[0.0],[1.0]])` |
| 收敛慢 | lr 太小 | 课件默认 `SGD(lr=0.01)` |

---

## 十、本课你需要记住的

1. **逻辑回归 = 分类**，不是回归
2. **Sigmoid** 把线性输出变成 (0, 1) 概率
3. **BCELoss** 专用于二分类，输入必须是概率
4. **训练五步循环不变**——换任务只换模型 forward 和损失
5. 第 7 集会把输入从 1 维扩展到 **8 维**，模型结构会加深

---

## 十一、与后续课程的关系

| 第 6 集学会 | 在哪用到 |
|------------|----------|
| Sigmoid + 二分类 | 第 7 集多维输入、第 9 集多分类 |
| BCELoss | 二分类任务、NLP 部分场景 |
| 分类 vs 回归 | 后续监督学习的分岔点 |
| 五步训练循环 | 继续复用 |

---

## 十二、自测清单

- [ ] 能解释为什么分类需要 Sigmoid
- [ ] 能写出 BCE 损失的直觉含义
- [ ] 能独立把第 5 集代码改成逻辑回归（改 y、forward、loss 三处）
- [ ] 知道 BCELoss 和 BCEWithLogitsLoss 的区别
- [ ] 能说出逻辑回归和线性回归的 3 处不同

---

## 十三、一句话总结

> 第 6 集 = 第 5 集训练套路不变，输出加 **Sigmoid** 变概率，**MSELoss 换 BCELoss**，标签变 **0/1**；这是从回归迈向分类的第一步。

---

*下一课：第七课 · 处理多维特征的输入（糖尿病数据集，8 维特征 + 多层网络）*
