# PyTorch 深度学习实践 · 第八课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=8)  
> **集数**：第 8 集 · 加载数据集（Dataset and DataLoader）  
> **时长**：约 37 分钟  
> **整理日期**：2026-07-12  
> **重要程度**：⭐⭐⭐ **工程化数据加载，训练循环从「全量」升级为「mini-batch」**

---

## 一、第八课在学什么

第 7 集把**全部样本**一次性喂给模型（batch size = N）。  
数据量一大，内存和计算都扛不住；随机性也不够。

```
第 7 集（全量）              第 8 集（mini-batch）
─────────────────           ─────────────────
x_data 全部 (N, 8)           每次只取 32 条 (32, 8)
一次 forward 算 N 个样本      每个 epoch 迭代 N/32 次
手写 np.loadtxt              Dataset + DataLoader 封装
训练循环 1 层 for            训练循环 2 层 for（epoch + batch）
```

**本课核心**：用 `Dataset` 封装数据、用 `DataLoader` 自动分批 + 打乱；**模型不变，训练循环加一层 batch 循环**。

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 7 集 | diabetes 数据加载、`Linear(8,6→6,4→4,1)` 多层模型 |
| 第 6 集 | BCELoss、Sigmoid、五步训练循环 |
| 第 3 集 | BGD / SGD / mini-batch 概念 |
| Python OOP | 类继承、`__init__`、魔法方法 |

如果第 7 集 diabetes 模型能跑通，可以直接进第 8 集。

---

## 三、为什么要 Mini-Batch

| 方式 | 每次用多少样本 | 特点 |
|------|----------------|------|
| **Batch GD（全量）** | 全部 N 个 | 梯度准确，内存大、慢 |
| **SGD（单样本）** | 1 个 | 随机性强，更新频繁但不稳定 |
| **Mini-Batch** | 一小批（如 32） | **折中方案**，工程最常用 |

Mini-Batch 优点：
- 利用 GPU **并行计算**（矩阵运算）
- 内存占用可控
- 每轮有一定**随机性**（配合 shuffle）
- 训练效率与稳定性平衡

> 第 3 集 bgd_test / sgd_test 的手写版本，本集用 PyTorch 官方工具实现 mini-batch。

---

## 四、两个核心类

```python
from torch.utils.data import Dataset, DataLoader
```

| 类 | 角色 | 能否直接实例化 |
|----|------|----------------|
| **Dataset** | 定义「数据集是什么、怎么取第 i 条」 | ❌ 抽象类，需继承 |
| **DataLoader** | 按 batch 加载、shuffle、多线程 | ✅ 直接实例化 |

关系：

```
Dataset（存数据、支持下标访问）
    ↓ 传给
DataLoader（自动分批、打乱、迭代）
    ↓ 训练时
for inputs, labels in train_loader: ...
```

---

## 五、自定义 Dataset（课件重点）

继承 `Dataset`，必须实现 **3 个方法**：

| 方法 | 作用 | Java 类比 |
|------|------|-----------|
| `__init__` | 加载/准备数据 | 构造函数 |
| `__getitem__(index)` | 返回第 index 个样本 | `list.get(index)` |
| `__len__` | 返回样本总数 | `list.size()` |

```python
import numpy as np
import torch
from torch.utils.data import Dataset, DataLoader

class DiabetesDataset(Dataset):
    def __init__(self, filepath):
        xy = np.loadtxt(filepath, delimiter=',', dtype=np.float32)
        self.len = xy.shape[0]                          # 样本数 N
        self.x_data = torch.from_numpy(xy[:, :-1])      # (N, 8)
        self.y_data = torch.from_numpy(xy[:, [-1]])     # (N, 1)

    def __getitem__(self, index):
        return self.x_data[index], self.y_data[index]   # 单样本: x(8,), y(1,)

    def __len__(self):
        return self.len
```

### 使用方式

```python
dataset = DiabetesDataset('diabetes.csv.gz')
print(len(dataset))           # N，如 759
print(dataset[0])             # 等价于 __getitem__(0)，返回 (x, y)
x, y = dataset[3]
```

### 大数据集的两种策略（课件会提）

| 策略 | 适用 | 做法 |
|------|------|------|
| **全部读入内存** | 小数据集（如 diabetes） | `__init__` 里 `loadtxt`，`__getitem__` 直接取 |
| **按需读取** | 大图像集（GB 级） | `__init__` 只存文件路径列表，`__getitem__` 再读文件 |

本课用第一种（与第 7 集相同数据，只是封装方式变了）。

---

## 六、DataLoader 怎么用

```python
train_loader = DataLoader(
    dataset=dataset,
    batch_size=32,
    shuffle=True,
    num_workers=0      # Windows 建议先设 0
)
```

| 参数 | 含义 |
|------|------|
| `dataset` | 你的 Dataset 实例 |
| `batch_size` | 每个 mini-batch 的样本数（如 32） |
| `shuffle` | 每个 epoch 是否打乱顺序（训练集 True，测试集 False） |
| `num_workers` | 多线程加载；**Windows 常设 0**，Linux 可设 2/4/8 |

### 每个 epoch 迭代多少次

\[
\text{iterations} = \left\lceil \frac{\text{len(dataset)}}{\text{batch\_size}} \right\rceil
\]

例：`len=759, batch_size=32` → 每 epoch 约 **24 次**迭代。

### 取出来的 shape

```python
for i, data in enumerate(train_loader):
    inputs, labels = data
    # inputs.shape: (32, 8)
    # labels.shape: (32, 1)
    break
```

DataLoader 会自动把 32 个单样本堆叠成 batch 矩阵——这就是 mini-batch 的体现。

---

## 七、训练循环的变化（最重要）

### 第 7 集（全量，1 层循环）

```python
for epoch in range(100):
    y_pred = model(x_data)          # 一次喂全部 N 个
    loss = criterion(y_pred, y_data)
    optimizer.zero_grad()
    loss.backward()
    optimizer.step()
```

### 第 8 集（mini-batch，2 层循环）

```python
for epoch in range(100):
    for i, data in enumerate(train_loader):
        inputs, labels = data       # inputs: (32, 8), labels: (32, 1)

        y_pred = model(inputs)
        loss = criterion(y_pred, labels)

        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

        # loss, acc 可选：每 N 个 batch 打印一次
```

| 变化 | 说明 |
|------|------|
| 外层 | 仍是 **epoch**（所有样本过一遍 = 1 epoch） |
| 内层 | 新增 **batch 循环**，遍历 `train_loader` |
| forward 输入 | 从 `(N, 8)` 变成 `(32, 8)` |
| 五步更新 | **不变**：zero_grad → backward → step |

> 模型结构（`Linear(8,6→6,4→4,1)` + Sigmoid）、BCELoss、SGD 与第 7 集相同。

---

## 八、完整代码预览（听课前可先扫一眼）

```python
import torch
from torch.utils.data import Dataset, DataLoader

# Step 1: Dataset
class DiabetesDataset(Dataset):
    def __init__(self, filepath):
        xy = np.loadtxt(filepath, delimiter=',', dtype=np.float32)
        self.len = xy.shape[0]
        self.x_data = torch.from_numpy(xy[:, :-1])
        self.y_data = torch.from_numpy(xy[:, [-1]])

    def __getitem__(self, index):
        return self.x_data[index], self.y_data[index]

    def __len__(self):
        return self.len

dataset = DiabetesDataset('diabetes.csv.gz')
train_loader = DataLoader(dataset, batch_size=32, shuffle=True, num_workers=0)

# Step 2: 模型（与第 7 集相同）
class Model(torch.nn.Module):
    def __init__(self):
        super(Model, self).__init__()
        self.linear1 = torch.nn.Linear(8, 6)
        self.linear2 = torch.nn.Linear(6, 4)
        self.linear3 = torch.nn.Linear(4, 1)
        self.sigmoid = torch.nn.Sigmoid()

    def forward(self, x):
        x = self.sigmoid(self.linear1(x))
        x = self.sigmoid(self.linear2(x))
        x = self.sigmoid(self.linear3(x))
        return x

model = Model()
criterion = torch.nn.BCELoss(reduction='mean')
optimizer = torch.optim.SGD(model.parameters(), lr=0.01)

# Step 3: 训练（注意双层循环）
for epoch in range(100):
    for i, (inputs, labels) in enumerate(train_loader):
        y_pred = model(inputs)
        loss = criterion(y_pred, labels)

        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

    print(f'epoch {epoch}, last batch loss: {loss.item():.4f}')
```

---

## 九、听课时要重点关注的

| 关注点 | 为什么重要 |
|--------|------------|
| `Dataset` 三个魔法方法 | 后面所有项目都要自定义 Dataset |
| `DataLoader` 四个参数 | batch_size / shuffle 是训练标配 |
| 训练循环变成 **2 层 for** | 与第 7 集最大区别 |
| `inputs.shape` 是 `(batch, 8)` | 理解 batch 维度 |
| `len(dataset) / batch_size` | 知道每 epoch 迭代几次 |
| `num_workers=0` on Windows | 避免多线程报错 |
| shuffle 的作用 | 每个 epoch 数据顺序不同，提高泛化 |

---

## 十、与第 7 集的对照

| 项目 | 第 7 集 | 第 8 集 |
|------|--------|--------|
| 数据加载 | `np.loadtxt` 直接用 | **Dataset 封装** |
| 喂数据方式 | 全量 `(N, 8)` | **mini-batch `(32, 8)`** |
| 训练循环 | 1 层 for（epoch） | **2 层 for（epoch + batch）** |
| 随机性 | 无 shuffle | **shuffle=True** |
| 模型 | 多层 Linear + Sigmoid | **完全相同** |
| 损失 / 优化器 | BCELoss + SGD | **完全相同** |

---

## 十一、预习动手任务（20 分钟）

在听第 8 集之前，建议完成：

- [ ] 确认 `diabetes.csv.gz` 仍在项目目录（第 7 集已下载）
- [ ] 手写 `DiabetesDataset` 类，实现 `__init__`、`__getitem__`、`__len__`
- [ ] 实例化 `DataLoader(batch_size=32, shuffle=True, num_workers=0)`
- [ ] 打印 `len(dataset)` 和第一个 batch 的 `inputs.shape`、`labels.shape`
- [ ] 把第 7 集训练循环改成双层 for，观察能否跑通
- [ ] 对比：全量训练 1 次 forward vs mini-batch 需要多少次迭代

---

## 十二、常见坑（提前避雷）

| 问题 | 原因 | 解决 |
|------|------|------|
| `Dataset` 无法实例化 | 直接 `Dataset()` | 必须**继承**并实现自己的类 |
| 忘记实现 `__len__` | DataLoader 需要知道数据集大小 | 返回 `self.len` |
| Windows 多线程报错 | `num_workers > 0` | **设为 0** |
| batch 维度不对 | 单样本 `(8,)` vs batch `(32, 8)` | 用 DataLoader 自动堆叠 |
| 内层循环漏掉 | 仍用全量 `x_data` | 改成 `for inputs, labels in train_loader` |
| shuffle 在测试集上也 True | 测试不需要随机 | 训练集 shuffle=True，测试集 False |
| epoch 里 loss 含义 | 每个 batch 的 loss 不同 | 可记录 epoch 平均 loss |

---

## 十三、关键概念速查

| 术语 | 含义 |
|------|------|
| **Sample** | 一条数据（一个病人的 8 个指标 + 标签） |
| **Batch / Mini-Batch** | 一次训练用的若干样本（如 32 条） |
| **Iteration** | 处理 1 个 batch 算 1 次迭代 |
| **Epoch** | 所有样本都参与过 1 次训练 = 1 epoch |
| **batch_size** | 每个 mini-batch 的样本数 |
| **shuffle** | 每个 epoch 开始前打乱样本顺序 |

关系：

```
1 epoch = len(dataset) / batch_size 次 iteration（约）
100 epoch = 100 × 上述 iteration 次 参数更新
```

---

## 十四、与后续课程的关系

| 第 8 集学会 | 在哪用到 |
|------------|----------|
| `Dataset` 自定义 | 所有 PyTorch 项目 |
| `DataLoader` | 训练标配，CNN/RNN 同样用法 |
| mini-batch 训练 | 图像、NLP 等大数据场景 |
| shuffle / batch_size | 调参、复现实验 |
| 双层训练循环 | 第 9 集多分类、第 12 集 RNN |

---

## 十五、第 9 集预告（了解即可）

第 8 集仍是**二分类**（BCELoss + Sigmoid）。第 9 集会扩展到**多分类**：

- 输出从 1 维变成 **C 维**（类别数）
- **Softmax** 替代 Sigmoid
- **CrossEntropyLoss**（`nn.CrossEntropyLoss`）替代 BCELoss
- 数据集可能换成 MNIST 等

第 8 集先把「怎么喂数据」学扎实，第 9 集只换损失和输出层。

---

## 十六、一句话预习总结

> 第 8 集 = 第 7 集 diabetes 模型不变 + **Dataset 封装数据** + **DataLoader 分批/shuffle** + 训练循环从 1 层变成 **epoch × batch** 双层循环；这是 PyTorch 工程化训练的标准数据管道。

---

*听课建议：先单独跑通 Dataset 和 DataLoader，print 每个 batch 的 shape；再改训练循环，确认 loss 能下降后再调 epoch 和 lr。*
