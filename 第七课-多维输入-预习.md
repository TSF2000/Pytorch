# PyTorch 深度学习实践 · 第七课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys)  
> **集数**：第 7 集 · 处理多维特征的输入  
> **时长**：约 55 分钟  
> **整理日期**：2026-07-11  
> **重要程度**：⭐⭐⭐ **第一个真实数据集 + 多层网络，逻辑回归从 1 维走向 8 维**

---

## 一、第七课在学什么

第 5～6 集输入只有 **1 个特征**（一个 x）。  
现实世界的数据往往是**多维的**——比如预测糖尿病，有 8 项体检指标。

```
第 6 集（1 维）              第 7 集（8 维）
─────────────────           ─────────────────
x: 单个数值                  x: 8 个特征（X1~X8）
Linear(1, 1)                 Linear(8, 1) 或 多层网络
玩具数据                     diabetes.csv 真实数据集
```

**本课核心**：学会处理多维输入、从 CSV 加载数据、构建多层神经网络。

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 6 集 | Sigmoid、BCELoss、逻辑回归、五步训练循环 |
| 第 5 集 | `nn.Linear(in, out)` 含义 |
| NumPy | `np.loadtxt` 读 CSV |
| 线性代数 | 矩阵乘法 \([m \times n] \cdot [n \times k] = [m \times k]\) |

如果第 6 集逻辑回归能跑通，可以直接进第 7 集。

---

## 三、数据集：Diabetes（糖尿病预测）

课件使用 **diabetes.csv**（或 `diabetes.csv.gz`）：

| 列 | 含义 |
|----|------|
| X1 ~ X8 | 8 项特征（体检指标） |
| Y | 标签：1 = 一年后病情会加重，0 = 不会 |

**任务**：根据 8 维特征，预测 Y（二分类，与第 6 集相同）。

> 课件网盘：[百度网盘](https://pan.baidu.com/s/1_J1f5VSyYl-Jj2qIuc1pXw) 提取码：wyhu

---

## 四、从 CSV 加载数据（提前熟悉）

```python
import numpy as np
import torch

xy = np.loadtxt('diabetes.csv.gz', delimiter=',', dtype=np.float32)

x_data = torch.from_numpy(xy[:, :-1])    # 前 8 列特征，shape: (N, 8)
y_data = torch.from_numpy(xy[:, [-1]])   # 最后 1 列标签，shape: (N, 1)
```

### 关键细节：`[:, [-1]]` 不能写成 `[:, -1]`

| 写法 | shape | 结果 |
|------|-------|------|
| `xy[:, -1]` | `(N,)` | 一维向量 ❌ |
| `xy[:, [-1]]` | `(N, 1)` | 二维矩阵 ✅ |

> 与第 5 集「双层括号」是同一个道理：PyTorch 期望 batch 标签是 `(N, 1)` 矩阵。

### `torch.from_numpy` 注意

- NumPy 数组和 Tensor **共享内存**
- 改 Tensor 可能影响原 NumPy 数组

---

## 五、模型变化：从 1 维到 8 维

### 方案 A：单层逻辑回归（8 维 → 1 维）

与第 6 集结构相同，只改 `Linear` 的输入维度：

```python
class Model(torch.nn.Module):
    def __init__(self):
        super(Model, self).__init__()
        self.linear = torch.nn.Linear(8, 1)   # 8 维输入，1 维输出
        self.sigmoid = torch.nn.Sigmoid()

    def forward(self, x):
        x = self.sigmoid(self.linear(x))
        return x
```

数学形式：

\[
z = w_1 x_1 + w_2 x_2 + \cdots + w_8 x_8 + b
\]
\[
\hat{y} = \sigma(z)
\]

### 方案 B：多层神经网络（课件重点）

堆叠多个 Linear + Sigmoid，增加模型表达能力：

```python
class Model(torch.nn.Module):
    def __init__(self):
        super(Model, self).__init__()
        self.linear1 = torch.nn.Linear(8, 6)
        self.linear2 = torch.nn.Linear(6, 4)
        self.linear3 = torch.nn.Linear(4, 1)
        self.sigmoid = torch.nn.Sigmoid()   # 无参数，定义一次即可

    def forward(self, x):
        x = self.sigmoid(self.linear1(x))
        x = self.sigmoid(self.linear2(x))
        x = self.sigmoid(self.linear3(x))
        return x
```

计算图：

```
x(N×8) → Linear(8,6) → Sigmoid → Linear(6,4) → Sigmoid → Linear(4,1) → Sigmoid → y(N×1)
```

### `nn.Linear(in, out)` 矩阵维度

| 层 | 输入 shape | 权重 shape | 输出 shape |
|----|-----------|-----------|-----------|
| `Linear(8, 6)` | `(N, 8)` | `(6, 8)` | `(N, 6)` |
| `Linear(6, 4)` | `(N, 6)` | `(4, 6)` | `(N, 4)` |
| `Linear(4, 1)` | `(N, 4)` | `(1, 4)` | `(N, 1)` |

> **N** = 样本数（batch size），本课先用全量数据一次训练（第 8 集才引入 mini-batch）。

---

## 六、两种 Sigmoid 写法

| 写法 | 类型 | 用法 |
|------|------|------|
| `torch.sigmoid(x)` | 函数 | 第 6 集用法，`forward` 里直接调用 |
| `torch.nn.Sigmoid()` | 类（Module） | 第 7 集用法，在 `__init__` 定义，`forward` 里 `self.sigmoid(x)` |

两者效果相同。多层网络中常用 `nn.Sigmoid()` 实例，方便统一替换为 ReLU 等。

---

## 七、完整代码预览（听课前可先扫一眼）

```python
import numpy as np
import torch

# Step 1: 加载数据
xy = np.loadtxt('diabetes.csv.gz', delimiter=',', dtype=np.float32)
x_data = torch.from_numpy(xy[:, :-1])
y_data = torch.from_numpy(xy[:, [-1]])

# Step 2: 多层模型
class Model(torch.nn.Module):
    def __init__(self):
        super(Model, self).__init__()
        self.linear1 = torch.nn.Linear(8, 6)
        self.linear2 = torch.nn.Linear(6, 4)
        self.linear3 = torch.nn.Linear(4, 1)
        self.sigmoid = torch.nn.Sigmoid()

    def forward(self, x):
        x = self.sigmoid(self.linear1(x))
        x = self.sigmoid(self.linear2(x))
        x = self.sigmoid(self.linear3(x))
        return x

model = Model()

# Step 3: 损失 + 优化器
criterion = torch.nn.BCELoss(reduction='mean')   # 本课常用 mean
optimizer = torch.optim.SGD(model.parameters(), lr=0.1)

# Step 4: 训练（五步循环不变）
for epoch in range(100000):
    y_pred = model(x_data)
    loss = criterion(y_pred, y_data)
    print(epoch, loss.item())

    optimizer.zero_grad()
    loss.backward()
    optimizer.step()
```

**与第 6 集的变化**：数据来源、模型层数、`Linear` 输入维度、可能调整 lr 和 epoch 数。**训练循环不变。**

---

## 八、听课时要重点关注的

| 关注点 | 为什么重要 |
|--------|------------|
| `Linear(8, 1)` vs `Linear(1, 1)` | 理解输入维度怎么设 |
| 多层 Linear 串联 | 后面 CNN、RNN 都是层叠结构 |
| `xy[:, [-1]]` 取标签 | 保证 y 是 `(N, 1)` 矩阵 |
| `torch.from_numpy` | 从文件到 Tensor 的标准路径 |
| 全量 batch 训练 | 第 8 集会改成 mini-batch |
| `nn.Sigmoid()` vs `torch.sigmoid()` | 两种等价写法 |

---

## 九、与第 6 集的对照

| 项目 | 第 6 集 | 第 7 集 |
|------|--------|--------|
| 输入维度 | 1 | **8** |
| 数据来源 | 手写 Tensor | **CSV 文件** |
| 模型 | 单层 Linear + Sigmoid | **多层 Linear + Sigmoid** |
| 样本数 | 3 | **数百～数千** |
| 损失 | BCELoss | BCELoss（可能改 `reduction='mean'`） |
| 训练循环 | 五步 | **完全相同** |

---

## 十、预习动手任务（20 分钟）

在听第 7 集之前，建议完成：

- [ ] 下载 `diabetes.csv.gz`，放到项目 `data/` 目录
- [ ] 用 `np.loadtxt` 加载，打印 `x_data.shape` 和 `y_data.shape`
- [ ] 确认 `y_data` 是 `(N, 1)` 而不是 `(N,)`
- [ ] 把第 6 集代码里的 `Linear(1, 1)` 改成 `Linear(8, 1)`，观察能否跑通
- [ ] 可选：数一下数据集有多少行（样本数 N）

---

## 十一、常见坑（提前避雷）

| 问题 | 原因 | 解决 |
|------|------|------|
| 维度不匹配 | `Linear(1,1)` 用了 8 维输入 | 改成 `Linear(8, 1)` |
| y shape 不对 | 用了 `[:, -1]` | 改成 `[:, [-1]]` |
| 找不到文件 | CSV 路径错误 | 检查相对路径或用绝对路径 |
| loss 下降极慢 | lr 太小或 epoch 不够 | 课件可能用 `lr=0.1`，epoch 更大 |
| 最后一层 Sigmoid 缺失 | 输出不在 (0,1) | BCELoss 要求概率输入 |
| ReLU 最后一层不加 Sigmoid | ReLU 输出可负，BCE 报错 | 二分类最后一层用 Sigmoid |

---

## 十二、与后续课程的关系

| 第 7 集学会 | 在哪用到 |
|------------|----------|
| 多维 `nn.Linear` | 所有深度学习模型 |
| 多层网络堆叠 | CNN、RNN、Transformer |
| CSV 数据加载 | 第 8 集封装成 Dataset |
| 全量 → mini-batch | **第 8 集 DataLoader** |
| BCELoss 二分类 | 第 9 集扩展到多分类 |

---

## 十三、第 8 集预告（了解即可）

第 7 集用**全量数据**一次喂给模型。第 8 集会引入：

```python
from torch.utils.data import Dataset, DataLoader
```

- 自定义 `Dataset` 类（`__init__`、`__getitem__`、`__len__`）
- `DataLoader` 实现 mini-batch、shuffle、多线程加载

第 7 集先掌握「多维 + 多层」，第 8 集再学「怎么高效喂数据」。

---

## 十四、一句话预习总结

> 第 7 集 = 第 6 集逻辑回归 + **8 维真实数据** + **多层 Linear**；从 CSV 读数据，`Linear(8,6→6,4→4,1)` 堆叠，训练循环不变；为第 8 集 Dataset/DataLoader 打基础。

---

*听课建议：先跑通数据加载，确认 shape 正确，再一层层加 Linear；每加一层就 print 输出 shape 验证维度。*
