# PyTorch 深度学习实践 · 第十课 卷积神经网络基础 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=10)  
> **集数**：第 10 集 · 卷积神经网络（基础篇）  
> **时长**：约 66 分钟  
> **整理日期**：2026-07-16  
> **前置**：第 9 集多分类（MNIST、CrossEntropyLoss、准确率评估）

---

## 一、本课定位

第 9 集用**全连接网络**处理 MNIST：先把 28×28 展平成 784，再堆 `Linear`。  
第 10 集引入 **CNN（Convolutional Neural Network）**：保留图像空间结构，用卷积/池化提取局部特征，再接分类头。

```
第 9 集（全连接）            第 10 集（CNN）
─────────────────           ─────────────────
view → 784 维向量            保留 (C,H,W) 空间结构
多层 Linear + ReLU           Conv2d + ReLU + MaxPool2d
参数多、忽略邻域关系         权值共享、捕捉局部图案
数据 / 损失 / 循环           **完全复用第 9 集**
```

**本课核心**：理解卷积与池化；能写出 MNIST 小型 CNN；**训练框架不变，只换模型结构**。

---

## 二、为什么图像不要一上来就展平？

### 全连接的问题

| 问题 | 说明 |
|------|------|
| 丢失空间信息 | 相邻像素展平后可能离得很远 |
| 参数爆炸 | 784→512 就有 40 万+ 参数 |
| 无局部性 | 每个输入节点都参与每个输出计算 |

### CNN 的优势

| 优势 | 说明 |
|------|------|
| **局部连接** | 每个卷积核只看一小块区域 |
| **权值共享** | 同一个核扫遍整张图 |
| **保留空间结构** | 特征图仍按 (C,H,W) 组织 |
| **平移不变性** | 图案挪位置，仍可能被同一核识别 |

直观：数字「7」的笔画，用一个小滤波器扫整张图即可识别，不必为每个像素位置单独学一套权重。

---

## 三、卷积（Convolution）

### 核心规则

| 规则 | 说明 |
|------|------|
| 卷积核个数 = 输出通道数 | m 个核 → m 张特征图 |
| 每个核的通道数 = 输入通道数 | 3 通道输入 → 每个核也是 3 通道 |
| 核大小自定 | 与图像大小无关（常见 3×3、5×5） |
| 权值共享 | 对图像每个位置用**相同**的核 |

### 单通道例子

```
输入: (1, 5, 5)    卷积核: (1, 1, 3, 3)
输出: (1, 3, 3)    高宽各减 (K-1)
```

### 多通道

RGB 图像 `(3, H, W)`：每个 patch 是 `3×3×3` 的张量，与核做加权求和，得到一个输出值。

---

## 四、卷积层 `nn.Conv2d`

```python
torch.nn.Conv2d(
    in_channels,     # 输入通道数（灰度=1，RGB=3）
    out_channels,    # 输出通道数 = 卷积核个数
    kernel_size,     # 核大小，如 5 或 (5,5)
    stride=1,        # 滑动步长
    padding=0,       # 边缘补零
)
```

### 输入 / 输出形状（NCHW）

```
输入: (N, C_in,  H_in,  W_in)
输出: (N, C_out, H_out, W_out)
```

### 输出尺寸公式

\[
H_{out} = \left\lfloor \frac{H_{in} + 2P - K}{S} \right\rfloor + 1
\]

| 参数 | 含义 |
|------|------|
| \(K\) | kernel_size |
| \(P\) | padding |
| \(S\) | stride |

**MNIST 典型（k=5, p=0, s=1）**：28 → 24 →（池化后 12）→ 8 →（池化后 4）

---

## 五、Padding（填充）

在图像四周补 0，控制输出尺寸。

| 卷积核 | 保持尺寸不变的 padding |
|--------|------------------------|
| 3×3 | padding=1 |
| 5×5 | padding=2 |

> 公式：`padding = K // 2`（当 stride=1 时，输出与输入同尺寸）。

---

## 六、Stride（步长）

卷积核每次滑动的步数。stride 越大，输出越小。

```
5×5 输入, 3×3 核, stride=2 → 输出约 2×2
```

---

## 七、最大池化 `nn.MaxPool2d`

**下采样**：缩小高宽，保留主要响应，增大感受野。

```python
torch.nn.MaxPool2d(kernel_size=2)   # 默认 stride=kernel_size
```

| 特点 | 说明 |
|------|------|
| **无权重** | 不做学习，只做取最大值 |
| 通道数不变 | 只缩 H、W |
| 2×2 池化 | 高宽各约减半 |

```
(N, 10, 24, 24) → MaxPool2d(2) → (N, 10, 12, 12)
```

### 卷积 vs 池化

| | 卷积层 | 池化层 |
|---|--------|--------|
| 作用 | 特征提取（找图案） | 下采样（降尺寸） |
| 权重 | 有，需要学习 | 无 |
| 通道 | 可变 | 不变 |

---

## 八、MNIST 经典 CNN 尺寸推演（必会）

```
输入          (N,  1, 28, 28)
Conv2d(1→10, k=5)   → (N, 10, 24, 24)
MaxPool2d(2)        → (N, 10, 12, 12)
Conv2d(10→20, k=5)  → (N, 20,  8,  8)
MaxPool2d(2)        → (N, 20,  4,  4)
view / flatten      → (N, 320)     # 20×4×4=320
Linear(320→10)      → (N, 10) logits
```

> **320 怎么来的**：最后一层特征图 `20 通道 × 4 高 × 4 宽 = 320`。写 `Linear` 前必须手算或 `print(x.shape)`。

---

## 九、模型代码

```python
import torch
import torch.nn.functional as F

class Net(torch.nn.Module):
    def __init__(self):
        super(Net, self).__init__()  # 调用父类构造，注册子模块
        # 第1个卷积层：输入1通道(灰度)，输出10张特征图，核大小5×5
        # 输入 (N,1,28,28) → 输出 (N,10,24,24)
        self.conv1 = torch.nn.Conv2d(1, 10, kernel_size=5)
        # 第2个卷积层：输入10通道，输出20张特征图，核大小5×5
        # 输入 (N,10,12,12) → 输出 (N,20,8,8)
        self.conv2 = torch.nn.Conv2d(10, 20, kernel_size=5)
        # 最大池化：2×2窗口取最大值，高宽约减半，通道数不变，无权重
        self.pooling = torch.nn.MaxPool2d(2)
        # 全连接分类头：把 20×4×4=320 维特征映射到 10 类 logits
        self.fc = torch.nn.Linear(320, 10)

    def forward(self, x):
        # x 输入 shape: (N, 1, 28, 28)，不要先 view 成 784
        batch_size = x.size(0)  # 取出当前 batch 大小（最后一批可能不满64）
        # Conv → ReLU → Pool： (N,1,28,28) → (N,10,24,24) → (N,10,12,12)
        x = self.pooling(F.relu(self.conv1(x)))
        # Conv → ReLU → Pool： (N,10,12,12) → (N,20,8,8) → (N,20,4,4)
        x = self.pooling(F.relu(self.conv2(x)))
        # 展平为二维： (N,20,4,4) → (N,320)，才能接 Linear
        x = x.view(batch_size, -1)
        # 输出 10 维 logits；不过 Softmax（CrossEntropyLoss 内部会做）
        x = self.fc(x)
        return x
```

| 点 | 说明 |
|----|------|
| 输入**不要**先 `view(-1,784)` | 卷积要保留 4D `(N,C,H,W)` |
| 展平放在**卷积之后** | 再接全连接分类头 |
| `Linear(320,10)` | 320 必须与特征图尺寸一致 |
| 损失仍是 `CrossEntropyLoss` | 与第 9 集相同 |

---

## 十、数据与训练循环（几乎原封不动）

```python
import torch
from torch import optim
from torch.utils.data import DataLoader
from torchvision import datasets, transforms

batch_size = 64
transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.1307,), (0.3081,))
])

train_loader = DataLoader(
    datasets.MNIST('./dataset/mnist', train=True, download=True, transform=transform),
    batch_size=batch_size, shuffle=True
)
test_loader = DataLoader(
    datasets.MNIST('./dataset/mnist', train=False, download=True, transform=transform),
    batch_size=batch_size, shuffle=False
)

model = Net()
criterion = torch.nn.CrossEntropyLoss()
optimizer = optim.SGD(model.parameters(), lr=0.01, momentum=0.5)

def train(epoch):
    running_loss = 0.0
    for batch_idx, (inputs, target) in enumerate(train_loader):
        optimizer.zero_grad()
        outputs = model(inputs)
        loss = criterion(outputs, target)
        loss.backward()
        optimizer.step()
        running_loss += loss.item()

def test():
    correct = 0
    total = 0
    with torch.no_grad():
        for inputs, target in test_loader:
            outputs = model(inputs)
            _, predicted = torch.max(outputs.data, dim=1)
            total += target.size(0)
            correct += (predicted == target).sum().item()
    print(f'Accuracy: {100 * correct / total:.2f}%')
```

| 保持不变 | 本课新写 |
|----------|----------|
| MNIST + transform | `Conv2d` / `MaxPool2d` |
| `CrossEntropyLoss` | 尺寸推演与 `Linear(320,10)` |
| epoch × batch 双层循环 | CNN 的 `forward` |
| `torch.max` 算准确率 | （可选）GPU |

---

## 十一、GPU 加速（课件涉及）

```python
device = torch.device("cuda:0" if torch.cuda.is_available() else "cpu")
model.to(device)

# 训练循环里
inputs, target = inputs.to(device), target.to(device)
```

| 步骤 | 说明 |
|------|------|
| 1. `model.to(device)` | 模型参数搬到 GPU |
| 2. `inputs.to(device)` | 每个 batch 的数据也要搬 |
| 3. `target.to(device)` | 标签同样要搬 |

> 没有 NVIDIA 显卡用 CPU 完全能跑 MNIST，本课重点是理解 CNN 结构。

---

## 十二、与第 9 集的对照

| 项目 | 第 9 集 | 第 10 集 |
|------|--------|--------|
| 任务 / 数据 | MNIST 10 类 | **相同** |
| 损失 / 标签 | CE + long | **相同** |
| 训练循环 | epoch × batch | **相同** |
| 输入处理 | 立刻 `view(-1,784)` | **先 Conv/Pool，最后再展平** |
| 主干 | 多层 `Linear` | **`Conv2d` + `MaxPool2d`** |
| 分类头 | 多层 FC | 通常 **一层 `Linear(320,10)`** |
| 关键 shape | `(N,784)` | `(N,C,H,W)` → `(N,320)` |

---

## 十三、常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| `Linear` 维度不匹配 | 320 算错 / 改过核大小 | `view` 前 `print(x.shape)` |
| 一上来就 `view(-1,784)` | 套了第 9 集习惯 | 卷积前保持 4D |
| `Conv2d` 报通道错 | `in_channels` 对不上 | 第一层 1，第二层=上一层 out |
| 用了 3 通道写法 | MNIST 是灰度 | 第一层 `in_channels=1` |
| 最后又加 Softmax | 与 CE 重复 | 输出 logits 即可 |
| `view(-1,320)` 写死 batch | 最后 batch 可能不满 | 用 `x.size(0)` |
| GPU 报错 | 只搬了 model 没搬 data | inputs/target 也要 `.to(device)` |

---

## 十四、本课你需要记住的

1. **CNN** = 卷积提取特征 + 池化降采样 + 全连接分类
2. **`nn.Conv2d(in, out, k)`**：in=输入通道，out=核个数=输出通道
3. **`nn.MaxPool2d(2)`**：无权重，通道不变，高宽约减半
4. **展平时机**：卷积/池化**之后**，全连接**之前**
5. **320 = 20×4×4**：写 `Linear` 输入维的依据
6. **训练循环不变**：数据、CE、准确率评估与第 9 集相同
7. 第 11 集会学 **Inception、ResNet** 等更复杂结构

---

## 十五、与后续课程的关系

| 第 10 集学会 | 在哪用到 |
|------------|----------|
| `Conv2d` / `MaxPool2d` | 几乎所有视觉模型 |
| 特征图尺寸推演 | 设计网络、改结构 |
| CNN + CE + 准确率 | 图像分类标准流水线 |
| GPU 基本用法 | 更大模型、更大数据 |
| 基础 CNN | **第 11 集**：Inception / ResNet |

---

## 十六、自测清单

- [ ] 能解释全连接处理图像的 3 个问题
- [ ] 能手算 MNIST CNN：28→24→12→8→4→320
- [ ] 能独立写出 `Conv2d` + `MaxPool2d` + `Linear` 的 `forward`
- [ ] 知道卷积核个数 = 输出通道数
- [ ] 知道池化无权重、通道不变
- [ ] 能把第 9 集 MNIST 代码换成 CNN 并跑通
- [ ] 知道 `padding` 保持尺寸的用法
- [ ] （可选）会用 `model.to(device)` 和 `inputs.to(device)`

---

## 十七、一句话总结

> 第 10 集 = 第 9 集 MNIST 训练流水线不变 + **用 Conv2d / MaxPool2d 提取空间特征** + **卷积后再展平接 Linear**；从「把图像当长向量」升级为「真正按图像结构建模」。

---

*下一课：第十一课 · 卷积神经网络（高级篇）—— Inception 模块、1×1 卷积、ResNet 残差连接*
