# PyTorch 深度学习实践 · 第九课 多分类 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=9)  
> **集数**：第 9 集 · 多分类问题（Softmax Classifier）  
> **时长**：约 50 分钟  
> **整理日期**：2026-07-14  
> **前置**：第 8 集 Dataset / DataLoader 双层训练循环

---

## 一、本课定位

第 6～8 集都是**二分类**（1 个输出 + Sigmoid + BCELoss）。  
第 9 集解决**多分类**：输出 C 个类别分数，用 Softmax / CrossEntropyLoss，数据换成 **MNIST 手写数字（10 类）**。

```
第 8 集（二分类）            第 9 集（多分类）
─────────────────           ─────────────────
输出：1 维概率               输出：10 维 logits
激活：Sigmoid                最后一层不过 Softmax
损失：BCELoss                损失：CrossEntropyLoss
标签：float 0/1              标签：long 0~9
数据：diabetes（8 维）       数据：MNIST（28×28）
评估：看 loss                评估：准确率 accuracy
```

**本课核心**：DataLoader 双层循环不变；换 **MNIST + 展平 + 10 类输出 + CrossEntropyLoss + 准确率评估**。

---

## 二、Softmax：多分类的「概率化」

二分类用 Sigmoid 把 1 个实数压到 (0, 1)。  
多分类有 C 个原始分数 \(z_1,\ldots,z_C\)（logits），需要变成**概率分布**：

\[
\text{Softmax}(z_i) = \frac{e^{z_i}}{\sum_{j=1}^{C} e^{z_j}}
\]

| 性质 | 说明 |
|------|------|
| 每个输出 | ∈ (0, 1) |
| 总和 | = 1（构成完整概率分布） |
| 直观 | 分数越大，对应类别概率越大 |

```python
import torch.nn.functional as F

z = torch.tensor([2.0, 1.0, 0.1])
prob = F.softmax(z, dim=0)   # ≈ [0.66, 0.24, 0.10]
```

> Softmax 本课**理解即可**；真正用损失时通常**不**手写 Softmax（见下一节）。

---

## 三、CrossEntropyLoss（最重要）

多分类损失：**真实类别的预测概率越高，loss 越小**。

### 内部等价关系

```
CrossEntropyLoss  ≡  LogSoftmax + NLLLoss
```

PyTorch 合并成一个算子，数值更稳定。

### 输入要求（易错）

| 项目 | 要求 |
|------|------|
| 模型输出 | **原始 logits**，shape `(N, C)`，**不要**先 Softmax |
| 标签 | **`long`**，shape `(N,)`，取值 `0 ~ C-1` |
| 损失 | `nn.CrossEntropyLoss()` |

```python
z = torch.tensor([[2.0, 1.0, 0.1]])   # 1 个样本，3 类
y = torch.LongTensor([0])             # 真实是第 0 类

criterion = torch.nn.CrossEntropyLoss()
loss = criterion(z, y)
```

### 与 BCELoss 对照

| | 二分类（第 6～8 集） | 多分类（第 9 集） |
|--|---------------------|------------------|
| 损失 | `BCELoss` | `CrossEntropyLoss` |
| 最后一层 | 要 Sigmoid | **不要 Softmax** |
| 标签 dtype | `float` | **`long`** |
| 标签 shape | `(N, 1)` | **`(N,)`** |

> 口诀：**CE 吃 logits + 长整型类别号；不要再加 Softmax。**

---

## 四、MNIST 数据集

| 集合 | 样本数 | 内容 |
|------|--------|------|
| 训练集 | 60,000 | 28×28 灰度手写数字 |
| 测试集 | 10,000 | 同上，用于算准确率 |

标签是整数 **0～9**（10 类）。

### torchvision 加载

```python
from torchvision import datasets, transforms
from torch.utils.data import DataLoader

transform = transforms.Compose([
    transforms.ToTensor(),                      # → [0,1] tensor
    transforms.Normalize((0.1307,), (0.3081,))   # MNIST 均值/标准差
])

train_dataset = datasets.MNIST('./dataset/mnist', train=True, download=True, transform=transform)
test_dataset = datasets.MNIST('./dataset/mnist', train=False, download=True, transform=transform)

train_loader = DataLoader(train_dataset, batch_size=64, shuffle=True)
test_loader = DataLoader(test_dataset, batch_size=64, shuffle=False)
```

| 项 | shape |
|----|-------|
| 一个 batch 图像 | `(64, 1, 28, 28)` → batch × 通道 × 高 × 宽 |
| 一个 batch 标签 | `(64,)` → 整数 0～9 |

本课用官方 `datasets.MNIST`，不再手写 Dataset；**DataLoader 用法与第 8 集相同**。

---

## 五、`x.view(-1, 784)`：图像展平

全连接层要一维向量，不能直接吃 `(1, 28, 28)`：

```python
x = x.view(-1, 28 * 28)   # (64, 1, 28, 28) → (64, 784)
```

| 参数 | 含义 |
|------|------|
| `-1` | 自动推断 batch 维 |
| `784` | 28 × 28，像素展平后的特征数 |

`view` 只改形状，不改数据内容。

---

## 六、模型结构

本课不用 CNN，用**多层全连接 + ReLU**，最后一层输出 10 维 logits：

```
(64,1,28,28) → view → (64,784)
  → Linear(784,512) → ReLU
  → Linear(512,256) → ReLU
  → Linear(256,128) → ReLU
  → Linear(128,64)  → ReLU
  → Linear(64,10)   → logits
  → CrossEntropyLoss
```

```python
class Net(torch.nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.l1 = torch.nn.Linear(784, 512)
        self.l2 = torch.nn.Linear(512, 256)
        self.l3 = torch.nn.Linear(256, 128)
        self.l4 = torch.nn.Linear(128, 64)
        self.l5 = torch.nn.Linear(64, 10)

    def forward(self, x):
        x = x.view(-1, 784)
        x = torch.relu(self.l1(x))
        x = torch.relu(self.l2(x))
        x = torch.relu(self.l3(x))
        x = torch.relu(self.l4(x))
        return self.l5(x)          # 最后一层不过激活
```

隐层用 **ReLU**（比第 7～8 集一直 Sigmoid 更常见）；输出层只出 logits。

---

## 七、训练与测试

### 优化器新增 momentum

```python
optimizer = torch.optim.SGD(model.parameters(), lr=0.01, momentum=0.5)
```

`momentum` 利用历史梯度方向，加速收敛、减轻震荡。

### 训练（双层循环 + 五步更新）

```python
def train(epoch):
    running_loss = 0.0
    for batch_idx, (inputs, labels) in enumerate(train_loader):
        optimizer.zero_grad()
        outputs = model(inputs)
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()

        running_loss += loss.item()
        if batch_idx % 300 == 299:
            print('[%d, %5d] loss: %.3f' % (epoch + 1, batch_idx + 1, running_loss / 300))
            running_loss = 0.0
```

与第 8 集相比：**循环结构不变**，只是数据、模型和损失换成多分类版本。

### 测试（算准确率）

```python
def test():
    correct = 0
    total = 0
    with torch.no_grad():
        for inputs, labels in test_loader:
            outputs = model(inputs)
            _, predicted = torch.max(outputs.data, dim=1)
            total += labels.size(0)
            correct += (predicted == labels).sum().item()
    print('Accuracy: %d %%' % (100 * correct / total))
```

| API | 作用 |
|-----|------|
| `torch.max(outputs, dim=1)` | 每行取最大 logit 的**列索引** = 预测类别 |
| `torch.no_grad()` | 测试不算梯度，省内存、加快速度 |
| `model.train()` / `model.eval()` | 训练/评估模式（有 Dropout/BN 时更关键） |

全连接 MNIST 训约 10 个 epoch，测试准确率通常可到 **90%+～97%** 量级。

---

## 八、完整代码骨架

```python
import torch
from torch.utils.data import DataLoader
from torchvision import datasets, transforms

transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.1307,), (0.3081,))
])

train_loader = DataLoader(
    datasets.MNIST('./dataset/mnist', train=True, download=True, transform=transform),
    batch_size=64, shuffle=True
)
test_loader = DataLoader(
    datasets.MNIST('./dataset/mnist', train=False, download=True, transform=transform),
    batch_size=64, shuffle=False
)

class Net(torch.nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.l1 = torch.nn.Linear(784, 512)
        self.l2 = torch.nn.Linear(512, 256)
        self.l3 = torch.nn.Linear(256, 128)
        self.l4 = torch.nn.Linear(128, 64)
        self.l5 = torch.nn.Linear(64, 10)

    def forward(self, x):
        x = x.view(-1, 784)
        x = torch.relu(self.l1(x))
        x = torch.relu(self.l2(x))
        x = torch.relu(self.l3(x))
        x = torch.relu(self.l4(x))
        return self.l5(x)

model = Net()
criterion = torch.nn.CrossEntropyLoss()
optimizer = torch.optim.SGD(model.parameters(), lr=0.01, momentum=0.5)

for epoch in range(10):
    train(epoch)   # 见上一节
    test()
```

---

## 九、与第 8 集的对照

| 项目 | 第 8 集 | 第 9 集 |
|------|--------|--------|
| 任务 | 二分类 | **10 分类** |
| 数据 | 自定义 Dataset | **`datasets.MNIST`** |
| 输入 | `(batch, 8)` | `(batch,1,28,28)` → **view 成 784** |
| 输出 | 1 维 + Sigmoid | **10 维 logits** |
| 损失 | BCELoss | **CrossEntropyLoss** |
| 标签 | float `(N,1)` | **long `(N,)`** |
| 训练循环 | epoch × batch | **相同** |
| 评估 | 主要看 loss | **准确率** |
| 优化器 | SGD | SGD + **momentum** |

---

## 十、二分类 vs 多分类决策表

| 场景 | 输出层 | 损失 | 标签 |
|------|--------|------|------|
| 二分类（课件） | 1 维 + Sigmoid | BCELoss | float 0/1 |
| 二分类（工程） | 1 维 logits | BCEWithLogitsLoss | float 0/1 |
| **多分类** | **C 维 logits** | **CrossEntropyLoss** | **long 0~C-1** |

---

## 十一、常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| CE 报错 | 标签是 float | 用 `long` / `LongTensor` |
| 标签 shape 错 | 写成了 `(N,1)` | 改为 `(N,)` |
| 最后又加 Softmax | 与 CE 内部重复 | 去掉，直接输出 logits |
| 误用 Sigmoid + CE | 二分类写法硬套 | 多分类用 CE |
| `view` 报错 | 元素个数对不上 | 确认 1×28×28=784 |
| 准确率极低 | 忘了 view / 标签类型错 | 先 print shape、dtype |
| 测试也 shuffle | 没必要 | `shuffle=False` |
| MNIST 下载失败 | 网络 | 手动放到 `root` 或换镜像 |

---

## 十二、本课你需要记住的

1. **Softmax** 把 C 个 logits 变成概率分布（总和为 1）
2. **`CrossEntropyLoss` = LogSoftmax + NLL**，输入是 **logits + long 标签**
3. 最后一层**不要** Softmax（和二分类「要 Sigmoid」正好相反）
4. MNIST：`ToTensor` + `Normalize` + DataLoader
5. **`x.view(-1, 784)`** 把图像喂给全连接层
6. 预测类别用 **`torch.max(..., dim=1)`** 的下标
7. 测试用 **`torch.no_grad()`**，主要指标是 **accuracy**
8. 双层训练循环与第 8 集相同，换的是「数据 + 模型输出 + 损失」

---

## 十三、与后续课程的关系

| 第 9 集学会 | 在哪用到 |
|------------|----------|
| Softmax / CrossEntropyLoss | 几乎所有多分类任务 |
| MNIST + torchvision | 图像分类入门标配 |
| `view` 展平 | 全连接处理图像的过渡写法 |
| 准确率评估 | 分类项目通用 |
| 全连接 MNIST ~97% | **第 10 集 CNN** 会利用空间结构再提升 |

---

## 十四、自测清单

- [ ] 能写出 Softmax 的作用（变概率、总和为 1）
- [ ] 知道用 CE 时为什么最后一层不要 Softmax
- [ ] 能说明 CE 标签为什么必须是 `long`、shape `(N,)`
- [ ] 能独立写出 MNIST 的 `transform` + DataLoader
- [ ] 能解释 `(64,1,28,28)` 如何变成 `(64,784)`
- [ ] 能用 `torch.max` 从 `(N,10)` 得到预测类别
- [ ] 能独立写出 `train()` + `test()` 两个函数
- [ ] 能对比二分类 / 多分类在输出层与损失上的差异

---

## 十五、一句话总结

> 第 9 集 = 第 8 集 DataLoader 双层循环不变 + **MNIST** + **`view` 成 784** + 输出改 **10 维 logits** + **BCE 换成 CrossEntropyLoss** + 用 **准确率**评估；这是从二分类迈向多分类的关键一步。

---

*下一课：第十课 · 卷积神经网络（Conv2d、MaxPool2d，同样用 MNIST，准确率再提升）*
