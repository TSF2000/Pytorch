# PyTorch 深度学习实践 · 第十三课 循环神经网络高级 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=13)  
> **集数**：第 13 集 · 循环神经网络（高级篇）  
> **时长**：约 70～80 分钟  
> **整理日期**：2026-08-08  
> **前置**：第 12 集 RNN 基础（维度、`nn.RNN`、隐藏状态）；第 8～9 集 DataLoader 与 CrossEntropyLoss

---

## 一、本课定位

第 12 集用 `hello → ohlol` 搞懂 Cell 与维度。  
第 13 集完成**课程级真实任务**：输入人名，预测所属语言/国家（**18 分类**）。

```
第 12 集（基础）                 第 13 集（高级）
─────────────────────           ─────────────────────
逐步字符多分类                   整段序列一个类别
One-Hot + RNN                    Embedding + GRU
定长、小 batch                   变长 Padding + pack
单向玩具网                       双向、多层分类器
```

**本课核心**：Embedding、序列分类头、变长批处理（pad → 排序 → pack）、双向 GRU。

---

## 二、任务定义

| 项目 | 内容 |
|------|------|
| 输入 | 人名，如 `Maclean` |
| 输出 | 18 国/语言之一 |
| 编码 | 字符 → `ord(c)` → ASCII 下标（字典大小 128） |
| 损失 | `CrossEntropyLoss` |
| 数据 | `names_train.csv.gz` / `names_test.csv.gz` |

与第 12 集最大差别：**标签在「整条名字」上，不在每个字符上**。因此通常只用 RNN/GRU 的**最终隐藏状态**接全连接，而不是每一步都算 CE。

---

## 三、Embedding：替代 One-Hot

```python
self.embedding = nn.Embedding(input_size=128, embedding_dim=hidden_size)
# 输入 LongTensor 下标: (seq_len, batch)
# 输出: (seq_len, batch, hidden_size)
```

| | One-Hot（第 12 集） | Embedding（第 13 集） |
|--|---------------------|------------------------|
| 输入形态 | 稀疏 float 向量 | **整数下标** |
| 维度 | = 字典大小 | 自定（如 100） |
| 参数 | 无 | **可学习查表** |

`Embedding(num_embeddings, embedding_dim)`：第一维是「有多少种 token」，第二维是「每个 token 变成多长的向量」。

---

## 四、为什么只要最终 hidden？

序列标注（第 12 集）：每步都要输出 → 用完整 `out`。  
序列分类（本课）：只要「整段像哪一类」→ 用最后时刻的表示即可：

```
字符序列 → Embedding → GRU → 最终 hidden → Linear(→18) → CE
```

双向时，把正向最后一步与反向最后一步 **`torch.cat`** 再送入 Linear。

---

## 五、GRU 与双向

### GRU

API 与 `nn.RNN` 类似，门控结构更稳，课件用：

```python
self.gru = nn.GRU(hidden_size, hidden_size, n_layers, bidirectional=True)
```

### 双向 BiRNN / BiGRU

```
正向：从左到右扫完整名字
反向：从右到左再扫一遍
分类前：拼接两端最终状态
```

| 项目 | shape / 宽度 |
|------|----------------|
| h0 | `(n_layers * n_directions, batch, hidden_size)` |
| 单向送入 FC | `hidden[-1]` → 宽 `H` |
| 双向送入 FC | `cat([hidden[-1], hidden[-2]], dim=1)` → 宽 `2H` |
| FC | `Linear(H * n_directions, 18)` |

---

## 六、变长序列：Padding → 排序 → Pack

### 1. 名字 → 下标列表

```python
def name2list(name):
    arr = [ord(c) for c in name]
    return arr, len(arr)
```

### 2. Padding

batch 内按最长长度补 0，得到 `(batch, max_len)` 的 `LongTensor`。

### 3. 按长度降序排序

`pack_padded_sequence` 要求长度从大到小；**输入与标签用同一 `perm_idx` 重排**。

### 4. `pack_padded_sequence`

```python
emb = self.embedding(input)                 # (seq, batch, H)
packed = pack_padded_sequence(emb, seq_lengths.cpu())
output, hidden = self.gru(packed, h0)
```

作用：让 GRU **不计 padding 位置**，提速且减少噪声。

### `make_tensors` 流程（务必记住）

```
names (字符串 batch)
  → ASCII 列表 + 真实长度
  → 零矩阵 pad
  → 按长度降序重排（seq 与 country 同步）
  → 返回 seq_tensor, seq_lengths, countries
```

---

## 七、模型完整骨架

```python
class RNNClassifier(torch.nn.Module):
    def __init__(self, input_size, hidden_size, output_size,
                 n_layers=1, bidirectional=True):
        super().__init__()
        self.hidden_size = hidden_size
        self.n_layers = n_layers
        self.n_directions = 2 if bidirectional else 1
        self.embedding = torch.nn.Embedding(input_size, hidden_size)
        self.gru = torch.nn.GRU(
            hidden_size, hidden_size, n_layers, bidirectional=bidirectional
        )
        self.fc = torch.nn.Linear(hidden_size * self.n_directions, output_size)

    def _init_hidden(self, batch_size):
        return torch.zeros(
            self.n_layers * self.n_directions, batch_size, self.hidden_size
        )

    def forward(self, input, seq_lengths):
        # DataLoader 侧常是 (B, S)，RNN 默认要 (S, B)
        input = input.t()
        batch_size = input.size(1)
        hidden = self._init_hidden(batch_size)
        embedding = self.embedding(input)
        gru_input = pack_padded_sequence(embedding, seq_lengths)
        _, hidden = self.gru(gru_input, hidden)
        if self.n_directions == 2:
            hidden_cat = torch.cat([hidden[-1], hidden[-2]], dim=1)
        else:
            hidden_cat = hidden[-1]
        return self.fc(hidden_cat)
```

课件常见超参：`HIDDEN_SIZE=100`，`BATCH_SIZE=256`，`N_LAYER=2`，`N_CHARS=128`，`N_EPOCHS=100`，`Adam(lr=0.001)`。

---

## 八、Dataset 与训练循环要点

### NameDataset

- 读 gz + csv → `(name, country字符串)`  
- `country_list = sorted(set(...))` → 建 `country_dict`  
- `__getitem__` 返回 `(name, country_index)`  
- `getCountriesNum()` → 模型 `output_size`（18）

### 训练 / 测试

```python
inputs, seq_lengths, target = make_tensors(names, countries)
output = classifier(inputs, seq_lengths)   # (batch, 18)
loss = criterion(output, target)

# 测试
pred = output.max(dim=1, keepdim=True)[1]
correct += pred.eq(target.view_as(pred)).sum().item()
```

训练框架仍是五步法；增量主要在 **`make_tensors` + 模型 forward**。

---

## 九、维度速查

| 阶段 | shape（示意） |
|------|----------------|
| pad 后下标 | `(batch, max_seq)` → `.t()` → `(max_seq, batch)` |
| Embedding 后 | `(max_seq, batch, H)` |
| h0 | `(n_layers * n_dir, batch, H)` |
| 双向最终表示 | `(batch, 2H)` |
| FC 输出 | `(batch, 18)` |

---

## 十、与第 12 集对照

| 项目 | 第 12 集 | 第 13 集 |
|------|----------|----------|
| 任务类型 | 逐步预测 | **序列分类** |
| 编码 | One-Hot | **Embedding** |
| 单元 | RNNCell / RNN | **GRU** |
| 方向 | 单向 | **常双向** |
| 长度 | 固定 | **pad + sort + pack** |
| 损失 | 每步 CE | **一次 CE / 样本** |
| 批大小 | 常 1 | **大 batch + DataLoader** |

---

## 十一、常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| Embedding 类型错误 | 传入 float | 使用 `LongTensor` 下标 |
| pack 失败 | 未降序 | 长度 `sort(descending=True)` 并同步重排标签 |
| GPU + pack | lengths 在 CUDA | `seq_lengths.cpu()` 再 pack |
| Linear 输入维不对 | 忽略双向 | `H * n_directions` |
| 准确率异常 | padding 当有效字符算 | 使用 `pack_padded_sequence` |
| 每步都接损失 | 与第 12 集任务混淆 | 分类只用最终 hidden |
| 转置忘记 | B×S 直接喂默认 RNN | `input.t()` 或设 `batch_first`（课件用转置） |

---

## 十二、本课你需要记住的

1. **序列分类**：整段一个标签 → 最终 hidden（双向则 cat）→ Linear → CE  
2. **Embedding**：下标 → 可学习稠密向量，替代 One-Hot  
3. **变长三件套**：Padding、按长度排序、`pack_padded_sequence`  
4. **双向 GRU**：`n_directions=2`，FC 输入宽为 `2H`  
5. **ASCII 字典 128**：`ord(c)` 简单可靠（本课设定）  
6. **训练管线不变**：Dataset → make_tensors → 前向 → CE → Adam  

---

## 十三、课程收官：你串起来的能力

从第 1 集到第 13 集，一条完整深度学习流水线已经齐了：

| 模块 | 课次 |
|------|------|
| 模型 / 损失 / 优化器 | 第 2～5 集 |
| 激活与多维输入 | 第 6～7 集 |
| Dataset / DataLoader | 第 8 集 |
| 多分类 + CE | 第 9 集 |
| CNN（图像） | 第 10～11 集 |
| RNN 基础 → 高级（序列） | 第 12～13 集 |

后续自学方向：LSTM 细节、Attention、Transformer、`torchtext` / HuggingFace 等——本课的 Embedding + 序列分类骨架仍然适用。

---

## 十四、自测清单

- [ ] 能说明第 12 集与第 13 集在「损失作用位置」上的差别  
- [ ] 能写出 `Embedding` 输入/输出 shape  
- [ ] 能默写 pad → 排序 → pack 三步及为什么要排序  
- [ ] 能说出双向时 `hidden[-1]` / `hidden[-2]` 如何拼接  
- [ ] 知道 `FC` 输入维为什么是 `hidden_size * n_directions`  
- [ ] 能指认 `make_tensors` 里哪一步保证了 name 与 country 对齐  
- [ ] 能独立跟课件跑通训练并看到测试准确率上升  

---

## 十五、一句话总结

> 第 13 集 = **人名国家分类**：ASCII 下标 → **Embedding** → **pad/排序/pack** → **双向 GRU** → 拼接最终 hidden → Linear + CE。吃透「整段一个标签」和「变长批处理」，就具备了最基本的 NLP 分类范式。

---

*本课为《PyTorch 深度学习实践》系列收官。建议把第 12～13 集代码各敲一遍，对照 shape 打印，再尝试改成单向 / 换 LSTM / 改 hidden_size 做消融。*
