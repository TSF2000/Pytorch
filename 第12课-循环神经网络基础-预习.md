# PyTorch 深度学习实践 · 第十二课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=12)  
> **集数**：第 12 集 · 循环神经网络（基础篇）  
> **时长**：约 77 分钟  
> **整理日期**：2026-07-18  
> **重要程度**：⭐⭐⭐ **序列建模入门：RNNCell / RNN，NLP 与时间序列的基础**

---

## 一、第十二课在学什么

第 9～11 集处理的是**图像**（空间结构，CNN）。  
第 12 集进入**序列数据**（时间/顺序结构，RNN）：文字、语音、天气、股价等「前后有关」的数据。

```
第 9～11 集（CNN）           第 12 集（RNN）
─────────────────           ─────────────────
输入：(N, C, H, W)           输入：(seq_len, batch, input_size)
空间邻域相关                 时间先后相关
Conv / Pool                  RNNCell / RNN
MNIST 分类                   hello → ohlol（序列到序列）
```

**本课核心**：理解隐藏状态如何传递；分清 `RNNCell` 与 `RNN`；能用 One-Hot 把字符变成向量，训练一个小序列转换任务。

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 9 集 | `CrossEntropyLoss`、`torch.max` 取类别 |
| 第 5～7 集 | `nn.Module`、五步训练循环 |
| 线性代数直觉 | 向量、矩阵乘法 |
| 序列概念 | 「第 t 步依赖第 t-1 步」 |

有 Java 背景：把 RNN 想成**同一个函数在 for 循环里反复调用**，每次多带一个「记忆」参数 `hidden`。

---

## 三、为什么不用普通全连接？

### 例子：用前 3 天天气预测第 4 天是否下雨

| 做法 | 问题 |
|------|------|
| 把 3 天特征拼成一个长向量 → Linear | 丢掉「第 1 天→第 2 天→第 3 天」的先后顺序 |
| 每天单独一个网络 | 参数爆炸，且学不到「同一套规律」在每一天复用 |

**RNN 思路**：

- **同一套权重**处理每一个时间步（权值共享）
- 每一步把**上一步的隐藏状态** `h_{t-1}` 和下一步输入 `x_t` 一起算，得到 `h_t`
- 这样「记忆」可以沿时间传递

适用：自然语言、语音、金融序列、传感器时序等。

---

## 四、RNN Cell 在算什么

### 直觉

```
h0（初始，常全 0）
  + x1 → Cell → h1
  + x2 → Cell → h2
  + x3 → Cell → h3
  ...
```

- **Cell 本质**：带两个输入的线性变换 + `tanh`
- **共享权重**：每一步用的是**同一个** Cell（同一套 W）

### 公式（理解即可）

\[
h_t = \tanh(W_{ih} x_t + b_{ih} + W_{hh} h_{t-1} + b_{hh})
\]

| 符号 | 含义 |
|------|------|
| \(x_t\) | 当前时刻输入，`input_size` 维 |
| \(h_{t-1}\) | 上一时刻隐藏状态，`hidden_size` 维 |
| \(h_t\) | 当前隐藏状态（也常当作当前输出） |
| \(\tanh\) | 激活，输出约在 (-1, 1) |

`h0`：无先验时设为全 0；有先验（如图像生成文字）可用 CNN 特征当作 `h0`。

---

## 五、两种实现方式（听课重点）

### 方式 A：`nn.RNNCell` —— 自己写 for 循环

```python
cell = torch.nn.RNNCell(input_size=input_size, hidden_size=hidden_size)

# dataset: (seq_len, batch, input_size)
hidden = torch.zeros(batch_size, hidden_size)
for input_t in dataset:          # 每步: (batch, input_size)
    hidden = cell(input_t, hidden)  # → (batch, hidden_size)
```

| 张量 | shape |
|------|-------|
| 每步 input | `(batch, input_size)` |
| hidden | `(batch, hidden_size)` |

适合：搞懂「循环」本身；灵活控制每一步。

### 方式 B：`nn.RNN` —— 一次喂完整序列

```python
rnn = torch.nn.RNN(input_size, hidden_size, num_layers=1)

# inputs: (seq_len, batch, input_size)
# h0:     (num_layers, batch, hidden_size)
out, hidden = rnn(inputs, h0)
# out:    (seq_len, batch, hidden_size)  # 每一步的 h
# hidden: (num_layers, batch, hidden_size)  # 最后一层最后时刻
```

| 参数 | 含义 |
|------|------|
| `input_size` | 每个时间步特征维 |
| `hidden_size` | 隐藏状态维 |
| `num_layers` | 堆几层 RNN（纵向加深） |
| `batch_first=True` | 输入改成 `(batch, seq_len, input_size)` |

**关系**：`RNN` ≈ 内部对整个序列自动跑完 `RNNCell` 循环。

---

## 六、维度速查表（预习必背）

默认（`batch_first=False`）：

| 张量 | shape |
|------|-------|
| 整段输入 | `(seq_len, batch, input_size)` |
| 初始 h0 | `(num_layers, batch, hidden_size)` |
| 输出 out | `(seq_len, batch, hidden_size)` |
| 最终 hidden | `(num_layers, batch, hidden_size)` |

`batch_first=True` 时：输入/输出的前两维对调为 `(batch, seq_len, …)`，**h0 形状不变**。

听课时盯紧：**谁在第 0 维——是 seq 还是 batch**。

---

## 七、课件主任务：hello → ohlol

把字符串看成序列分类：

```
输入: h e l l o
输出: o h l o l
```

字符集 `{e, h, l, o}` → 4 类，本质是**每个时间步做一个 4 分类**。

### 字符怎么进网络？One-Hot

字符不能直接喂网络，先变成向量：

```
e → [1,0,0,0]
h → [0,1,0,0]
l → [0,0,1,0]
o → [0,0,0,1]
```

```python
idx2char = ['e', 'h', 'l', 'o']
x_data = [1, 0, 2, 2, 3]   # hello 的下标
y_data = [3, 1, 2, 3, 2]   # ohlol 的下标
# 再查表变成 one-hot，labels 用 LongTensor
```

损失：`CrossEntropyLoss`（与第 9 集多分类相同）。  
预测：对每一步输出 `torch.max` 取类别，再映射回字符。

---

## 八、代码骨架预览

### RNNCell 版（手写循环）

```python
class Model(torch.nn.Module):
    def __init__(self, input_size, hidden_size, batch_size):
        super(Model, self).__init__()
        self.batch_size = batch_size
        self.hidden_size = hidden_size
        self.rnncell = torch.nn.RNNCell(input_size, hidden_size)

    def forward(self, input, hidden):
        return self.rnncell(input, hidden)

    def init_hidden(self):
        return torch.zeros(self.batch_size, self.hidden_size)

# 训练时：
hidden = net.init_hidden()
for input_t, label_t in zip(inputs, labels):
    hidden = net(input_t, hidden)
    loss += criterion(hidden, label_t)
loss.backward()
optimizer.step()
```

### RNN 版（一次前向）

```python
class Model(torch.nn.Module):
    def __init__(self, input_size, hidden_size, batch_size, num_layers=1):
        super(Model, self).__init__()
        self.batch_size = batch_size
        self.hidden_size = hidden_size
        self.num_layers = num_layers
        self.rnn = torch.nn.RNN(input_size, hidden_size, num_layers)

    def forward(self, input):
        h0 = torch.zeros(self.num_layers, self.batch_size, self.hidden_size)
        out, _ = self.rnn(input, h0)
        return out.view(-1, self.hidden_size)  # 接到 CE

outputs = net(inputs)                 # (seq_len*batch, hidden)
loss = criterion(outputs, labels)     # labels: (seq_len*batch,)
```

优化器课件常用 `Adam(lr=0.1)`（序列短、玩具任务）。

---

## 九、听课时要重点关注的

| 关注点 | 为什么重要 |
|--------|------------|
| `h_t` 如何依赖 `h_{t-1}` | RNN「有记忆」的本质 |
| `RNNCell` vs `RNN` | 一个手写循环，一个自动循环 |
| `(seq, batch, feature)` 维度 | 最容易写错 |
| One-Hot 与字典 `idx2char` | 字符 ↔ 数字 ↔ 向量 |
| 每步 CE、整段 loss 求和/平均 | 序列损失怎么反传 |
| `hidden` 初始化为 0 | 每条序列开始前要重置 |

---

## 十、与 CNN 课的对照

| 项目 | CNN（第 10～11 集） | RNN（第 12 集） |
|------|---------------------|-----------------|
| 数据形态 | 图像网格 | 时间序列 / 字符串 |
| 核心层 | `Conv2d` / `MaxPool2d` | `RNNCell` / `RNN` |
| 「共享」 | 空间上卷积核共享 | 时间上 Cell 权重共享 |
| 典型 shape | `(N,C,H,W)` | `(seq,batch,input_size)` |
| 损失 | CE（整图一类） | CE（**每个时间步**一类） |

---

## 十一、预习动手任务（20～30 分钟）

- [ ] 用纸画出 `hello → ohlol` 五个时间步的 `x_t / h_t`
- [ ] 跑通一段 `RNNCell` 随机输入，打印每步 `hidden.shape`
- [ ] 跑通一段 `nn.RNN`，确认 `out.shape`、`hidden.shape`
- [ ] 手写 `e,h,l,o` 的 One-Hot 表，把 `hello` 转成 `(5,1,4)` Tensor
- [ ] 对比：`batch_first=False` 与 `True` 时输入 shape 差在哪
- [ ] 复习第 9 集：`CrossEntropyLoss` 要 logits + `long` 标签

---

## 十二、常见坑（提前避雷）

| 问题 | 原因 | 解决 |
|------|------|------|
| shape 对不上 | seq / batch 维放反 | 先 `print`，对照速查表 |
| 损失维度错 | labels 没和 out 对齐 | RNN 版常用 `view(-1, C)` |
| 每条样本不重置 h0 | 用了上一条的记忆 | 每个 epoch/每条序列前 `zeros` |
| One-Hot 用 float 标签 | CE 要类别下标 | labels 用 `LongTensor` |
| 把 RNN 输出再 Softmax 再 CE | 与第 9 集同样错误 | 直接 logits + CE |
| 字符维 input_size 设错 | 字典大小 ≠ One-Hot 维 | `input_size = 字符种类数` |

---

## 十三、与后续课程的关系

| 第 12 集学会 | 在哪用到 |
|------------|----------|
| RNN 隐藏状态 | LSTM / GRU、序列标注 |
| `nn.RNN` 用法 | 第 13 集人名分类等 |
| One-Hot | Embedding 的前身（课件可能提改进） |
| 序列 + CE | NLP、语音入门 |

第 13 集（高级篇 / 分类器）会把 RNN 用到更接近真实的分类任务；第 12 集先把 **Cell、维度、小例子** 吃透。

---

## 十四、对你学习路线的提醒

压缩方案里：

- ✅ **第 12 集必看**（RNN 基础，后面 NLP / Transformer 的前置直觉）
- CNN 第 10～11 集可跳过；若已看完，正好对比「空间 vs 时间」两种归纳偏置

---

## 十五、一句话预习总结

> 第 12 集 = **用共享权重的 Cell 沿时间传递隐藏状态**；先搞懂 `RNNCell` 手写循环，再用 `nn.RNN` 一次处理整段序列；用 One-Hot 把 `hello→ohlol` 变成逐步多分类。核心不是公式，而是 **`(seq, batch, feature)` 维度** 和 **`h_t` 如何接下一步**。

---

*听课建议：先画 5 步 hello 的数据流；再只跑维度 demo；最后跟课件把 Cell 版和 RNN 版都敲一遍，对比哪里少了 for 循环。*
