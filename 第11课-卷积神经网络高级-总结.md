# PyTorch 深度学习实践 · 第十一课 卷积神经网络高级 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=11)  
> **集数**：第 11 集 · 卷积神经网络（高级篇）  
> **时长**：约 60–70 分钟  
> **整理日期**：2026-07-16  
> **前置**：第 10 集 CNN 基础（Conv2d、MaxPool、MNIST 训练流水线、GPU）

---

## 一、本课定位

第 10 集学的是**串型 CNN**：一层接一层（Conv → Pool → Conv → Pool → FC）。  
第 11 集升级为**带分支的高级结构**：

| 结构 | 代表 | 核心思想 |
|------|------|----------|
| **Inception** | GoogLeNet | 同一层并行多种卷积核，训练自动选优 |
| **Residual** | ResNet | 跳跃连接（shortcut），缓解梯度消失，网络可以更深 |

```
第 10 集（基础 CNN）              第 11 集（高级 CNN）
─────────────────────            ─────────────────────
串型：上一层输出 → 下一层输入      分支：多路并行 / 跳跃相加
自己设计每层核大小                Inception：多种核一起试
层数一深就难训                    ResNet：残差让深层可训
数据 / 损失 / 循环                **仍完全复用 MNIST 流水线**
```

**本课核心**：会写 `InceptionA` 与 `ResidualBlock` 两个模块类；理解 `torch.cat`（拼接）与 `x + y`（残差相加）的区别；训练框架不变，只换模型。

---

## 二、串型 vs 分支结构

### 串型（第 10 集）

```
输入 → Conv → Pool → Conv → Pool → FC → 输出
```

每一层只有一个入口、一个出口。

### 分支型（本课）

```
Inception:          ResNet:
  输入                   输入
  ├─ 1×1 ─→              ├─ Conv → ReLU → Conv ─┐
  ├─ 1×1→5×5 ─→          │                      + → ReLU → 输出
  ├─ 1×1→3×3→3×3 ─→      └──────────────────────┘
  └─ Pool→1×1 ─→
       ↓
   torch.cat（沿通道拼）
```

| 操作 | Inception | ResNet |
|------|-----------|--------|
| 分支如何汇合 | **`torch.cat(..., dim=1)`** 通道维拼接 | **`x + y`** 逐元素相加 |
| 对 H、W 要求 | 各分支宽高必须相同 | 两路 C、H、W **全部相同** |
| 对 C 要求 | 通道可以不同（拼起来就行） | 通道必须相同才能相加 |

---

## 三、GoogLeNet 与 Inception Module

### 1. 要解决什么问题？

设计 CNN 时，**卷积核大小难选**（3×3？5×5？）。  
GoogLeNet 的思路：在一个块里**几种核都用上**，训练后有用的通路权重大、没用的自然变小——相当于让网络自己学「哪种配置更好」。

### 2. 四条分支（本课课件版本）

| 分支 | 结构 | 输出通道 | 要点 |
|------|------|----------|------|
| 1×1 | `Conv 1×1` | 16 | 最简单通路 |
| 5×5 | `1×1 → 5×5` | 24 | 大感受野；5×5 用 `padding=2` 保尺寸 |
| 3×3 | `1×1 → 3×3 → 3×3` | 24 | 两层 3×3 近似更大感受野；`padding=1` |
| Pool | `AvgPool → 1×1` | 24 | 池化用 `stride=1, padding=1` **保 H、W** |

输出总通道：

\[
16 + 24 + 24 + 24 = 88
\]

### 3. 拼接规则

```python
outputs = [branch1x1, branch5x5, branch3x3, branch_pool]
return torch.cat(outputs, dim=1)  # dim=1 → 沿 Channel 拼
```

- 输入形状：`(B, C, H, W)`
- 各分支输出：`(B, C_i, H, W)` —— **H、W 必须一致**
- 拼接后：`(B, ΣC_i, H, W)` = `(B, 88, H, W)`

---

## 四、1×1 卷积：本课关键积木

### 作用

| 作用 | 说明 |
|------|------|
| **改通道数** | `C×H×W` → `m×H×W`（用 m 个 1×1 核） |
| **降计算量** | 先 1×1 降维，再做 3×3/5×5，比直接大核省很多 |
| **跨通道信息融合** | 每个输出位置聚合了所有输入通道同位置信息 |

又称 **Network in Network**。  
Inception 里几乎每条分支前都先接 1×1，就是为了**控通道、控算力**。

### 直观对比

```
直接 5×5：  输入 C 很大 → 5×5 卷积参数爆炸
先 1×1 再 5×5：  C → 较小中间通道 → 再 5×5，参数可控
```

---

## 五、InceptionA 代码

```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class InceptionA(nn.Module):
    def __init__(self, in_channels):
        super(InceptionA, self).__init__()
        # 分支1：AvgPool → 1×1
        self.branch_pool = nn.Conv2d(in_channels, 24, kernel_size=1)
        # 分支2：1×1
        self.branch1x1 = nn.Conv2d(in_channels, 16, kernel_size=1)
        # 分支3：1×1 → 5×5（padding=2 保尺寸）
        self.branch5x5_1 = nn.Conv2d(in_channels, 16, kernel_size=1)
        self.branch5x5_2 = nn.Conv2d(16, 24, kernel_size=5, padding=2)
        # 分支4：1×1 → 3×3 → 3×3
        self.branch3x3_1 = nn.Conv2d(in_channels, 16, kernel_size=1)
        self.branch3x3_2 = nn.Conv2d(16, 24, kernel_size=3, padding=1)
        self.branch3x3_3 = nn.Conv2d(24, 24, kernel_size=3, padding=1)

    def forward(self, x):
        branch_pool = F.avg_pool2d(x, kernel_size=3, stride=1, padding=1)
        branch_pool = self.branch_pool(branch_pool)

        branch1x1 = self.branch1x1(x)

        branch5x5 = self.branch5x5_1(x)
        branch5x5 = self.branch5x5_2(branch5x5)

        branch3x3 = self.branch3x3_1(x)
        branch3x3 = self.branch3x3_2(branch3x3)
        branch3x3 = self.branch3x3_3(branch3x3)

        outputs = [branch1x1, branch5x5, branch3x3, branch_pool]
        return torch.cat(outputs, dim=1)  # → 通道数 88
```

### 嵌进完整网络（MNIST）

```python
class Net(nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.conv1 = nn.Conv2d(1, 10, kernel_size=5)
        self.conv2 = nn.Conv2d(88, 20, kernel_size=5)  # 88 来自 incep1 输出
        self.incep1 = InceptionA(in_channels=10)
        self.incep2 = InceptionA(in_channels=20)
        self.mp = nn.MaxPool2d(2)
        self.fc = nn.Linear(1408, 10)  # 88 × 4 × 4 = 1408

    def forward(self, x):
        in_size = x.size(0)
        x = F.relu(self.mp(self.conv1(x)))   # → (N,10,12,12)
        x = self.incep1(x)                   # → (N,88,12,12)
        x = F.relu(self.mp(self.conv2(x)))   # → (N,20,4,4)
        x = self.incep2(x)                   # → (N,88,4,4)
        x = x.view(in_size, -1)              # → (N,1408)
        return self.fc(x)
```

### 通道 / 尺寸推演

```
输入 (1,28,28)
  → conv1+pool   (10,12,12)
  → incep1       (88,12,12)     # 16+24+24+24
  → conv2+pool   (20,4,4)
  → incep2       (88,4,4)
  → view         88×4×4 = 1408
  → Linear(1408, 10)
```

> **不必死算 1408**：写网络时先注释掉 `fc`，用假数据 `print(x.shape)` 看展平前维度再填。

---

## 六、ResNet：为什么层数多了反而更差？

### 现象

Plain 网络（普通堆叠卷积）：**20 层往往比 56 层更好**。  
更深不一定更好——至少对普通串型网络如此。

### 原因：梯度消失

反向传播用链式法则，梯度连乘：

\[
\frac{\partial L}{\partial w_{\text{浅层}}} = \underbrace{\frac{\partial L}{\partial y} \cdot \frac{\partial y}{\partial \cdots} \cdot \cdots}_{\text{很多小于 1 的数相乘}} \to 0
\]

浅层参数几乎不更新 → 深层白搭、浅层学不好。

### ResNet 的解法：残差 + 跳跃连接

不直接学 \(H(x)\)，而学残差 \(F(x)\)：

\[
H(x) = F(x) + x
\]

- 前向：主路卷积 + **旁路原样抄过来的 x**，再相加、激活
- 反向：梯度有一条**直通路径**到浅层，不易乘没

关键约束：**相加的两路 C、H、W 必须完全相同**。

---

## 七、ResidualBlock 代码

```python
class ResidualBlock(nn.Module):
    def __init__(self, channels):
        super(ResidualBlock, self).__init__()
        # padding=1 + kernel=3 → 输出与输入同尺寸
        self.conv1 = nn.Conv2d(channels, channels, kernel_size=3, padding=1)
        self.conv2 = nn.Conv2d(channels, channels, kernel_size=3, padding=1)

    def forward(self, x):
        y = F.relu(self.conv1(x))
        y = self.conv2(y)
        return F.relu(x + y)  # 先相加，再激活
```

要点：

1. 输入输出通道相同（本课简化版）
2. `padding=1` 保证 H、W 不变，才能和 x 相加
3. **先 `x + y`，再 `ReLU`**（不是先激活再加）

### 嵌进完整网络（MNIST）

```python
class Net(nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.conv1 = nn.Conv2d(1, 16, kernel_size=5)
        self.conv2 = nn.Conv2d(16, 32, kernel_size=5)
        self.mp = nn.MaxPool2d(2)
        self.rblock1 = ResidualBlock(16)
        self.rblock2 = ResidualBlock(32)
        self.fc = nn.Linear(512, 10)  # 32 × 4 × 4 = 512

    def forward(self, x):
        in_size = x.size(0)
        x = self.mp(F.relu(self.conv1(x)))  # → (N,16,12,12)
        x = self.rblock1(x)                 # 尺寸不变
        x = self.mp(F.relu(self.conv2(x)))  # → (N,32,4,4)
        x = self.rblock2(x)
        x = x.view(in_size, -1)             # → (N,512)
        return self.fc(x)
```

### 尺寸推演

```
输入 (1,28,28)
  → conv1+pool   (16,12,12)
  → rblock1      (16,12,12)   # 残差，尺寸不变
  → conv2+pool   (32,4,4)
  → rblock2      (32,4,4)
  → view         32×4×4 = 512
  → Linear(512, 10)
```

---

## 八、Inception vs ResNet 对照

| 维度 | Inception | ResNet |
|------|-----------|--------|
| 动机 | 核大小难选 → 多种并行 | 网络太深 → 梯度消失 |
| 汇合方式 | `torch.cat`（通道拼接） | `x + y`（逐元素加） |
| 尺寸约束 | 各分支 **H、W** 相同 | 两路 **C、H、W** 全同 |
| 1×1 卷积 | 大量使用（降维/改通道） | 完整 ResNet 里也有（本课简化版未用） |
| 本课输出通道 | Inception 后固定 **88** | 残差块通道 = 输入通道 |
| FC 输入维 | **1408** | **512** |

---

## 九、训练流水线：和第 10 集完全一样

```python
device = torch.device("cuda:0" if torch.cuda.is_available() else "cpu")
model.to(device)

criterion = nn.CrossEntropyLoss()
optimizer = optim.SGD(model.parameters(), lr=0.01, momentum=0.5)

def train(epoch):
    model.train()
    for batch_idx, (inputs, target) in enumerate(train_loader):
        inputs, target = inputs.to(device), target.to(device)
        optimizer.zero_grad()
        loss = criterion(model(inputs), target)
        loss.backward()
        optimizer.step()

def test():
    model.eval()
    correct = total = 0
    with torch.no_grad():
        for inputs, target in test_loader:
            inputs, target = inputs.to(device), target.to(device)
            _, pred = torch.max(model(inputs), dim=1)
            total += target.size(0)
            correct += (pred == target).sum().item()
    print(f'Accuracy: {100 * correct / total:.2f}%')
```

| 保持不变 | 本课新写 |
|----------|----------|
| MNIST + Normalize | `InceptionA` / `ResidualBlock` |
| CrossEntropyLoss | `torch.cat` / `x + y` |
| epoch × batch 循环 | 分支结构的尺寸对齐 |
| GPU `.to(device)` | 模块化封装可复用块 |

---

## 十、工程实践技巧（课件强调）

### 1. 增量式搭网络

不要一次写完巨型网络。每加一个模块就：

```python
x = some_block(x)
print(x.shape)  # 核对是否符合预期
```

### 2. Linear 输入维怎么确定？

先注释掉 `self.fc` 和最后的 `view`/`fc`，用假输入跑一遍：

```python
x = torch.randn(1, 1, 28, 28)
print(model_without_fc(x).shape)  # 例如 torch.Size([1, 88, 4, 4])
# → Linear(88*4*4, 10)
```

### 3. 测试集准确率峰值 ≠ 最后一轮

- 准确率最高的那一轮，**保存模型参数**（泛化最好）
- 轮数越多不一定越好 → 可能过拟合

### 4. 复杂结构 → 封装成类

Inception、Residual 都先写成独立 `nn.Module`，主网里像搭积木一样调用——和面向对象「减少重复」同一思路。

---

## 十一、常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| `torch.cat` 报错 | 某分支 H/W 不一致 | 检查 padding / AvgPool 的 stride、padding |
| `x + y` 报错 | 通道或尺寸不同 | 残差块用 `padding=1`；通道保持一致 |
| `conv2` 写成 `in=20` | 忘了 Inception 输出 88 | `Conv2d(88, 20, ...)` |
| `Linear` 维度错 | 1408 / 512 算错 | `print` 展平前 shape |
| AvgPool 后尺寸变了 | 用了默认 stride=kernel | 指定 `stride=1, padding=1` |
| 残差先激活再相加 | 写反了顺序 | `F.relu(x + y)` |
| 分支里 MaxPool(2) | 宽高减半，拼不上 | Inception 池化分支用 stride=1 保尺寸 |

---

## 十二、本课你需要记住的

1. **高级 CNN** = 分支结构：并行（Inception）或跳跃（ResNet）
2. **Inception**：多种核并行 → `torch.cat` 沿通道拼；输出通道常为 **88**
3. **1×1 卷积**：改通道、降计算量、跨通道融合
4. **ResNet**：`H(x)=F(x)+x`，缓解梯度消失；**先加再激活**
5. **拼接看 H/W，相加看 C/H/W**
6. **训练流水线不变**：只换模型块
7. **模块化**：复杂结构先封装成类，再拼主网
8. **增量验证 shape**：比一次性手算更靠谱

---

## 十三、与第 10 集 / 后续的关系

| 第 10 集 | 第 11 集 |
|----------|----------|
| 串型 Conv + Pool | Inception 并行分支 |
| 自己选核大小 | 多种核一起训 |
| 层数有限 | 残差让网络可以更深 |
| `Linear(320,10)` | `Linear(1408,10)` 或 `Linear(512,10)` |

| 本课概念 | 后续用途 |
|----------|----------|
| Inception / 多尺度特征 | 目标检测、语义分割等 |
| Residual / shortcut | ResNet、Transformer 残差等现代标配 |
| 1×1 卷积 | 几乎所有高效 CNN |
| 模块化 `nn.Module` | 读论文实现、改现成模型 |

> 本课是课程 CNN 部分收尾；之后多转向 RNN / 自然语言等，或自行扩展读 ResNet 论文、用 `torchvision.models.resnet18` 等预训练模型。

---

## 十四、自测清单

- [ ] 能说出串型 CNN 和分支结构的区别
- [ ] 能解释 Inception「多种核并行」在解决什么问题
- [ ] 知道 1×1 卷积的三个作用
- [ ] 能写出 `InceptionA` 的四条分支并用 `torch.cat` 拼接
- [ ] 能推演：Inception 输出通道 = 88，FC 输入 = 1408
- [ ] 能解释为何深层 Plain 网络可能更差（梯度消失）
- [ ] 能写出 `ResidualBlock`：`F.relu(x + y)`
- [ ] 知道 `cat` 与 `+` 对尺寸的不同要求
- [ ] 会用「假输入 + print shape」确定 Linear 输入维
- [ ] 能在第 10 集 MNIST 代码上替换为 Inception 或 ResNet 并跑通

---

## 十五、一句话总结

> 第 11 集 = 第 10 集训练流水线不变 + **Inception（多核并行 + cat）** + **ResNet（残差相加，对抗梯度消失）**；从「一层层串起来」升级为「可复用的模块化高级块」。

---

*上一课：第十课 · CNN 基础｜本课为课程 CNN 专题收官*
