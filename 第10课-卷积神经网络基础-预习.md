# PyTorch 深度学习实践 · 第十课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=10)  
> **集数**：第 10 集 · 卷积神经网络（基础篇）  
> **时长**：约 50～60 分钟  
> **整理日期**：2026-07-14  
> **重要程度**：⭐⭐⭐ **图像任务的核心结构：用 Conv 替代「一上来就展平」**

---

## 一、第十课在学什么

第 9 集用**全连接网络**做 MNIST：先把 28×28 展平成 784，再堆 `Linear`。  
第 10 集引入 **CNN（Convolutional Neural Network）**：先用卷积/池化提取空间特征，再接到分类头。

```
第 9 集（全连接）            第 10 集（CNN）
─────────────────           ─────────────────
view → 784 维向量            保留 (C,H,W) 空间结构
多层 Linear + ReLU           Conv2d + ReLU + MaxPool2d
参数多、忽略邻域信息         参数共享、捕捉局部图案
准确率 ~97%                  通常更高、更合图像先验
数据 / 损失 / 循环           **完全复用第 9 集**
```

**本课核心**：理解卷积与池化在干什么；能写出 MNIST 的小型 CNN；**训练框架不变，只换模型结构**。

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 9 集 | MNIST、`CrossEntropyLoss`、准确率、`torch.max` |
| 第 8 集 | `DataLoader`、双层训练循环 |
| 第 7 集 | 多层 `nn.Module`、`forward` |
| 直觉 | 图像相邻像素有相关性（笔画是局部的） |

如果第 9 集全连接 MNIST 能训到 90%+，可以直接进第 10 集。

---

## 三、为什么图像不要一上来就展平？

| 全连接（第 9 集） | 卷积（第 10 集） |
|------------------|------------------|
| 把像素当成独立特征 | 利用**局部邻域**（笔画、边缘） |
| 像素位置一打乱，信息乱 | **平移附近仍可能认出同样图案** |
| `Linear(784,512)` 参数极多 | 卷积核小、**权值共享**，参数少 |
| 难学空间结构 | 层层得到「特征图」 |

直观：数字「7」左上角和右下角的笔画模式，用同一个小滤波器去扫整张图即可识别。

---

## 四、卷积层 `nn.Conv2d`（提前熟悉）

二维卷积：一个小的 **kernel（卷积核）** 在高、宽上滑动，做加权求和，得到特征图。

### 构造参数

```python
torch.nn.Conv2d(
    in_channels,     # 输入通道数（灰度图=1，RGB=3）
    out_channels,    # 输出通道数 = 卷积核个数 = 特征图张数
    kernel_size,     # 核大小，如 5 或 (5,5)
    stride=1,        # 滑动步长
    padding=0,       # 边缘补零
)
```

| 参数 | 直觉 |
|------|------|
| `in_channels` | 「这一层吃几张图」 |
| `out_channels` | 「这一层产出几张特征图」 |
| `kernel_size` | 每个局部感受野多大 |
| `stride` | 每次跳几格（越大，输出越小） |
| `padding` | 四周补几圈 0（常用来保尺寸） |

### 输入 / 输出形状（NCHW）

```
输入: (N, C_in,  H_in,  W_in)
输出: (N, C_out, H_out, W_out)
```

### 输出尺寸公式（单边）

\[
H_{out} = \left\lfloor \frac{H_{in} + 2P - K}{S} \right\rfloor + 1
\]

其中 \(K\)=kernel_size，\(P\)=padding，\(S\)=stride。宽 \(W\) 同理。

**例子（默认 stride=1, padding=0，核 5×5）：**

| 输入 H | → 卷积 | 输出 H |
|--------|--------|--------|
| 28 | \((28-5)/1+1\) | **24** |
| 12 | \((12-5)/1+1\) | **8** |

听课时重点看：**通道变了多少、高宽缩了多少**。

---

## 五、池化层 `nn.MaxPool2d`（提前熟悉）

池化做**下采样**：缩小高宽，保留主要响应，并增大感受野。

```python
torch.nn.MaxPool2d(kernel_size=2)   # 常简写成 MaxPool2d(2)
# 2×2 窗口内取最大值；默认 stride = kernel_size → 边长约减半
```

| 操作 | 作用 |
|------|------|
| `MaxPool` | 取局部最大（突出强响应） |
| `AvgPool` | 取局部平均（本课少用） |

**例子：** `(N, 10, 24, 24)` → `MaxPool2d(2)` → `(N, 10, 12, 12)`  
通道数不变，高宽各约 /2。

---

## 六、MNIST 经典 CNN 尺寸推演（必会）

课件典型结构（核 5、池化 2、无 padding）：

```
输入          (N,  1, 28, 28)
Conv2d(1→10, k=5)   → (N, 10, 24, 24)
MaxPool2d(2)        → (N, 10, 12, 12)
Conv2d(10→20, k=5)  → (N, 20,  8,  8)
MaxPool2d(2)        → (N, 20,  4,  4)
view / flatten      → (N, 320)     # 20×4×4=320
Linear(320→10)      → (N, 10) logits
```

> 为什么是 320？**听课前先手算一遍**，写 `Linear` 时就不会蒙。

对应 `forward` 流水线：

```
x → Conv → MaxPool → ReLU → Conv → MaxPool → ReLU → view → Linear → logits
```

（课件有时写成 `ReLU(pooling(conv(x)))`，顺序略有写法差异，核心一样。）

---

## 七、模型代码预览

```python
import torch
import torch.nn.functional as F

class Net(torch.nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.conv1 = torch.nn.Conv2d(1, 10, kernel_size=5)
        self.conv2 = torch.nn.Conv2d(10, 20, kernel_size=5)
        self.pooling = torch.nn.MaxPool2d(2)
        self.fc = torch.nn.Linear(320, 10)

    def forward(self, x):
        batch_size = x.size(0)
        x = F.relu(self.pooling(self.conv1(x)))
        x = F.relu(self.pooling(self.conv2(x)))
        x = x.view(batch_size, -1)   # (N, 320)
        x = self.fc(x)               # 最后一层不过 Softmax
        return x
```

| 点 | 说明 |
|----|------|
| 输入**不要**先 `view(-1,784)` | 卷积要保留 `(C,H,W)` |
| 展平放在**卷积之后** | 接全连接分类头 |
| `Linear(320,10)` 的 320 | 必须与前面特征图尺寸一致 |
| 损失仍是 `CrossEntropyLoss` | 与第 9 集相同 |

---

## 八、数据与训练循环（几乎原封不动）

数据加载、损失、测试准确率 **直接抄第 9 集**：

```python
# 数据：与第 9 集相同
transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.1307,), (0.3081,))
])
train_loader = DataLoader(..., batch_size=64, shuffle=True)
test_loader  = DataLoader(..., batch_size=64, shuffle=False)

criterion = torch.nn.CrossEntropyLoss()
optimizer = torch.optim.SGD(model.parameters(), lr=0.01, momentum=0.5)

# train() / test()：与第 9 集相同
# 区别只在：model = Net() 换成 CNN
```

| 保持不变 | 本课新写 |
|----------|----------|
| MNIST + transform | `Conv2d` / `MaxPool2d` |
| `CrossEntropyLoss` | 尺寸推演与 `Linear(320,10)` |
| epoch × batch 双层循环 | CNN 的 `forward` |
| `torch.max` 算准确率 | （可选）GPU `.to(device)` |

---

## 九、完整代码骨架（听课前扫一眼）

```python
import torch
import torch.nn.functional as F
from torch import optim
from torch.utils.data import DataLoader
from torchvision import datasets, transforms

batch_size = 64
transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.1307,), (0.3081,))
])

train_loader = DataLoader(
    datasets.MNIST('./dataset/mnist', train=True, download=True, transform=transform),
    batch_size=batch_size, shuffle=True
)
test_loader = DataLoader(
    datasets.MNIST('./dataset/mnist', train=False, download=True, transform=transform),
    batch_size=batch_size, shuffle=False
)

class Net(torch.nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.conv1 = torch.nn.Conv2d(1, 10, kernel_size=5)
        self.conv2 = torch.nn.Conv2d(10, 20, kernel_size=5)
        self.pooling = torch.nn.MaxPool2d(2)
        self.fc = torch.nn.Linear(320, 10)

    def forward(self, x):
        batch_size = x.size(0)
        x = F.relu(self.pooling(self.conv1(x)))
        x = F.relu(self.pooling(self.conv2(x)))
        x = x.view(batch_size, -1)
        return self.fc(x)

model = Net()
criterion = torch.nn.CrossEntropyLoss()
optimizer = optim.SGD(model.parameters(), lr=0.01, momentum=0.5)

for epoch in range(10):
    # train(epoch)  同第 9 集
    # test()        同第 9 集
    pass
```

---

## 十、与第 9 集的对照

| 项目 | 第 9 集 | 第 10 集 |
|------|--------|--------|
| 任务 / 数据 | MNIST 10 类 | **相同** |
| 损失 / 标签 | CE + long | **相同** |
| 训练循环 | epoch × batch | **相同** |
| 输入处理 | 立刻 `view(-1,784)` | **先 Conv/Pool，最后再展平** |
| 主干 | 多层 `Linear` | **`Conv2d` + `MaxPool2d`** |
| 分类头 | 多层 FC | 通常 **一层 `Linear(320,10)`** |
| 关键 shape | `(N,784)` | `(N,C,H,W)` → `(N,320)` |

---

## 十一、听课时要重点关注的

| 关注点 | 为什么重要 |
|--------|------------|
| 通道 / 高 / 宽怎么变 | 写 `Linear` 输入维的依据 |
| `in_channels` / `out_channels` | 层与层之间必须对上 |
| 为什么卷积比全连接参数少 | 权值共享、局部连接 |
| 池化为什么常用 2×2 | 减半尺寸、降计算量 |
| 何时才 `view` | 卷积之后、全连接之前 |
| 最后一层仍不过 Softmax | 继续用 CE，别和第 6 集搞混 |
| （若讲）GPU | `model.to(device)`，数据也要 `.to(device)` |

---

## 十二、预习动手任务（20～30 分钟）

- [ ] 复习第 9 集：能独立写 MNIST DataLoader + `train`/`test`
- [ ] 手算：28→Conv5→24→Pool2→12→Conv5→8→Pool2→4，再算 `20×4×4=320`
- [ ] 在笔记本里 `print` 一层 `Conv2d(1,10,5)` 对随机 `(2,1,28,28)` 的输出 shape
- [ ] 再 `print` 接 `MaxPool2d(2)` 后的 shape
- [ ] 对比：第 9 集在 `forward` 开头 `view`；第 10 集应在**卷积结束**后 `view`
- [ ] 可选：把第 9 集的 `Net` 换成上面的 CNN，跑 1～2 个 epoch 看准确率

---

## 十三、常见坑（提前避雷）

| 问题 | 原因 | 解决 |
|------|------|------|
| `Linear` 维度不匹配 | 320 算错 / 改过核大小 | 在 `view` 前 `print(x.shape)` |
| 一上来就 `view(-1,784)` | 套了第 9 集习惯 | 卷积前保持 4D |
| `Conv2d` 报通道错 | `in_channels` 对不上 | 第一层 1，第二层=上一层 out |
| 用了 3 通道写法 | MNIST 是灰度 | 第一层 `in_channels=1` |
| 最后又加 Softmax | 与 CE 重复 | 输出 logits 即可 |
| `view(-1,320)` 写死 batch | 最后一 batch 可能不满 | 用 `x.size(0)` 或 `view(-1, 320)` |
| 准确率不如预期 | lr / epoch / 结构 | 先确认 shape 全对再调参 |

---

## 十四、小实验：只看 shape（可不训练）

```python
import torch
import torch.nn as nn

x = torch.randn(64, 1, 28, 28)
conv1 = nn.Conv2d(1, 10, 5)
pool = nn.MaxPool2d(2)
conv2 = nn.Conv2d(10, 20, 5)

x = pool(conv1(x)); print(x.shape)   # 期望: [64, 10, 12, 12]
x = pool(conv2(x)); print(x.shape)   # 期望: [64, 20, 4, 4]
print(x.view(64, -1).shape)          # 期望: [64, 320]
```

把这条流水线跑通，听课会轻松很多。

---

## 十五、与后续课程的关系

| 第 10 集学会 | 在哪用到 |
|------------|----------|
| `Conv2d` / `MaxPool2d` | 几乎所有视觉模型 |
| 特征图尺寸推演 | 设计网络、改结构 |
| CNN + CE + 准确率 | 图像分类标准流水线 |
| 基础 CNN | **第 11 集**：更深网络、Inception / ResNet 思路 |

---

## 十六、第 11 集预告（了解即可）

第 10 集是**基础 CNN**：两层卷积 + 池化 + 一层全连接。  
第 11 集进入**高级 CNN**：

- 更深、更复杂的结构思想
- 如 GoogLeNet（Inception）、ResNet（残差连接）等（课件侧重思想 + 结构）
- 同样解决「怎么更好地抽特征」

第 10 集先把「卷积 + 池化 + 分类」闭环跑通。

---

## 十七、一句话预习总结

> 第 10 集 = 第 9 集 MNIST 训练流水线不变 + **用 Conv2d / MaxPool2d 提取空间特征** + **卷积后再展平接 Linear**；从「把图像当长向量」升级为「真正按图像结构建模」。

---

*听课建议：先手算 28→24→12→8→4→320；再用 4 行代码 print 每层 shape；最后把第 9 集的 `Net` 换成 CNN，对比准确率与收敛速度。*
