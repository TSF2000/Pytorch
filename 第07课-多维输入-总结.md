# PyTorch 深度学习实践 · 第七课 多维输入 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=7)  
> **集数**：第 7 集 · 处理多维特征的输入（Multiple Dimension Input）  
> **时长**：约 43 分钟  
> **整理日期**：2026-07-12  
> **前置**：第 6 集逻辑回归（Sigmoid、BCELoss、二分类五步循环）

---

## 一、本课定位

第 5～6 集输入只有 **1 个特征**（一个 x），是玩具数据。  
第 7 集进入**真实场景**：每个样本有 **8 个特征**，从 CSV 文件加载，用**多层神经网络**做二分类。

```
第 6 集（1 维）              第 7 集（8 维）
─────────────────           ─────────────────
x: 单个数值                  x: 8 个特征（X1~X8）
Linear(1, 1)                 Linear(8,6) → Linear(6,4) → Linear(4,1)
手写 Tensor                  diabetes.csv 真实数据集
3 个样本                     数百～数千个样本
```

**本课核心**：多维输入怎么表示、怎么从文件读数据、怎么堆叠多层 `Linear`；**训练五步循环不变**。

---

## 二、任务：糖尿病二分类

使用 **Diabetes 数据集**（`diabetes.csv` / `diabetes.csv.gz`）：

| 列 | 含义 |
|----|------|
| X1 ~ X8 | 8 项体检指标（特征） |
| Y | 标签：1 = 一年后病情会加重，0 = 不会 |

**任务**：根据 8 维特征，预测 Y（与第 6 集相同的二分类，只是特征变多了）。

### 数据表的两种视角

| 视角 | 行 | 列 |
|------|----|----|
| 数据库表 | 一条记录 | 一个字段 |
| 深度学习数据集 | **一个样本** | **一个特征** |

---

## 三、从 CSV 加载数据

```python
import numpy as np
import torch

xy = np.loadtxt('diabetes.csv.gz', delimiter=',', dtype=np.float32)

x_data = torch.from_numpy(xy[:, :-1])    # 前 8 列特征，shape: (N, 8)
y_data = torch.from_numpy(xy[:, [-1]])   # 最后 1 列标签，shape: (N, 1)
```

### 关键细节

| 写法 | shape | 结果 |
|------|-------|------|
| `xy[:, -1]` | `(N,)` | 一维向量 ❌ |
| `xy[:, [-1]]` | `(N, 1)` | 二维矩阵 ✅ |

> 与第 5 集「双层括号」同一道理：PyTorch 期望 batch 标签是 `(N, 1)` 矩阵，不是 `(N,)` 向量。

### `torch.from_numpy` 注意

- NumPy 数组和 Tensor **共享内存**
- 改 Tensor 可能影响原 NumPy 数组

---

## 四、从 1 维到 8 维：模型怎么变

### 方案 A：单层逻辑回归（8 维 → 1 维）

与第 6 集结构相同，只改 `Linear` 输入维度：

\[
z = w_1 x_1 + w_2 x_2 + \cdots + w_8 x_8 + b
\]
\[
\hat{y} = \sigma(z)
\]

```python
class Model(torch.nn.Module):
    def __init__(self):
        super(Model, self).__init__()
        self.linear = torch.nn.Linear(8, 1)
        self.sigmoid = torch.nn.Sigmoid()

    def forward(self, x):
        return self.sigmoid(self.linear(x))
```

### 方案 B：多层神经网络（课件重点）

堆叠多个 `Linear + Sigmoid`，增强非线性表达能力：

```python
class Model(torch.nn.Module):
    def __init__(self):
        super(Model, self).__init__()
        self.linear1 = torch.nn.Linear(8, 6)
        self.linear2 = torch.nn.Linear(6, 4)
        self.linear3 = torch.nn.Linear(4, 1)
        self.sigmoid = torch.nn.Sigmoid()

    def forward(self, x):
        x = self.sigmoid(self.linear1(x))
        x = self.sigmoid(self.linear2(x))
        x = self.sigmoid(self.linear3(x))
        return x
```

计算图：

```
x(N×8) → Linear(8,6) → Sigmoid → Linear(6,4) → Sigmoid → Linear(4,1) → Sigmoid → y(N×1)
```

---

## 五、Linear 层 = 矩阵空间变换

`nn.Linear(in, out)` 本质是：**把 in 维空间的向量，线性映射到 out 维空间**。

| 层 | 输入 shape | 权重 shape | 输出 shape |
|----|-----------|-----------|-----------|
| `Linear(8, 6)` | `(N, 8)` | `(6, 8)` | `(N, 6)` |
| `Linear(6, 4)` | `(N, 6)` | `(4, 6)` | `(N, 4)` |
| `Linear(4, 1)` | `(N, 4)` | `(1, 4)` | `(N, 1)` |

> **N** = 样本数。本课先用**全量数据**一次训练（Mini-Batch 概念在本集引入，第 8 集才用 DataLoader 实现）。

### 为什么需要多层 + 激活函数

| 概念 | 含义 |
|------|------|
| 单层 Linear | 只能做线性变换 |
| 多层 Linear 组合 | 通过多个线性层 + 激活函数，**模拟非线性变换** |
| 激活函数（Sigmoid） | 给线性变换增加非线性因子，才能拟合复杂规律 |
| 隐层数量 / 每层宽度 | **超参数**，需要实验搜索；层数越多表达能力越强，但也更容易过拟合 |

---

## 六、向量化运算（Mini-Batch 思想）

PyTorch 的 `nn.Linear`、`Sigmoid` 等都是**向量化函数**：

- 一次传入 **N 个样本** `(N, 8)`，并行计算 N 个输出
- 不需要 Python `for` 循环逐个样本算
- Sigmoid 按元素作用到每个输出上

本课虽然叫「全量训练」，本质上是 **batch size = N** 的一次性矩阵运算——为第 8 集 mini-batch 打直觉。

---

## 七、两种 Sigmoid 写法

| 写法 | 类型 | 用法 |
|------|------|------|
| `torch.sigmoid(x)` | 函数 | 第 6 集用法，`forward` 里直接调用 |
| `torch.nn.Sigmoid()` | 类（Module） | 第 7 集用法，在 `__init__` 定义，`forward` 里 `self.sigmoid(x)` |

两者效果相同。多层网络中常用 `nn.Sigmoid()` 实例，方便统一替换为 ReLU 等。

---

## 八、完整代码

```python
import numpy as np
import torch

# Step 1: 加载数据
xy = np.loadtxt('diabetes.csv.gz', delimiter=',', dtype=np.float32)
x_data = torch.from_numpy(xy[:, :-1])
y_data = torch.from_numpy(xy[:, [-1]])

# Step 2: 多层模型
class Model(torch.nn.Module):
    def __init__(self):
        super(Model, self).__init__()
        self.linear1 = torch.nn.Linear(8, 6)
        self.linear2 = torch.nn.Linear(6, 4)
        self.linear3 = torch.nn.Linear(4, 1)
        self.sigmoid = torch.nn.Sigmoid()

    def forward(self, x):
        x = self.sigmoid(self.linear1(x))
        x = self.sigmoid(self.linear2(x))
        x = self.sigmoid(self.linear3(x))
        return x

model = Model()

# Step 3: 损失 + 优化器
criterion = torch.nn.BCELoss(reduction='mean')
optimizer = torch.optim.SGD(model.parameters(), lr=0.1)

# Step 4: 训练（五步循环与第 5、6 集完全相同）
for epoch in range(100000):
    y_pred = model(x_data)
    loss = criterion(y_pred, y_data)

    optimizer.zero_grad()
    loss.backward()
    optimizer.step()

    if epoch % 10000 == 0:
        print(epoch, loss.item())
```

**与第 6 集的变化**：数据来源、模型层数、`Linear` 输入维度、可能调整 `lr` 和 `epoch` 数。**训练循环不变。**

---

## 九、与第 6 集的对照

| 项目 | 第 6 集 | 第 7 集 |
|------|--------|--------|
| **输入维度** | 1 | **8** |
| **数据来源** | 手写 Tensor | **CSV 文件** |
| **模型** | 单层 Linear + Sigmoid | **多层 Linear + Sigmoid** |
| **样本数** | 3 | **数百～数千** |
| **损失** | BCELoss | BCELoss（常用 `reduction='mean'`） |
| **训练循环** | 五步 | **完全相同** |
| **Sigmoid 写法** | `torch.sigmoid()` | `nn.Sigmoid()` 实例 |

---

## 十、BCELoss 的 reduction 参数

| 参数 | 含义 |
|------|------|
| `reduction='sum'` | 对所有样本 loss 求和（第 6 集课件用法） |
| `reduction='mean'` | 对所有样本 loss 求平均（第 7 集常用） |

> 旧版 `size_average=False/True` 已弃用，改用 `reduction`。

---

## 十一、常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| 维度不匹配 | `Linear(1,1)` 用了 8 维输入 | 改成 `Linear(8, 1)` 或多层结构 |
| y shape 不对 | 用了 `[:, -1]` | 改成 `[:, [-1]]` |
| 找不到 CSV | 路径错误 | 检查相对路径或放 `data/` 目录 |
| BCELoss 报错 | 输入不在 (0,1) | 最后一层加 Sigmoid |
| loss 下降极慢 | lr 太小或 epoch 不够 | 课件可能用 `lr=0.1`，epoch 更大 |
| 隐层过多 | 过拟合训练集噪声 | 控制层数和宽度，第 8 集后可用验证集评估 |

---

## 十二、本课你需要记住的

1. **多维输入**：每个样本是一个特征向量，shape 为 `(N, feature_dim)`
2. **CSV 加载**：`np.loadtxt` + `torch.from_numpy`，标签用 `[:, [-1]]` 保持二维
3. **`nn.Linear(in, out)`**：in 必须和特征数一致，out 是输出维度
4. **多层网络**：多个 Linear + 激活函数，模拟非线性变换
5. **向量化**：一次喂入 N 个样本，PyTorch 并行计算，不用手写 for
6. **训练五步循环不变**——换数据、换模型结构，流程照旧
7. 第 8 集会把全量数据改成 **Dataset + DataLoader** 的 mini-batch 训练

---

## 十三、与后续课程的关系

| 第 7 集学会 | 在哪用到 |
|------------|----------|
| 多维 `nn.Linear` | 所有深度学习模型 |
| 多层网络堆叠 | CNN、RNN、Transformer |
| CSV → Tensor | 第 8 集封装成 `Dataset` |
| 全量 → mini-batch | **第 8 集 DataLoader** |
| BCELoss 二分类 | 第 9 集扩展到多分类（Softmax） |

---

## 十四、自测清单

- [ ] 能解释 `xy[:, :-1]` 和 `xy[:, [-1]]` 分别取什么
- [ ] 能说出 `Linear(8, 6)` 的输入/输出 shape
- [ ] 能独立写出三层 Linear 的 `forward`
- [ ] 知道为什么多层网络需要激活函数
- [ ] 能说出第 7 集相对第 6 集改了哪几处（数据、模型、超参）
- [ ] 知道 `torch.sigmoid()` 和 `nn.Sigmoid()` 的区别
- [ ] 已成功加载 diabetes 数据并跑通训练

---

## 十五、一句话总结

> 第 7 集 = 第 6 集逻辑回归 + **8 维真实数据** + **多层 Linear**；从 CSV 读数据，`Linear(8→6→4→1)` 堆叠 + Sigmoid，训练循环不变；这是从玩具例子迈向真实数据集的第一步，下一课学 **Dataset / DataLoader** 做 mini-batch。

---

*下一课：第八课 · 加载数据集（Dataset、DataLoader、mini-batch、shuffle）*
