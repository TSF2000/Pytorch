# PyTorch 深度学习实践 · 第四课 反向传播 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys)  
> **集数**：第 4 集 · Back Propagation（反向传播）  
> **时长**：约 47 分钟  
> **整理日期**：2026-07-11  
> **前置**：第 3 集梯度下降（手动写 gradient 函数）

---

## 一、本课定位

第 3 集你要**手写梯度公式**。神经网络参数成千上万，不可能每个都手推。

第 4 集解决：**PyTorch 如何自动求梯度？**

**答案：计算图 + 反向传播（backward）**

本课是第 5 集（正式 PyTorch 训练）的**直接铺垫**——第 5 集只是把本课写法封装成 `nn.Module` + `optimizer`。

---

## 二、反向传播是什么

| 步骤 | 做什么 |
|------|--------|
| **前向传播（Forward）** | 数据从输入流向输出，算出预测值和 Loss |
| **反向传播（Backward）** | 从 Loss 往回，用**链式法则**算每个参数的梯度 |
| **更新权重** | 用梯度下降更新参数 |

> 线性模型 \(y = wx\) 可以看作**最简单的神经网络**，用它理解计算图最合适。

---

## 三、计算图（Computation Graph）

### 1. 什么是计算图

把一次计算拆成**原子运算节点**（加法、乘法等），连成有向图：

```
x ──→ × ──→ y_pred ──→ - ──→ ()² ──→ Loss
      ↑
      w
```

- **Forward**：沿箭头方向算，得到 Loss  
- **Backward**：从 Loss 往回，用链式法则传播梯度

### 2. 链式法则（直觉）

若 \(Loss = f(g(w))\)，则：

\[
\frac{\partial Loss}{\partial w} = \frac{\partial Loss}{\partial g} \cdot \frac{\partial g}{\partial w}
\]

复杂网络 = 很多层链式法则串联，**PyTorch 全自动完成**。

### 3. 为什么多层需要激活函数

- 若每层都是线性变换 \(wx+b\)，多层叠起来**等价于一层**
- 加 **Sigmoid、ReLU** 等非线性激活，模型才有表达能力
- 本课点到为止，第 6 集会讲 Sigmoid

---

## 四、Tensor 与张量

### 1. Tensor 是什么

PyTorch 的基本数据单元，可表示标量、向量、矩阵、高维数组。

### 2. Tensor 的两个关键属性

| 属性 | 含义 |
|------|------|
| **data** | 张量本身的数值 |
| **grad** | 该张量对 Loss 的梯度（反向传播后填入） |

### 3. 关键 API

| 写法 | 作用 |
|------|------|
| `w.requires_grad = True` | 告诉 PyTorch：训练时要对 w 求梯度 |
| `l.backward()` | 从 Loss 反向传播，梯度写入 `w.grad` |
| `w.grad.data` | 取梯度数值（更新权重时用） |
| `w.grad.item()` | 把单元素张量变成 Python 标量（打印用） |
| `w.grad.data.zero_()` | 梯度清零（下一轮训练前必须做） |
| `l.item()` | 取 Loss 标量值，**不参与计算图** |

### 4. 重要细节

- 每次调用 `loss()` 都在**动态构建计算图**
- `backward()` 后计算图**被释放**
- 累加 Loss 时用 `l.item()`，不要用张量直接加，否则会构建无用计算图、占内存

---

## 五、用 PyTorch 实现线性模型（本课核心代码）

这是第 5 集之前的「原始写法」，建议理解每一行：

```python
import torch

x_data = [1.0, 2.0, 3.0]
y_data = [2.0, 4.0, 6.0]

w = torch.Tensor([1.0])
w.requires_grad = True   # 需要计算梯度

def forward(x):
    return x * w           # x 会自动转成 tensor

def loss(x, y):
    y_pred = forward(x)
    return (y_pred - y) ** 2

print('predict before training', 4, forward(4).item())

# 训练（随机梯度下降）
for epoch in range(100):
    for x, y in zip(x_data, y_data):
        l = loss(x, y)           # 1. 前向：算 Loss
        l.backward()             # 2. 反向：自动求梯度
        print('\tgrad:', x, y, w.grad.item())
        w.data -= 0.01 * w.grad.data   # 3. 更新：手动改 w
        w.grad.data.zero_()      # 4. 梯度清零
    print("progress:", epoch, l.item())

print('predict after training', 4, forward(4).item())
```

### 训练四步（本课版本）

```
1. l = loss(x, y)          # 前向
2. l.backward()            # 反向（自动求梯度）
3. w.data -= lr * w.grad   # 更新权重
4. w.grad.zero_()          # 梯度清零
```

> 第 5 集会把第 3、4 步封装成 `optimizer.zero_grad()` + `optimizer.step()`。

---

## 六、与第 3 课的对比

| | 第 3 课 | 第 4 课 |
|---|--------|--------|
| 求梯度 | 手写 `gradient()` 函数 | `l.backward()` 自动求 |
| 更新参数 | `w -= 0.01 * grad` | `w.data -= 0.01 * w.grad.data` |
| 数据结构 | Python 浮点数 | PyTorch Tensor |
| 计算图 | 无 | 动态构建 |

---

## 七、本课你需要记住的

1. **计算图** = 把运算拆成节点，Forward 算 Loss，Backward 传梯度  
2. **`requires_grad=True`** = 这个张量要参与求导  
3. **`backward()`** = PyTorch 自动链式求导，不用手写公式  
4. **更新用 `.data`**，打印用 `.item()`，更新前 **梯度要清零**  
5. 第 5 集只是把这套流程**工程化**（Module + Loss + Optimizer）

---

## 八、常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| 梯度越来越大/NaN | 学习率太大 | 减小 lr |
| 梯度不更新 | 忘记 `requires_grad=True` | 检查参数设置 |
| 内存暴涨 | 用张量累加 Loss 没 `.item()` | 累加时用 `l.item()` |
| 梯度累加错乱 | 没清零 | 每轮更新前 `zero_()` |

---

## 九、自测清单

- [ ] 能画出 \(y=wx\) 的简单计算图
- [ ] 能解释 Forward 和 Backward 各做什么
- [ ] 知道 `requires_grad`、`backward()`、`zero_()` 的作用
- [ ] 能独立跑通本课 PyTorch 线性模型代码
- [ ] 理解为什么更新用 `w.data` 而不是直接改 `w`

---

## 十、一句话总结

> 反向传播 = 在计算图上用链式法则自动求每个参数的梯度；PyTorch 的 `backward()` 替你在第 3 集手写的 `gradient()` 函数；第 5 集在此基础上引入标准的模型/损失/优化器写法。

---

*下一课：第五课 · 用 PyTorch 实现线性回归（第一个完整工程化训练）*
