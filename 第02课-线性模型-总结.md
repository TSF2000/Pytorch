# PyTorch 深度学习实践 · 第二课 线性模型 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=2)  
> **集数**：第 2 集 · 线性模型  
> **时长**：约 43 分钟  
> **整理日期**：2026-07-09  

---

## 一、本课定位

第二课是**第一次真正动手建模**，但用的是 **NumPy + 纯 Python**，还没上 PyTorch。

- 把第一课「训练流程」拆成可执行的 4 步
- 用**最简单的线性模型**理解：模型、损失、找最优参数
- 用**穷举法**暴力搜索最优 `w`，直观感受「训练 = 最小化 Loss」
- 为第 3 集「梯度下降」、第 5 集「PyTorch 实战」铺路

**本课核心**：不是学会 PyTorch，而是建立「监督学习最小闭环」的直觉。

---

## 二、监督学习的标准流程

```
Data（数据集）→ Model（模型设计）→ Training（训练，确定权重）→ Inferring（推理/应用）
```

| 步骤 | 做什么 |
|------|--------|
| **DataSet** | 准备训练集 + 测试集 |
| **Model** | 选择/设计模型结构（本课：线性模型） |
| **Training** | 在训练集上调整参数，使 Loss 最小 |
| **Inferring** | 用训练好的模型预测新数据 |

### 训练集 vs 测试集

| 概念 | 含义 |
|------|------|
| **训练集** | 用来学参数（`w`、`b`） |
| **测试集** | 用来评估模型在「没见过」的数据上表现如何 |
| **过拟合** | 把训练集里的噪声也学进去了，测试集表现变差 |
| **泛化能力** | 对没见过的数据也能正确预测的能力 |

> 后端类比：训练集 ≈ 开发环境调参；测试集 ≈ 上线前压测，看真实泛化效果。

---

## 三、线性模型

### 1. 基本形式

\[
y = w \cdot x + b
\]

- \(x\)：输入（特征）
- \(y\)：输出（标签/预测目标）
- \(w\)：权重（weight），控制斜率
- \(b\)：偏置（bias），控制截距

**本课先简化**：令 \(b = 0\)，即 \(y = w \cdot x\)，只学一个参数 \(w\)。

### 2. 课程示例数据

```python
x_data = [1.0, 2.0, 3.0]
y_data = [2.0, 4.0, 6.0]
```

规律很明显：\(y = 2x\)，所以最优 \(w = 2.0\)。  
但机器不知道这个规律——要靠训练从数据里「找」出来。

---

## 四、损失函数（Loss）

### 单个样本的误差

\[
\text{loss} = (\hat{y} - y)^2 = (w \cdot x - y)^2
\]

- \(\hat{y}\)：模型预测值
- \(y\)：真实值
- 用**平方**是为了：误差可正可负不互相抵消；且数学上便于求导（第 3 集会用到）

### 训练集上的 MSE（均方误差）

\[
\text{MSE} = \frac{1}{N} \sum_{i=1}^{N} (\hat{y}_i - y_i)^2
\]

- **Loss 是相对样本而言的**（每个点有一个误差）
- **MSE 是相对整个训练集而言的**（一个标量，衡量模型整体好坏）
- **训练目标**：找到使 MSE 最小的 \(w\)（以及后续的 \(b\)）

---

## 五、穷举法（Exhaustive Search）

### 思路

不知道最优 \(w\) 是多少？那就**逐个试**：

1. 设定 \(w\) 的搜索范围（如 0.0 ~ 4.0，步长 0.1）
2. 对每个 \(w\)，用所有训练样本算 MSE
3. 画出 **Loss–\(w\) 曲线**，找最低点

### 核心代码结构

```python
import numpy as np
import matplotlib.pyplot as plt

x_data = [1.0, 2.0, 3.0]
y_data = [2.0, 4.0, 6.0]

def forward(x):
    return x * w          # w 在循环外被赋值

def loss(x, y):
    y_pred = forward(x)
    return (y_pred - y) ** 2

w_list = []
mse_list = []

for w in np.arange(0.0, 4.1, 0.1):
    l_sum = 0
    for x_val, y_val in zip(x_data, y_data):
        y_pred_val = forward(x_val)
        loss_val = loss(x_val, y_val)
        l_sum += loss_val
    mse_list.append(l_sum / len(x_data))
    w_list.append(w)

plt.plot(w_list, mse_list)
plt.xlabel('w')
plt.ylabel('Loss')
plt.show()
```

### 结果

- 当 **\(w = 2.0\)** 时，MSE 最小 → 模型最准确
- Loss 曲线是**开口向上的抛物线**（只有一个最低点）
- 穷举法能工作，但参数一多就**算不动** → 第 3 集引入梯度下降

### 代码细节提醒

| 点 | 说明 |
|----|------|
| `forward()` 里的 `w` | 来自外层 `for w in ...` 循环，不是全局固定值 |
| `zip(x_data, y_data)` | 把输入和标签配对遍历 |
| `np.arange(0.0, 4.1, 0.1)` | 生成搜索序列；注意浮点精度，必要时 `.round(1)` |
| 横轴其实是 \(w\) | 老师提到：真正训练时横轴是 **Epoch（轮次）**，这里用 \(w\) 是为了直观展示 |

---

## 六、本课你需要记住的（精简版）

1. **监督学习四步**：数据 → 模型 → 训练 → 推理
2. **线性模型**：\(y = wx + b\)，本课先只学 \(w\)
3. **MSE**：衡量预测与真实的平均平方误差，**越小越好**
4. **训练 = 找参数使 Loss 最小**
5. **穷举法**：能跑通闭环，但只适合参数很少的情况
6. **还没用 PyTorch**——第 5 集才用 `torch` 重写同一套逻辑

---

## 七、课后作业（课程布置）

> 实现完整线性模型 \(y = wx + b\)，并画出 **Loss 的 3D 图像**（横轴 \(w\)，纵轴 \(b\)，高度为 MSE）。

- 需要二重循环：遍历 \(w\) 和 \(b\) 的组合
- 用 `matplotlib` 的 `Axes3D` 画曲面
- 直观看到 Loss 在 \((w, b)\) 平面上是一个「碗形」曲面，最低点即最优解

---

## 八、与前后课程的衔接

| 本集内容 | 在哪集升级 |
|----------|------------|
| 穷举法找最优 \(w\) | 第 3 集：**梯度下降**（高效搜索） |
| Loss 对参数的依赖 | 第 4 集：**反向传播**（自动算梯度） |
| NumPy 手写 forward / loss | **第 5 集：PyTorch 实现线性回归**（第一个重点实操） |
| 只有 1 个特征 \(x\) | 第 7 集：多维特征输入 |
| 回归问题（预测连续值） | 第 6 集：逻辑回归（分类问题） |

**学习路径建议**：

```
第 2 集（本课）穷举法建立直觉
    ↓
第 3 集 梯度下降（别跳过，是训练的核心）
    ↓
第 4 集 反向传播（理解 PyTorch 自动求导在干什么）
    ↓
第 5 集 用 PyTorch 重写 ← 必须跟练
```

---

## 九、学习检查（自测）

- [ ] 能说出监督学习的 4 个步骤
- [ ] 能解释训练集和测试集的区别，以及什么是过拟合、泛化
- [ ] 能写出线性模型 \(y = wx + b\) 并说明 \(w\)、\(b\) 的含义
- [ ] 能解释 MSE 是什么、为什么要最小化它
- [ ] 能看懂 `forward` / `loss` / 穷举循环的代码逻辑
- [ ] 知道本课示例数据的最优 \(w = 2.0\)，以及为什么
- [ ] 理解穷举法的局限（参数多时不可行）
- [ ] （可选）完成了 \(wx+b\) 的 3D Loss 曲面作业

---

## 十、常见困惑点

| 困惑 | 澄清 |
|------|------|
| 这课怎么没用 PyTorch？ | 故意先用 NumPy 把逻辑拆清楚，第 5 集才上 PyTorch |
| `w` 在 `forward` 里哪来的？ | Python 闭包/外层循环赋值，不是函数参数 |
| Loss 和 MSE 是一回事吗？ | 单样本叫 loss；整个训练集平均叫 MSE |
| 为什么要平方？ | 避免正负抵消；且便于求导（下集讲） |
| 穷举法步长怎么选？ | 步长越小越精确，但计算量越大；实际用梯度下降代替 |

---

## 十一、与第一课的关系

| 第一课（地图） | 第二课（走路） |
|----------------|----------------|
| 训练流程是概念 | 训练流程变成可运行代码 |
| 「调参数使 Loss 下降」 | 用穷举法亲眼看到 Loss 曲线最低点 |
| PyTorch 是工具 | 本课还没用，先建立数学和流程直觉 |

---

## 十二、一句话总结

> 监督学习就是：**拿一个模型（\(y=wx+b\)），在训练集上不断调整参数，让预测误差（MSE）尽量小**。第二课用穷举法「暴力演示」了这个过程；真正高效的参数搜索，从第三课的**梯度下降**开始。

---

*配合《后端转AI应用开发-学习方案记录.md》使用 · 7/9 计划：PyTorch 第 1–5 集 + 跟练第 5 集*
