# PyTorch 深度学习实践 · 第五课 线性回归 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys)  
> **集数**：第 5 集 · Linear Regression with PyTorch  
> **时长**：约 55 分钟  
> **整理日期**：2026-07-11  
> **前置**：第 4 集反向传播（`requires_grad`、`backward()`、梯度清零）

---

## 一、本课定位

第 2～4 集分别学了模型、梯度下降、自动求导。  
第 5 集把它们**整合成 PyTorch 标准训练模板**——后面所有深度学习代码都基于这个结构。

```
Before（第 4 集）          Now（第 5 集）
─────────────────         ─────────────────
手写 forward/loss         nn.Module 定义模型
手写 backward             自动 backward
手动 w.data -= ...        optimizer.step()
w.grad.zero_()            optimizer.zero_grad()
```

**本课核心**：掌握 PyTorch 训练的「万能五步循环」，完成第一个完整工程化模型。

---

## 二、四大步骤

### Step 1：Prepare Dataset（准备数据）

**变化**：\(x\) 和 \(y\) 必须是 **Tensor 矩阵**，不能是 Python 列表。

```python
x_data = torch.Tensor([[1.0], [2.0], [3.0]])   # shape: (3, 1)
y_data = torch.Tensor([[2.0], [4.0], [6.0]])   # shape: (3, 1)
```

| 写法 | shape | 含义 |
|------|-------|------|
| `[1.0, 2.0, 3.0]` | `(3,)` | 一维向量 ❌ 不符合 `nn.Linear` 输入习惯 |
| `[[1.0], [2.0], [3.0]]` | `(3, 1)` | 3 个样本，每个 1 维特征 ✅ |

> 注意双层括号：`[[1.0]]` 是矩阵，`[1.0]` 是向量。

---

### Step 2：Design Model（设计模型）

```python
class LinearModel(torch.nn.Module):
    def __init__(self):
        super(LinearModel, self).__init__()
        self.linear = torch.nn.Linear(1, 1)  # 输入 1 维，输出 1 维

    def forward(self, x):
        return self.linear(x)

model = LinearModel()
```

**要点**：

- 继承 `nn.Module`，`super().__init__()` 必须调用
- `nn.Linear(1, 1)` 内部已有 **w 和 b**，不用自己定义
- 只需写 `forward()`，**不用写 backward()**
- 调用 `model(x)` 时自动执行 `forward()`

---

### Step 3：Loss & Optimizer（损失函数和优化器）

```python
criterion = torch.nn.MSELoss(reduction='sum')  # 旧版：size_average=False
optimizer = torch.optim.SGD(model.parameters(), lr=0.01)
```

| 组件 | 作用 |
|------|------|
| `MSELoss` | 均方误差，适合回归（预测连续值） |
| `SGD` | 随机梯度下降优化器 |
| `model.parameters()` | 把模型所有可训练参数交给优化器 |
| `lr=0.01` | 学习率（课件默认值） |
| `reduction='sum'` | 对 batch 内 loss 求和（课件用法） |

> **Loss 必须是标量**，才能调用 `backward()`。

---

### Step 4：Training Loop（训练循环）⭐ 最重要

```python
for epoch in range(1000):
    y_pred = model(x_data)              # ① 前向传播
    loss = criterion(y_pred, y_data)    # ② 计算损失

    optimizer.zero_grad()               # ③ 梯度清零
    loss.backward()                     # ④ 反向传播
    optimizer.step()                    # ⑤ 更新参数

    print(epoch, loss.item())
```

### 训练五步口诀（务必背熟）

```
forward  → 算预测
loss     → 算误差
zero_grad → 清梯度
backward → 求梯度
step     → 更新参数
```

> 这是 PyTorch 训练的**万能模板**，后续逻辑回归、分类、NLP 都是这个结构。

---

## 三、完整代码

```python
import torch

# Step 1: 数据
x_data = torch.Tensor([[1.0], [2.0], [3.0]])
y_data = torch.Tensor([[2.0], [4.0], [6.0]])

# Step 2: 模型
class LinearModel(torch.nn.Module):
    def __init__(self):
        super(LinearModel, self).__init__()
        self.linear = torch.nn.Linear(1, 1)

    def forward(self, x):
        return self.linear(x)

model = LinearModel()

# Step 3: 损失 + 优化器
criterion = torch.nn.MSELoss(reduction='sum')
optimizer = torch.optim.SGD(model.parameters(), lr=0.01)

# Step 4: 训练
for epoch in range(1000):
    y_pred = model(x_data)
    loss = criterion(y_pred, y_data)

    optimizer.zero_grad()
    loss.backward()
    optimizer.step()

# 查看学到的参数
print('w =', model.linear.weight.item())
print('b =', model.linear.bias.item())

# 测试
x_test = torch.Tensor([[4.0]])
y_test = model(x_test)
print('y_pred =', y_test.data)
```

**期望结果**：\(w \approx 2.0\)，\(b \approx 0.0\)，预测 \(x=4\) 时 \(y \approx 8\)。

---

## 四、与第 4 课的对比

| 操作 | 第 4 集写法 | 第 5 集写法 |
|------|------------|------------|
| 定义参数 | `w = torch.Tensor([1.0])` | `nn.Linear(1,1)` 内置 w、b |
| 前向 | `forward(x)` 函数 | `model(x)` 调用 Module |
| 损失 | `(y_pred-y)**2` 手写 | `nn.MSELoss()` |
| 清零梯度 | `w.grad.data.zero_()` | `optimizer.zero_grad()` |
| 反向 | `l.backward()` | `loss.backward()` |
| 更新 | `w.data -= lr * w.grad` | `optimizer.step()` |

---

## 五、可视化（课后扩展）

用 `matplotlib` 观察训练过程：

```python
import matplotlib.pyplot as plt

epoch_list, loss_list, w_list, b_list = [], [], [], []

for epoch in range(1000):
    y_pred = model(x_data)
    loss = criterion(y_pred, y_data)
    optimizer.zero_grad()
    loss.backward()
    optimizer.step()

    epoch_list.append(epoch)
    loss_list.append(loss.item())
    w_list.append(model.linear.weight.item())
    b_list.append(model.linear.bias.item())

# loss 曲线
plt.plot(epoch_list, loss_list)
plt.xlabel('epoch')
plt.ylabel('loss')
plt.show()

# w、b 收敛曲线
plt.plot(epoch_list, w_list, label='w')
plt.plot(epoch_list, b_list, label='b')
plt.legend()
plt.show()
```

---

## 六、常见坑（实战总结）

| 问题 | 原因 | 解决 |
|------|------|------|
| `TypeError: new() got (list, list, list)` | `torch.Tensor([1.0],[2.0],[3.0])` 传了 3 个参数 | 改成 `torch.Tensor([[1.0],[2.0],[3.0]])` |
| 1000 轮后 w、b 偏差大 | SGD 的 `lr=0.001` 太小 | 课件用 **`lr=0.01`**，或增加 epoch |
| Adadelta loss 下不去 | 把 SGD 的 `lr=0.001` 套给 Adadelta | Adadelta 默认 **`lr=1.0`**，含义不同 |
| Adafactor 每次图差很多 | 自适应优化器 + 随机初始化 | 固定 `torch.manual_seed(42)`；小模型优先用 SGD |
| 不同优化器曲线不可比 | 每次 `model = LinearModel()` 起点不同 | 保存 `init_state`，对比前 `load_state_dict` |
| backward 报错 | Loss 不是标量 | 检查 `reduction` 或是否忘了 `.mean()`/`.sum()` |

### 优化器 lr 速查

| 优化器 | 课件/推荐 lr | 说明 |
|--------|-------------|------|
| **SGD** | `0.01` | 本课默认，简单稳定 |
| **Adam** | `0.001` ~ `0.01` | 自适应，小模型也好用 |
| **Adadelta** | `1.0`（默认） | lr 是更新系数，不是 SGD 那种步长 |
| **Adafactor** | 大模型场景 | 不适合 2 参数玩具题 |

---

## 七、本课你需要记住的

1. **数据**：回归任务用 `(N, 1)` 的 Tensor 矩阵，双层括号
2. **模型**：继承 `nn.Module`，`nn.Linear` 自带 w、b
3. **损失**：回归用 `MSELoss`，输出是连续值
4. **训练五步**：forward → loss → zero_grad → backward → step
5. **查看结果**：`model.linear.weight.item()`、`bias.item()`
6. **SGD + lr=0.01 + 1000 epoch** 是本课标准配置

---

## 八、课后练习回顾

| 练习 | 目的 |
|------|------|
| 换 Adam / Adadelta 等优化器 | 理解不同优化器的 lr 含义和收敛差异 |
| 画 loss / w / b 曲线 | 直观看到训练是否在收敛 |
| 固定随机种子对比 | 公平比较不同优化器 |

---

## 九、与后续课程的关系

| 第 5 集学会 | 在哪用到 |
|------------|----------|
| `nn.Module` + 训练循环 | 第 6 集逻辑回归、第 9 集分类 |
| `nn.Linear` | 第 7 集多维输入 |
| `MSELoss` + `SGD` | 第 8 集 Dataset/DataLoader |
| 训练五步模板 | 整个 NLP 课程、后续 RAG/Agent 项目 |

---

## 十、自测清单

- [ ] 能独立写出五步训练循环，顺序不错
- [ ] 能解释 `nn.Linear(1, 1)` 中两个 1 的含义
- [ ] 知道为什么 x_data 要用 `[[1.0], [2.0], [3.0]]`
- [ ] 能说出 MSELoss 适用于什么任务
- [ ] 跑通代码，得到 w≈2、b≈0
- [ ] 知道 SGD 的 lr 为什么是 0.01 而不是 0.001

---

## 十一、一句话总结

> 第 5 集 = 用 `nn.Module` + `nn.Linear` 定义模型 + `MSELoss` 算损失 + `SGD` 更新参数 + **五步训练循环**；这是 PyTorch 最核心的代码结构，后面每一课只换模型和损失，不换训练套路。

---

*下一课：第六课 · 逻辑回归（从回归走向分类，引入 Sigmoid 和 BCELoss）*
