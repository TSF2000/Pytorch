# PyTorch 深度学习实践 · 第六课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys)  
> **集数**：第 6 集 · Logistic Regression（逻辑斯蒂回归）  
> **时长**：约 50 分钟  
> **整理日期**：2026-07-11  
> **重要程度**：⭐⭐⭐ **第一个分类模型，训练循环与第 5 集相同，重点换模型和损失**

---

## 一、第六课在学什么

第 5 集用线性回归预测**连续值**（如房价、\(y=2x\)）。  
第 6 集解决**分类问题**：输出不是任意实数，而是 **0 或 1 的概率**（如是否及格、是否晴天）。

```
第 5 集（回归）              第 6 集（二分类）
─────────────────           ─────────────────
输出：连续实数               输出：(0, 1) 概率
损失：MSELoss                损失：BCELoss
模型：Linear                 模型：Linear + Sigmoid
标签 y：2, 4, 6              标签 y：0, 0, 1
```

**虽然叫「逻辑回归」，本质是分类算法，不是回归。**

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 5 集 | `nn.Module`、五步训练循环、`nn.Linear`、`SGD` |
| 第 4 集 | Sigmoid 激活函数的直觉（第 4 集已点到） |
| 数学 | 对数、指数的基本概念 |

如果第 5 集代码能跑通且 w≈2、b≈0，可以直接进第 6 集。

---

## 三、核心概念（提前熟悉）

### 1. 为什么需要 Sigmoid

线性输出 \(z = wx + b\) 是 \((-\infty, +\infty)\) 的实数，无法直接表示「概率」。

**Sigmoid 函数**把实数映射到 (0, 1)：

\[
\sigma(z) = \frac{1}{1 + e^{-z}}
\]

| z 的值 | σ(z) |
|--------|------|
| z = 0 | 0.5 |
| z → +∞ | → 1 |
| z → -∞ | → 0 |

```python
y_pred = torch.sigmoid(self.linear(x))
# 或
import torch.nn.functional as F
y_pred = F.sigmoid(self.linear(x))
```

> Sigmoid **没有可训练参数**，直接在 `forward()` 里调用即可，不用在 `__init__` 里初始化。

---

### 2. 逻辑回归模型结构

本质 = **线性层 + Sigmoid**：

```
x → Linear(1,1) → z (实数) → Sigmoid → y_pred (0~1 概率)
```

与第 5 集相比，**只改了 `forward()`**，训练循环完全不变。

---

### 3. 损失函数：BCELoss

回归用 MSE，分类用 **BCE（Binary Cross Entropy，二元交叉熵）**：

\[
loss = - \big[ y \cdot \log(\hat{y}) + (1-y) \cdot \log(1-\hat{y}) \big]
\]

直觉：

- 真实 \(y=1\) 时，希望预测 \(\hat{y}\) 越接近 1，loss 越小
- 真实 \(y=0\) 时，希望预测 \(\hat{y}\) 越接近 0，loss 越小

```python
criterion = torch.nn.BCELoss(reduction='sum')  # 旧版：size_average=False
```

> `BCELoss` 要求输入是 **概率值（0~1）**，所以模型 `forward` 里必须先过 Sigmoid。

---

## 四、与第 5 集的三处变化

| 项目 | 第 5 集（线性回归） | 第 6 集（逻辑回归） |
|------|-------------------|-------------------|
| **标签 y** | 连续值 `[[2.0],[4.0],[6.0]]` | 二值 `[[0],[0],[1]]` |
| **forward** | `return self.linear(x)` | `return torch.sigmoid(self.linear(x))` |
| **损失** | `nn.MSELoss(reduction='sum')` | `nn.BCELoss(reduction='sum')` |

**不变的部分**：`nn.Module` 结构、五步训练循环、`SGD(lr=0.01)`、`model.parameters()`。

---

## 五、完整代码预览（听课前可先扫一眼）

```python
import torch

# Step 1: 数据（注意 y 只有 0 或 1）
x_data = torch.Tensor([[1.0], [2.0], [3.0]])
y_data = torch.Tensor([[0], [0], [1]])

# Step 2: 模型（Linear + Sigmoid）
class LogisticRegressionModel(torch.nn.Module):
    def __init__(self):
        super(LogisticRegressionModel, self).__init__()
        self.linear = torch.nn.Linear(1, 1)

    def forward(self, x):
        y_pred = torch.sigmoid(self.linear(x))
        return y_pred

model = LogisticRegressionModel()

# Step 3: 损失 + 优化器
criterion = torch.nn.BCELoss(reduction='sum')
optimizer = torch.optim.SGD(model.parameters(), lr=0.01)

# Step 4: 训练（与第 5 集完全相同）
for epoch in range(1000):
    y_pred = model(x_data)
    loss = criterion(y_pred, y_data)
    print(epoch, loss.item())

    optimizer.zero_grad()
    loss.backward()
    optimizer.step()

print('w =', model.linear.weight.item())
print('b =', model.linear.bias.item())
```

**期望直觉**：x 越小越倾向预测 0，x 越大越倾向预测 1；loss 逐渐下降。

---

## 六、听课时要重点关注的

| 关注点 | 为什么重要 |
|--------|------------|
| Sigmoid 的作用 | 把线性输出变成概率 |
| 为什么不用 MSE 做分类 | MSE 对分类问题梯度性质差，BCE 更合适 |
| BCELoss 的输入要求 | 必须是 0~1 的概率，不能是原始 logits |
| 训练循环不变 | 强化「换模型/换损失，不换套路」 |
| 逻辑回归 vs 线性回归 | 名字带回归，实际是二分类 |

---

## 七、进阶提示（课上可能提到）

### BCEWithLogitsLoss（了解即可）

工程上更常用 **把 Sigmoid 和 BCE 合并** 的写法，数值更稳定：

```python
class Model(torch.nn.Module):
    def forward(self, x):
        return self.linear(x)  # 输出 logits，不过 Sigmoid

criterion = torch.nn.BCEWithLogitsLoss(reduction='sum')
```

| 损失 | 模型 forward 输出 | 说明 |
|------|------------------|------|
| `BCELoss` | 过 Sigmoid 后的概率 | **课件用法** |
| `BCEWithLogitsLoss` | 原始 logits（实数） | 工程推荐，本课先掌握课件版 |

---

## 八、预习动手任务（15 分钟）

在听第 6 集之前，建议完成：

- [ ] 回顾第 5 集 `linear_regression.py`，确认五步循环能默写
- [ ] 把第 5 集代码复制为 `logistic_regression.py`，按上面预览改三处（y、forward、loss）
- [ ] 思考：为什么 x=1,2 对应 y=0，x=3 对应 y=1 时，模型会学到什么样的 w 和 b？
- [ ] 可选：训练后打印 `y_pred`，看三个样本的预测概率是否分别接近 0 和 1

---

## 九、常见坑（提前避雷）

| 问题 | 原因 | 解决 |
|------|------|------|
| BCELoss 报错 | 输入不在 (0,1) | `forward` 里加 Sigmoid |
| loss 是 NaN | 预测值恰好为 0 或 1 取 log | 检查 lr 是否过大；了解 `BCEWithLogitsLoss` |
| 用了 BCEWithLogitsLoss 又加 Sigmoid | 重复激活 | 二选一：要么 Sigmoid+BCELoss，要么 logits+BCEWithLogitsLoss |
| y 标签写成 float 以外类型 | BCE 要求 float | `torch.Tensor([[0.0],[0.0],[1.0]])` |
| 收敛慢 | lr 太小 | 课件默认 SGD `lr=0.01` |

---

## 十、与后续课程的关系

| 第 6 集学会 | 在哪用到 |
|------------|----------|
| Sigmoid + 二分类 | 第 9 集多分类（Softmax + CrossEntropy） |
| BCELoss | 二分类任务、NLP 中的部分损失设计 |
| 分类 vs 回归 | 后续所有监督学习任务的分岔点 |
| 五步训练循环 | 继续复用，不变 |

---

## 十一、第 5 集 → 第 6 集对照表

| 操作 | 第 5 集 | 第 6 集 |
|------|--------|--------|
| 任务类型 | 回归（连续值） | 二分类（0/1） |
| 模型 forward | `self.linear(x)` | `torch.sigmoid(self.linear(x))` |
| 损失函数 | `MSELoss` | `BCELoss` |
| 标签 y | 任意实数 | 只能是 0 或 1 |
| 训练循环 | 五步 | **完全相同** |
| 优化器 | `SGD(lr=0.01)` | `SGD(lr=0.01)` |

---

## 十二、一句话预习总结

> 第 6 集 = 第 5 集的训练套路不变，把输出加上 **Sigmoid** 变成概率，把 **MSELoss 换成 BCELoss**，标签从连续值变成 **0/1**；这是从回归迈向分类的第一步。

---

*听课建议：先对照第 5 集代码，只改三处跑通；每讲 Sigmoid 或 BCE 公式时暂停，用自己的话复述一遍。*
