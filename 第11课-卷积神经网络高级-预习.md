# PyTorch 深度学习实践 · 第十一课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=11)  
> **集数**：第 11 集 · 卷积神经网络（高级篇）  
> **时长**：约 60 分钟  
> **整理日期**：2026-07-16  
> **重要程度**：⭐⭐ **网络设计思想：分支结构 + 残差连接，为理解现代 CNN 打基础**

---

## 一、第十一课在学什么

第 10 集是**串型 CNN**：Conv → Pool → Conv → Pool → FC，结构简单。  
第 11 集进入**高级 CNN**：网络不再只是直线堆叠，而是有**分支、拼接、跳连接**。

```
第 10 集（基础 CNN）          第 11 集（高级 CNN）
─────────────────           ─────────────────
串型结构（一层接一层）        分支结构（Inception）
简单堆叠卷积层               残差连接（ResNet）
LeNet 风格                   GoogLeNet / ResNet 思想
数据 / 损失 / 循环           **完全复用第 10 集**
```

**本课核心**：理解 Inception 模块（多路卷积 + 拼接）和 ResNet 残差块（跳连接）；**训练框架不变，只换模型结构**。

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 10 集 | `Conv2d`、`MaxPool2d`、尺寸推演、`view` 展平 |
| 第 9 集 | MNIST、CrossEntropyLoss、准确率、`train`/`test` |
| 第 7 集 | `nn.Module` 自定义子模块 |
| Python OOP | 类封装、在 `__init__` 里组合子模块 |

如果第 10 集基础 CNN 能跑通并理解 28→320 的尺寸变化，可以直接进第 11 集。

---

## 三、为什么需要更复杂的结构？

| 问题 | 第 10 集局限 | 第 11 集思路 |
|------|-------------|-------------|
| 卷积核大小难选 | 固定 5×5 | Inception：多路并行，让训练自己选 |
| 层数加深性能反而下降 | 简单堆叠 | ResNet：残差连接缓解梯度消失 |
| 代码重复 | 每层手写 | 封装成 `InceptionA`、`ResidualBlock` 类 |

> 本课偏**设计思想 + 代码结构**，不必死记所有通道数，重点是理解「分支」和「跳连接」。

---

## 四、GoogLeNet 与 Inception Module

### 核心思想

卷积核大小（3×3、5×5、1×1）不好事先决定 → **在同一个块里把几种卷积都试一遍**，训练后好用的路径权重会变大。

### Inception 模块结构（4 条分支）

```
输入 x (B, C, H, W)
    ├── 分支1: AvgPool(3, stride=1, pad=1) → Conv 1×1 → (B, 24, H, W)
    ├── 分支2: Conv 1×1                          → (B, 16, H, W)
    ├── 分支3: Conv 1×1 → Conv 5×5(pad=2)        → (B, 24, H, W)
    └── 分支4: Conv 1×1 → Conv 3×3 → Conv 3×3      → (B, 24, H, W)
                              ↓
                    torch.cat(dim=1) 沿通道拼接
                              ↓
                    输出 (B, 88, H, W)   # 24+16+24+24=88
```

### 拼接的关键约束

四条分支输出必须 **H、W 相同**，只有 **C（通道）** 可以不同。

| 分支 | 保持 H,W 的手段 |
|------|----------------|
| 池化分支 | `avg_pool2d(kernel=3, stride=1, padding=1)` |
| 5×5 卷积 | `padding=2` |
| 3×3 卷积 | `padding=1` |

### 输出通道计算

```
InceptionA 输出通道 = 24 + 16 + 24 + 24 = 88
```

下一层 `Conv2d` 的 `in_channels` 必须等于 **88**。

---

## 五、1×1 卷积（Network in Network）

### 作用

| 作用 | 说明 |
|------|------|
| **改变通道数** | C 通道 → m 通道，H/W 不变 |
| **降维 / 信息融合** | 每个输出元素融合所有输入通道同位置信息 |
| **减少计算量** | 先 1×1 降通道，再做大核卷积，运算量更小 |

```
输入 (B, C, H, W) → Conv2d(C, m, kernel_size=1) → (B, m, H, W)
```

> 1×1 卷积在 Inception 里常用于「先降维再做大卷积」。

---

## 六、InceptionA 代码预览

```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class InceptionA(nn.Module):
    def __init__(self, in_channels):
        super(InceptionA, self).__init__()
        self.branch_pool = nn.Conv2d(in_channels, 24, kernel_size=1)
        self.branch1x1 = nn.Conv2d(in_channels, 16, kernel_size=1)
        self.branch5x5_1 = nn.Conv2d(in_channels, 16, kernel_size=1)
        self.branch5x5_2 = nn.Conv2d(16, 24, kernel_size=5, padding=2)
        self.branch3x3_1 = nn.Conv2d(in_channels, 16, kernel_size=1)
        self.branch3x3_2 = nn.Conv2d(16, 24, kernel_size=3, padding=1)
        self.branch3x3_3 = nn.Conv2d(24, 24, kernel_size=3, padding=1)

    def forward(self, x):
        branch_pool = F.avg_pool2d(x, kernel_size=3, stride=1, padding=1)
        branch_pool = self.branch_pool(branch_pool)
        branch1x1 = self.branch1x1(x)
        branch5x5 = self.branch5x5_2(self.branch5x5_1(x))
        branch3x3 = self.branch3x3_3(self.branch3x3_2(self.branch3x3_1(x)))
        return torch.cat([branch1x1, branch5x5, branch3x3, branch_pool], dim=1)
```

| 点 | 说明 |
|----|------|
| `in_channels` 作为参数 | 不同位置实例化时通道数不同 |
| `torch.cat(..., dim=1)` | 沿**通道维**拼接 |
| 封装成类 | 主网络里 `self.incep1 = InceptionA(10)` 即可复用 |

---

## 七、带 Inception 的完整网络预览

```python
class Net(nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.conv1 = nn.Conv2d(1, 10, kernel_size=5)
        self.incep1 = InceptionA(in_channels=10)    # 输出 88 通道
        self.conv2 = nn.Conv2d(88, 20, kernel_size=5)
        self.incep2 = InceptionA(in_channels=20)   # 输出 88 通道
        self.mp = nn.MaxPool2d(2)
        self.fc = nn.Linear(1408, 10)

    def forward(self, x):
        in_size = x.size(0)
        x = F.relu(self.mp(self.conv1(x)))
        x = self.incep1(x)                       # 10 → 88
        x = F.relu(self.mp(self.conv2(x)))       # 88 → 20
        x = self.incep2(x)                       # 20 → 88
        x = x.view(in_size, -1)                  # → 1408
        return self.fc(x)
```

### 1408 怎么来？（不必手算）

课件建议：**先注释掉 `fc` 和 `view`，用随机输入跑一遍 `print(x.shape)`**。

```python
x = torch.randn(1, 1, 28, 28)
model = Net()
# 注释掉 view 和 fc，逐层 print shape
```

对 MNIST 28×28，最终展平后每张图 **1408** 个元素 → `Linear(1408, 10)`。

---

## 八、ResNet 与残差块

### 问题：层数越深越好吗？

实验发现：**20 层比 56 层效果更好**。

原因：**梯度消失**——反向传播时梯度连乘，靠近输入的层几乎得不到更新。

### 残差连接（Skip Connection）

\[
\text{output} = F(x) + x
\]

不直接学 \(H(x)\)，而是学**残差** \(F(x) = H(x) - x\)。  
跳连接让梯度可以「抄近路」传回前面，缓解梯度消失。

### 约束

相加时 \(F(x)\) 和 \(x\) 的 **C、H、W 必须完全一致**。

### ResidualBlock 代码预览

```python
class ResidualBlock(nn.Module):
    def __init__(self, channels):
        super(ResidualBlock, self).__init__()
        self.conv1 = nn.Conv2d(channels, channels, kernel_size=3, padding=1)
        self.conv2 = nn.Conv2d(channels, channels, kernel_size=3, padding=1)

    def forward(self, x):
        y = F.relu(self.conv1(x))
        y = self.conv2(y)
        return F.relu(x + y)    # 先相加，再激活
```

| 点 | 说明 |
|----|------|
| 输入输出通道相同 | `channels → channels` |
| `padding=1` + `k=3` | 保持 H、W 不变 |
| `x + y` | 跳连接的核心 |

### 带 ResNet 的网络预览

```python
class Net(nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.conv1 = nn.Conv2d(1, 16, kernel_size=5)
        self.rblock1 = ResidualBlock(16)
        self.conv2 = nn.Conv2d(16, 32, kernel_size=5)
        self.rblock2 = ResidualBlock(32)
        self.mp = nn.MaxPool2d(2)
        self.fc = nn.Linear(512, 10)

    def forward(self, x):
        in_size = x.size(0)
        x = self.mp(F.relu(self.conv1(x)))
        x = self.rblock1(x)
        x = self.mp(F.relu(self.conv2(x)))
        x = self.rblock2(x)
        x = x.view(in_size, -1)
        return self.fc(x)
```

MNIST 路径下展平为 **512** → `Linear(512, 10)`。

---

## 九、听课时要重点关注的

| 关注点 | 为什么重要 |
|--------|------------|
| Inception 4 条分支 | 理解「并行 + 拼接」设计模式 |
| `torch.cat(dim=1)` | 通道维拼接，后面 in_channels 要对上 |
| 1×1 卷积的作用 | 改通道、降维 |
| 残差 `x + y` | 理解跳连接为何能缓解梯度消失 |
| 子模块封装成类 | `InceptionA`、`ResidualBlock` 可复用 |
| 1408 / 512 怎么确定 | 用随机张量 `print shape`，不必死算 |
| 测试准确率不一定最后一轮最高 | 可能过拟合，要观察 test 曲线 |

---

## 十、与第 10 集的对照

| 项目 | 第 10 集 | 第 11 集 |
|------|--------|--------|
| 网络结构 | 串型 Conv→Pool→Conv→Pool→FC | **分支（Inception）+ 跳连接（ResNet）** |
| 子模块 | 无 | `InceptionA`、`ResidualBlock` |
| 拼接操作 | 无 | `torch.cat(dim=1)` |
| 1×1 卷积 | 未涉及 | **重点** |
| 数据 / 损失 / 循环 | MNIST + CE | **完全相同** |
| FC 输入维 | 320 | 1408（Inception）或 512（ResNet） |

---

## 十一、预习动手任务（30 分钟）

- [ ] 复习第 10 集：能独立写出基础 CNN 并手算 28→320
- [ ] 理解 `torch.cat`：两个 `(2, 3, 4, 4)` 沿 dim=1 拼接 → `(2, 6, 4, 4)`
- [ ] 手写 `InceptionA` 类，用 `x = torch.randn(2, 10, 12, 12)` 测试输出 shape 是否为 `(2, 88, 12, 12)`
- [ ] 手写 `ResidualBlock`，确认输入输出 shape 一致
- [ ] 用「注释 fc + 随机输入 print shape」法，验证 1408 或 512
- [ ] 可选：把第 10 集 `Net` 换成带 Inception 的版本，跑 1～2 epoch

---

## 十二、常见坑（提前避雷）

| 问题 | 原因 | 解决 |
|------|------|------|
| `cat` 后通道对不上 | 下一层 `in_channels` 没改 | Inception 输出 88，下一层 in=88 |
| 分支 H,W 不一致 | padding/stride 设错 | 按课件：5×5 用 pad=2，3×3 用 pad=1 |
| 残差相加报错 | C/H/W 不匹配 | 残差块内 conv 保持 channels 不变 |
| `Linear` 维度错 | 1408/512 手算错 | 用随机输入 `print(x.view(1,-1).shape)` |
| 性能提升不明显 | 全连接层太简单 | 课件观点：主要改卷积部分 |
| 训练轮数越多越好 | 过拟合 | 看 **test 准确率曲线**，峰值不一定在最后 |

---

## 十三、开发技巧（课件强调）

### 增量式构建网络

每加一个新模块，就 `print` 一次输出 shape，确认符合预期再往下加。

### 确定 FC 输入维的标准做法

```python
# 1. 先不写 fc
# 2. 用 MNIST 大小的随机输入
x = torch.randn(1, 1, 28, 28)
out = model(x)          # 跑到 view 前
print(out.view(1, -1).shape)   # 看最后一维是多少
# 3. 再写 self.fc = nn.Linear(最后一维, 10)
```

### 复杂结构用类封装

| 结构 | 封装方式 |
|------|----------|
| Inception | `class InceptionA(nn.Module)` |
| ResNet | `class ResidualBlock(nn.Module)` |
| 主网络 | `__init__` 里组合子模块 |

---

## 十四、与后续课程的关系

| 第 11 集学会 | 在哪用到 |
|------------|----------|
| 分支 + 拼接（Inception） | GoogLeNet、部分检测网络 |
| 残差连接（ResNet） | 几乎所有现代 CNN 骨干 |
| 1×1 卷积 | 通道变换、瓶颈结构 |
| 子模块封装 | 构建复杂模型的通用方法 |
| 观察 test 曲线防过拟合 | 所有训练任务 |

---

## 十五、第 12 集预告（了解即可）

第 11 集仍是**图像分类（CNN）**。第 12 集进入**序列数据**：

- **RNN（循环神经网络）**：处理文本、语音等序列
- 输入不再是 `(N,C,H,W)`，而是 `(N, Seq, Feature)`
- 核心：`nn.RNN`、隐藏状态、序列展开

CNN 处理空间，RNN 处理时间/顺序——两条不同的深度学习主线。

---

## 十六、一句话预习总结

> 第 11 集 = 第 10 集 MNIST 流水线不变 + **Inception（多路卷积 + cat 拼接）** + **ResNet（残差块 x+y 跳连接）** + **1×1 卷积**；重点是网络设计思想与子模块封装，不是死记通道数。

---

*听课建议：先单独跑通 `InceptionA` 和 `ResidualBlock` 的 shape 测试；再组装完整 `Net`；最后用「随机输入 print shape」确定 `Linear` 输入维，避免手算出错。*
