# PyTorch 深度学习实践 · 第三课 梯度下降 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys)  
> **集数**：第 3 集 · Gradient Descent（梯度下降算法）  
> **时长**：约 41 分钟  
> **整理日期**：2026-07-11  
> **前置**：第 2 集线性模型（Loss、Cost、\(y=wx+b\)）

---

## 一、本课定位

第 2 集用**穷举法**找最优 \(w\)，只能在小范围、低维参数里暴力搜索。  
第 3 集解决核心问题：**如何自动、高效地找到让 Loss 最小的参数？**

**答案：梯度下降（Gradient Descent）**

---

## 二、寻找最优 w 的三种方法

| 方法 | 思路 | 优缺点 |
|------|------|--------|
| **穷举法** | 在区间内逐个尝试 w | 简单但慢，参数一多不可行 |
| **分治法** | 在凸函数上二分找最优点 | 仅适用于规则凸曲面 |
| **梯度下降** | 沿负梯度方向一步步走 | 实用，但可能陷入局部最优/鞍点 |

> 梯度下降是**贪心法**：每一步只朝当前下降最快的方向走。

---

## 三、核心概念

### 1. Loss vs Cost

| 概念 | 含义 |
|------|------|
| **Loss** | 单个样本的误差 |
| **Cost** | 整个训练集 Loss 的平均（如 MSE） |

本课训练时优化的对象是 **Cost**。

### 2. 梯度（Gradient）

- Cost 对参数 \(w\) 的偏导数：\(\frac{\partial cost}{\partial w}\)
- **负梯度方向** = Cost **下降最快**的方向
- 更新公式：\(w \leftarrow w - \eta \cdot \frac{\partial cost}{\partial w}\)
- \(\eta\) = **学习率（learning rate）**

### 3. 线性模型 \(y = wx\) 的梯度

对单个样本，Loss = \((wx - y)^2\)，对 \(w\) 求导：

\[
\frac{\partial loss}{\partial w} = 2x(wx - y)
\]

多个样本时取平均。

---

## 四、批量梯度下降（BGD）

**做法**：每一轮（epoch）用**全部样本**算 Cost 和梯度，再更新 \(w\)。

```python
x_data = [1.0, 2.0, 3.0]
y_data = [2.0, 4.0, 6.0]
w = 3.0  # 随机初始值

def forward(x):
    return x * w

def cost(xs, ys):
    cost = 0
    for x, y in zip(xs, ys):
        cost += (forward(x) - y) ** 2
    return cost / len(xs)

def gradient(xs, ys):
    grad = 0
    for x, y in zip(xs, ys):
        grad += 2 * x * (x * w - y)
    return grad / len(xs)

for epoch in range(100):
    cost_val = cost(x_data, y_data)
    grad_val = gradient(x_data, y_data)
    w -= 0.01 * grad_val   # 学习率 0.01
    print('Epoch:', epoch, 'w=', w, 'loss=', cost_val)
```

**结果**：\(w\) 从 3.0 逐渐逼近 2.0，Cost 趋近 0。

### 训练过程观察

- 横轴用 **epoch**（训练轮数），纵轴画 **cost**，看下降趋势
- 真实数据有噪声，Loss **很难到 0**，这是正常的
- 可用**指数加权平滑**让曲线更易读：\(C_i' = \beta C_i + (1-\beta) C_{i-1}'\)

### 训练失败的信号

- Cost 先降后**上升** → **发散**，没收敛
- **最常见原因：学习率太大**

---

## 五、随机梯度下降（SGD）

**做法**：每个样本单独算梯度，**算一个更新一次** \(w\)。

```python
for epoch in range(100):
    for x, y in zip(x_data, y_data):
        grad = 2 * x * (x * w - y)
        w -= 0.01 * grad
        l = (x * w - y) ** 2
    print("progress:", epoch, "w:", w, "loss", l)
```

### 为什么用 SGD？

- 全批量 Cost 曲面可能有**鞍点**（梯度为 0 但不是最优点），会卡住
- 单样本梯度带**随机噪声**，可能「抖」过鞍点，继续下降
- 深度学习中 **SGD 非常常用**

### BGD vs SGD

| | 批量梯度下降 | 随机梯度下降 |
|---|-------------|-------------|
| 每次用多少样本 | 全部 | 1 个 |
| 并行性 | 可并行，效率高 | 样本间有依赖，难并行 |
| 收敛路径 | 平滑 | 有噪声，但更可能找到好解 |
| 实际使用 | 较少单独用 | 常用（或 Mini-Batch） |

---

## 六、Mini-Batch（小批量）

**折中方案**：每次取 **N 个样本**算梯度再更新。

- 兼顾 SGD 的随机性和 BGD 的计算效率
- 深度学习默认方式（第 7、8 集会详细讲）
- 本课先建立概念，第 5 集用 PyTorch 的 `optimizer` 封装

---

## 七、本课你需要记住的

1. **梯度下降** = 沿负梯度方向更新参数，让 Cost 变小  
2. **学习率**太大 → 发散；太小 → 收敛慢  
3. **SGD** = 每个样本更新一次，带噪声，深度学习中更常用  
4. **Mini-Batch** = 实际训练时的折中方案  
5. 训练三要素已经齐全：**模型（forward）+ 损失（cost）+ 更新规则（梯度下降）**

---

## 八、与前后课程衔接

| 本集 | 下集 |
|------|------|
| 手写 `gradient()` 函数 | 第 4 集：用**计算图 + backward()** 自动求梯度 |
| NumPy 手动更新 `w` | 第 5 集：用 **optimizer.step()** 自动更新 |
| 理解「为什么要反向传播」 | 第 4 集回答「PyTorch 怎么自动算梯度」 |

---

## 九、自测清单

- [ ] 能写出 \(w\) 的更新公式
- [ ] 能解释 Loss 和 Cost 的区别
- [ ] 能说出学习率太大时会怎样
- [ ] 能解释 SGD 为什么比全批量更常用
- [ ] 知道 Mini-Batch 是什么

---

## 十、一句话总结

> 穷举法太慢，梯度下降用「导数指引方向」自动找最优参数；SGD 每次用一个样本更新，带随机性更易收敛；第 4 集开始用 PyTorch 自动算这些梯度。

---

*下一课：第四课 · 反向传播与 Tensor*
