# PyTorch 深度学习实践 · 第十三课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=13)  
> **集数**：第 13 集 · 循环神经网络（高级篇）  
> **时长**：约 70～80 分钟  
> **整理日期**：2026-08-08  
> **重要程度**：⭐⭐⭐ **课程收官：Embedding + GRU + 变长序列 + 双向 RNN 分类器**

---

## 一、第十三课在学什么

第 12 集用玩具任务 `hello → ohlol` 搞懂了 **RNNCell / RNN** 和 `(seq, batch, feature)` 维度。  
第 13 集把 RNN 接到**更接近真实的 NLP 分类任务**：输入一个人名，预测它属于哪种语言/国家（**18 分类**）。

```
第 12 集（基础 RNN）              第 13 集（高级 RNN）
─────────────────────            ─────────────────────
hello → ohlol（逐步字符预测）     人名 → 国家（整段序列一个标签）
One-Hot + nn.RNN                  Embedding + GRU
固定短序列、batch=1               变长序列、批处理 + Padding
每步都算 CE                       只用最终 hidden → Linear → CE
单向、单层玩具网                  双向、多层、pack_padded_sequence
```

**本课核心**：

1. **Embedding**：用稠密向量替代稀疏 One-Hot  
2. **只用最后隐藏状态做分类**（序列分类 ≠ 逐步标注）  
3. **变长序列**：Padding + 按长度排序 + `pack_padded_sequence`  
4. **双向 GRU**：同时看「过去」和「未来」信息  

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 12 集 | `nn.RNN` 输入输出 shape、隐藏状态、`h0` 重置 |
| 第 9 集 | `CrossEntropyLoss`、`torch.max` 取类别、准确率 |
| 第 8 集 | `Dataset` / `DataLoader`、batch 训练循环 |
| Python | `ord(c)`、字符串、`gzip`/`csv` 读文件（跟课件即可） |

有 Java 背景：把人名想成「字符数组」；`Embedding` 像一张 **可学习的查表**（index → 向量）；双向 RNN 像**从左扫一遍再从右扫一遍，再把两端结果拼起来**。

---

## 三、任务长什么样？

### 输入 / 输出

| 项目 | 内容 |
|------|------|
| 输入 | 人名字符串，如 `Maclean`、`Zhang` |
| 输出 | 18 个国家/语言之一（如 Chinese、English…） |
| 损失 | `CrossEntropyLoss`（与第 9 集相同） |
| 数据 | `names_train.csv.gz` / `names_test.csv.gz` |

### 与第 12 集的本质差别

| | 第 12 集 | 第 13 集 |
|--|----------|----------|
| 标签粒度 | **每个时间步**一个标签 | **整条序列**一个标签 |
| 用哪段 hidden | 每一步 `h_t` 都接损失 | 通常只用**最终** `hidden` |
| 序列长度 | 固定（如 5） | **长短不一** |

预习时先想清楚：分类任务要的是「这个名字整体像哪国人」，不是「第 3 个字母该是什么」。

---

## 四、为什么要 Embedding？

第 12 集用 One-Hot：字典大小 = 向量维数，又高又稀疏。

本课字符用 **ASCII**（`ord(c)`，字典大小设为 **128**）：

```
'M' → 77 → Embedding 查表 → 一个 hidden_size 维稠密向量
```

| | One-Hot | Embedding |
|--|---------|-----------|
| 维度 | = 字典大小（大） | 自定（如 100，小） |
| 是否可学习 | 否（固定 0/1） | **是**（随训练更新） |
| 稠密性 | 稀疏 | 稠密 |
| 输入给层 | `FloatTensor` 向量 | **`LongTensor` 下标** |

```python
emb = nn.Embedding(num_embeddings=128, embedding_dim=100)
# 输入: (seq_len, batch) 的 Long 下标
# 输出: (seq_len, batch, 100)
```

> 直觉：Embedding ≈「把类别下标变成可学习的特征向量」；不必先手工 One-Hot。

---

## 五、为什么用 GRU？双向又是什么？

### GRU（了解即可）

相对普通 RNN，GRU 用**门控**缓解长程依赖/梯度问题，API 与 `nn.RNN` 很像：

```python
gru = nn.GRU(input_size, hidden_size, num_layers, bidirectional=True)
out, hidden = gru(inputs, h0)
```

本课重点是**用法**，不必死记 GRU 内部公式。

### 双向（Bidirectional）

```
单向：只从左到右 → 每个位置只「看见过去」
双向：再从右到左扫一遍 → 拼接两端信息
```

| 项目 | 单向 | 双向 |
|------|------|------|
| `n_directions` | 1 | 2 |
| h0 shape | `(num_layers, B, H)` | `(num_layers * 2, B, H)` |
| 最终送进 Linear | `hidden[-1]`，宽 `H` | `cat([hidden[-1], hidden[-2]])`，宽 `2H` |

人名分类里，前后缀（如 `-ski`、`Mac-`）都可能有用，所以课件默认 **双向**。

---

## 六、变长序列三件套（本课最难、最重要）

张量要求同一 batch 内 shape 一致，但名字长短不同 → 必须处理。

### 1. Padding

按 batch 内**最长名字**补 0，凑成矩形：

```
Maclean → [77,97,99,108,101,97,110]     长度 7
Li      → [76,105,  0,  0,  0,  0,  0]  补到 7
```

### 2. 按长度降序排序

`pack_padded_sequence` 要求序列**按真实长度从长到短排**；姓名和国家标签要**用同一套索引**一起重排。

### 3. `pack_padded_sequence`

打包后 GRU **跳过无效的 padding 位置**，既加速又避免「补零」污染计算。

```python
from torch.nn.utils.rnn import pack_padded_sequence
packed = pack_padded_sequence(embedding, seq_lengths)  # seq_lengths 常需 .cpu()
out, hidden = gru(packed, h0)
```

预习时记住流程即可：

```
name → ASCII 下标 → pad 成矩阵 → 按长度排序 → Embedding → pack → GRU → 拼 hidden → FC
```

---

## 七、模型骨架（听课时对照）

```python
class RNNClassifier(nn.Module):
    def __init__(self, input_size, hidden_size, output_size,
                 n_layers=1, bidirectional=True):
        super().__init__()
        self.n_directions = 2 if bidirectional else 1
        self.embedding = nn.Embedding(input_size, hidden_size)
        self.gru = nn.GRU(hidden_size, hidden_size, n_layers,
                          bidirectional=bidirectional)
        self.fc = nn.Linear(hidden_size * self.n_directions, output_size)

    def forward(self, input, seq_lengths):
        # input: (batch, seq) → 转置为 (seq, batch)
        input = input.t()
        hidden = self._init_hidden(input.size(1))
        emb = self.embedding(input)                    # (seq, batch, H)
        packed = pack_padded_sequence(emb, seq_lengths)
        _, hidden = self.gru(packed, hidden)
        if self.n_directions == 2:
            hidden_cat = torch.cat([hidden[-1], hidden[-2]], dim=1)
        else:
            hidden_cat = hidden[-1]
        return self.fc(hidden_cat)                     # (batch, 18)
```

常见超参（课件量级）：`HIDDEN_SIZE=100`，`BATCH_SIZE=256`，`N_LAYER=2`，`N_CHARS=128`，`Adam(lr=0.001)`。

---

## 八、数据流一眼图

```
"Maclean"
   │ ord(c)
   ▼
[77,97,99,108,101,97,110]     ← Long 下标，不是 One-Hot
   │ batch 内 pad + 按长度排序
   ▼
(seq, batch) 下标矩阵
   │ Embedding
   ▼
(seq, batch, H)
   │ pack_padded_sequence
   ▼
GRU（可双向、多层）
   │ 取最终 hidden（双向则 cat）
   ▼
Linear → 18 logits → CrossEntropyLoss
```

---

## 九、与第 12 集对照

| 项目 | 第 12 集 | 第 13 集 |
|------|----------|----------|
| 任务 | 序列到序列（逐步） | **序列到类别**（整段） |
| 字符编码 | One-Hot | **Embedding + ASCII 下标** |
| 循环单元 | RNN / RNNCell | **GRU** |
| 方向 | 单向 | **常双向** |
| 长度 | 固定 | **Padding + pack** |
| 损失位置 | 每一步 | **最后 FC 一次** |
| 批处理 | 常 batch=1 | **DataLoader 大 batch** |

---

## 十、预习动手任务（25～40 分钟）

- [ ] 复习第 12 集：默写 `nn.RNN` 的 `out` / `hidden` shape  
- [ ] 对 `"Ab"` 手算 `ord` → `[65, 98]`，想象 pad 成长度 5 后的样子  
- [ ] 跑通一小段 `nn.Embedding(128, 16)`，输入 `LongTensor`，打印输出 shape  
- [ ] 用纸画：单向 vs 双向时，最终 `hidden` 怎么拼进 Linear  
- [ ] 想清楚：为什么分类任务**不用**每一步的 `out`，而用最终 `hidden`  
- [ ] 扫一眼第 8 集 `Dataset`：本课 `NameDataset` 返回 `(name字符串, country索引)`  

---

## 十一、常见坑（提前避雷）

| 问题 | 原因 | 解决 |
|------|------|------|
| Embedding 报错 | 喂了 float One-Hot | 喂 **Long 下标** |
| pack 报错 | 未按长度降序 | `sort(descending=True)`，标签同步重排 |
| pack 与 GPU | `seq_lengths` 在 GPU 上 | 打包前 `.cpu()`（视 PyTorch 版本） |
| Linear 维不对 | 忘了双向 ×2 | `hidden_size * n_directions` |
| 准确率乱飞 | pad 参与了计算且没 pack | 用 `pack_padded_sequence` |
| 把每步 out 都接 CE | 当成了第 12 集逐步任务 | **整段一个标签**，只用最终表示 |
| 国家字符串当 label | CE 要类别下标 | `country_dict` 映射成 `0～17` |

---

## 十二、与整门课的关系

| 本课学会 | 意义 |
|----------|------|
| Embedding | 几乎所有 NLP 模型的入口 |
| 变长 + pack | 批处理真实文本的标准手法 |
| 双向 RNN / GRU | 理解 BiLSTM、序列标注的前置 |
| 序列分类流水线 | 情感分析、意图分类等同构 |

本课是该系列**最后一集**：把 Dataset、CE、RNN、Embedding、GPU 等串成一条完整业务线。

---

## 十三、对你学习路线的提醒

压缩方案里：

- ✅ **第 12 集必看**（维度与 Cell）  
- ✅ **第 13 集必看**（真实序列分类范式）  
- 听完第 13 集，再进 Embedding / LSTM / Attention / Transformer 会顺很多  

---

## 十四、一句话预习总结

> 第 13 集 = **用人名做 18 分类**：字符 ASCII 下标 → **Embedding** →（Padding + 排序 + **pack**）→ **双向 GRU** → 拼最终 hidden → Linear + CE。相对第 12 集，关键升级是 **「整段一个标签」** 和 **「变长批处理」**。

---

*听课建议：先分清「逐步预测」vs「序列分类」；再盯 make_tensors（pad + 排序）；最后跟写 Embedding→GRU→cat→FC，每步 print shape。*
