# PyTorch 深度学习实践 · 第五课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys)  
> **集数**：第 5 集 · Linear Regression with PyTorch  
> **时长**：约 55 分钟  
> **整理日期**：2026-07-11  
> **重要程度**：⭐⭐⭐ **本课程第一个必跟练集，建议边看边敲代码**

---

## 一、第五课在学什么

第 2～4 集分别学了：模型、梯度下降、自动求导。  
第 5 集把它们**整合成 PyTorch 标准训练模板**——后面所有深度学习代码都基于这个结构。

```
Before（第 4 集）          Now（第 5 集）
─────────────────         ─────────────────
手写 forward/loss         nn.Module 定义模型
手写 backward             自动 backward
手动 w.data -= ...        optimizer.step()
w.grad.zero_()            optimizer.zero_grad()
```

**学完第 5 集，你就掌握了 PyTorch 训练的基本套路。**

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 2 集 | 线性模型 \(y = wx + b\)，MSE 损失 |
| 第 3 集 | 梯度下降、学习率、epoch |
| 第 4 集 | Tensor、`requires_grad`、`backward()`、梯度清零 |
| Python | 类的继承、`__init__`、`super()` |

如果第 4 集代码能跑通，可以直接进第 5 集。

---

## 三、第五课四大步骤（提前熟悉）

### Step 1：Prepare Dataset（准备数据）

**变化**：\(x\) 和 \(y\) 必须是 **Tensor 矩阵**，不能是 Python 列表。

```python
x_data = torch.Tensor([[1.0], [2.0], [3.0]])   # shape: (3, 1)
y_data = torch.Tensor([[2.0], [4.0], [6.0]])   # shape: (3, 1)
```

> 注意双层括号：`[[1.0]]` 是矩阵，`[1.0]` 是向量。

---

### Step 2：Design Model（设计模型）

用 `torch.nn.Module` 封装模型：

```python
class LinearModel(torch.nn.Module):
    def __init__(self):
        super(LinearModel, self).__init__()
        self.linear = torch.nn.Linear(1, 1)  # 输入1维，输出1维

    def forward(self, x):
        y_pred = self.linear(x)
        return y_pred

model = LinearModel()
```

**要点**：

- 继承 `nn.Module`
- `nn.Linear(1, 1)` 内部已有 **w 和 b**，不用自己定义
- 只需写 `forward()`，**不用写 backward()**（自动求导）
- 调用 `model(x)` 时会自动执行 `forward()`

---

### Step 3：Loss & Optimizer（损失函数和优化器）

```python
criterion = torch.nn.MSELoss(reduction='sum')  # 课程旧版用 size_average=False
optimizer = torch.optim.SGD(model.parameters(), lr=0.01)
```

| 组件 | 作用 |
|------|------|
| `MSELoss` | 均方误差损失 |
| `SGD` | 随机梯度下降优化器 |
| `model.parameters()` | 把模型所有可训练参数交给优化器 |
| `lr=0.01` | 学习率 |

> **Loss 必须是标量**，才能调用 `backward()`。

---

### Step 4：Training Loop（训练循环）⭐ 最重要

```python
for epoch in range(1000):
    y_pred = model(x_data)              # ① 前向传播
    loss = criterion(y_pred, y_data)    # ② 计算损失

    optimizer.zero_grad()               # ③ 梯度清零
    loss.backward()                     # ④ 反向传播
    optimizer.step()                    # ⑤ 更新参数

    print(epoch, loss.item())
```

### 训练五步口诀（务必背熟）

```
forward  → 算预测
loss     → 算误差
zero_grad → 清梯度
backward → 求梯度
step     → 更新参数
```

> 这是 PyTorch 训练的**万能模板**，RAG、Agent 项目里的模型训练也是这个结构。

---

## 四、完整代码预览（听课前可先扫一眼）

```python
import torch

# Step 1: 数据
x_data = torch.Tensor([[1.0], [2.0], [3.0]])
y_data = torch.Tensor([[2.0], [4.0], [6.0]])

# Step 2: 模型
class LinearModel(torch.nn.Module):
    def __init__(self):
        super(LinearModel, self).__init__()
        self.linear = torch.nn.Linear(1, 1)

    def forward(self, x):
        return self.linear(x)

model = LinearModel()

# Step 3: 损失 + 优化器
criterion = torch.nn.MSELoss(reduction='sum')
optimizer = torch.optim.SGD(model.parameters(), lr=0.01)

# Step 4: 训练
for epoch in range(1000):
    y_pred = model(x_data)
    loss = criterion(y_pred, y_data)
    print(epoch, loss.item())

    optimizer.zero_grad()
    loss.backward()
    optimizer.step()

# 查看学到的参数
print('w =', model.linear.weight.item())
print('b =', model.linear.bias.item())

# 测试
x_test = torch.Tensor([[4.0]])
y_test = model(x_test)
print('y_pred =', y_test.data)
```

**期望结果**：\(w \approx 2.0\)，\(b \approx 0.0\)，预测 \(x=4\) 时 \(y \approx 8\)。

---

## 五、听课时要重点关注的

| 关注点 | 为什么重要 |
|--------|------------|
| `nn.Module` 怎么继承 | 后面所有模型都这么写 |
| `nn.Linear(in, out)` 含义 | 理解输入输出维度 |
| 训练循环五步 | 所有 PyTorch 项目通用 |
| `model.parameters()` | 优化器怎么找到 w、b |
| `weight.item()` / `bias.item()` | 怎么查看训练结果 |
| Loss 必须是标量 | 否则 backward 报错 |

---

## 六、第 4 集 → 第 5 集对照表

| 操作 | 第 4 集写法 | 第 5 集写法 |
|------|------------|------------|
| 定义参数 | `w = torch.Tensor([1.0])` | `nn.Linear(1,1)` 内置 w、b |
| 前向 | `forward(x)` 函数 | `model(x)` 调用 Module |
| 损失 | `(y_pred-y)**2` 手写 | `nn.MSELoss()` |
| 清零梯度 | `w.grad.data.zero_()` | `optimizer.zero_grad()` |
| 反向 | `l.backward()` | `loss.backward()` |
| 更新 | `w.data -= lr * w.grad` | `optimizer.step()` |

---

## 七、预习动手任务（15 分钟，强烈推荐）

在听第 5 集之前，建议完成：

- [ ] 确认 PyTorch 已安装：`python -c "import torch; print(torch.__version__)"`
- [ ] 跑通第 4 集代码（`w.requires_grad` + `backward` 版）
- [ ] 把第 4 集代码里的 `w.data -= 0.01 * w.grad.data` 改成 `0.001`，观察收敛速度变化
- [ ] 新建 `linear_regression.py`，把上面「完整代码预览」敲一遍（可先不看讲解）

---

## 八、课后练习预告

第 5 集作业：

1. **换不同优化器**：如 `torch.optim.Adam`，对比收敛速度
2. 阅读 [PyTorch 官方示例](https://pytorch.org/tutorials/beginner/examples.html)

---

## 九、与后续课程的关系

| 第 5 集学会 | 在哪用到 |
|------------|----------|
| `nn.Module` + 训练循环 | 第 6 集逻辑回归、第 9 集分类 |
| `nn.Linear` | 第 7 集多维输入 |
| `MSELoss` + `SGD` | 第 8 集 Dataset/DataLoader |
| 训练五步模板 | 整个 NLP 课程、后续 RAG/Agent 项目 |

---

## 十、一句话预习总结

> 第 5 集 = 用 `nn.Module` 定义模型 + `MSELoss` 算损失 + `SGD` 更新参数 + **五步训练循环**；这是 PyTorch 最核心的代码结构，**必须跟练**。

---

*听课建议：准备两个窗口——一个看视频，一个敲代码；每讲一步就暂停写一步，不要等看完再写。*
