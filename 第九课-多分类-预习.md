# PyTorch 深度学习实践 · 第九课 预习指南

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=9)  
> **集数**：第 9 集 · 多分类问题（Softmax Classifier）  
> **时长**：约 50 分钟  
> **整理日期**：2026-07-12  
> **重要程度**：⭐⭐⭐ **从二分类迈向多分类，引入 MNIST + CrossEntropyLoss**

---

## 一、第九课在学什么

第 6～8 集都是**二分类**（输出 1 个概率，BCELoss + Sigmoid）。  
第 9 集解决**多分类**问题：输出 C 个类别的概率（如手写数字 0～9 共 10 类）。

```
第 8 集（二分类）            第 9 集（多分类）
─────────────────           ─────────────────
输出：1 维概率               输出：C 维概率（如 10 维）
激活：Sigmoid                激活：Softmax（理解即可）
损失：BCELoss                损失：CrossEntropyLoss
标签：0 或 1（float）        标签：类别编号 0~9（long）
数据：diabetes（8 维）       数据：MNIST（28×28 图像）
```

**本课核心**：理解 Softmax、用 `CrossEntropyLoss` 做多分类；**Dataset/DataLoader 双层循环不变，换模型输出和损失函数**。

---

## 二、预习：你需要的前置知识

| 来自 | 需要掌握 |
|------|----------|
| 第 8 集 | `Dataset`、`DataLoader`、双层训练循环 |
| 第 7 集 | 多层 `nn.Linear`、隐层 ReLU |
| 第 6 集 | 交叉熵损失的直觉 |
| 数学 | 指数、对数的基本概念 |

如果第 8 集 diabetes + DataLoader 能跑通，可以直接进第 9 集。

---

## 三、Softmax 函数（提前熟悉）

二分类用 Sigmoid 把 1 个实数压到 (0, 1)。  
多分类有 C 个输出 \(z_1, z_2, \ldots, z_C\)，需要变成**概率分布**（每项 ∈ (0,1)，且总和 = 1）：

\[
\text{Softmax}(z_i) = \frac{e^{z_i}}{\sum_{j=1}^{C} e^{z_j}}
\]

| 性质 | 说明 |
|------|------|
| 输出范围 | 每个值 ∈ (0, 1) |
| 总和 | 所有类别概率之和 = 1 |
| 作用 | 从 C 个「原始分数」得到 C 个「类别概率」 |

```python
import torch
import torch.nn.functional as F

z = torch.tensor([2.0, 1.0, 0.1])
prob = F.softmax(z, dim=0)
# prob ≈ [0.66, 0.24, 0.10]，加起来 = 1
```

---

## 四、CrossEntropyLoss（课件重点）

多分类的损失函数，直觉与二分类 BCE 类似：**预测概率越接近真实类别，loss 越小**。

### 重要关系

```
CrossEntropyLoss  <==>  LogSoftmax + NLLLoss
```

PyTorch 把它们合并成一个函数，**数值更稳定**。

### 与 BCELoss 的对比

| 项目 | 二分类（第 6 集） | 多分类（第 9 集） |
|------|------------------|------------------|
| 损失 | `BCELoss` | `CrossEntropyLoss` |
| 模型最后输出 | Sigmoid 后的概率 | **原始 logits（不过 Softmax）** |
| 标签类型 | `float`，shape `(N, 1)` | **`long`，shape `(N,)`** |
| 标签含义 | 0.0 或 1.0 | 类别编号 0, 1, ..., C-1 |

### 最小示例

```python
import torch
import torch.nn as nn

# 3 分类，1 个样本，模型输出 3 个 logits
z = torch.tensor([[2.0, 1.0, 0.1]])
y = torch.LongTensor([0])          # 真实类别是第 0 类，必须是 long

criterion = nn.CrossEntropyLoss()
loss = criterion(z, y)
print(loss)
```

> **关键**：用 `CrossEntropyLoss` 时，最后一层**不要**加 Softmax，损失函数内部会处理。

---

## 五、MNIST 数据集（提前熟悉）

**MNIST** = 手写数字数据集，每张图 28×28 像素，灰度图，标签 0～9。

| 集合 | 样本数 | 用途 |
|------|--------|------|
| 训练集 | 60,000 | 训练 |
| 测试集 | 10,000 | 评估准确率 |

### 用 torchvision 加载（课件用法）

```python
import torch
from torchvision import datasets, transforms
from torch.utils.data import DataLoader

batch_size = 64

transform = transforms.Compose([
    transforms.ToTensor(),                        # PIL Image → Tensor，缩放到 [0,1]
    transforms.Normalize((0.1307,), (0.3081,))   # 标准化（MNIST 均值/标准差）
])

train_dataset = datasets.MNIST(
    root='./dataset/mnist',
    train=True,
    download=True,
    transform=transform
)
test_dataset = datasets.MNIST(
    root='./dataset/mnist',
    train=False,
    download=True,
    transform=transform
)

train_loader = DataLoader(train_dataset, batch_size=batch_size, shuffle=True)
test_loader = DataLoader(test_dataset, batch_size=batch_size, shuffle=False)
```

| 参数 | 含义 |
|------|------|
| `root` | 数据存放目录 |
| `train=True/False` | 训练集 / 测试集 |
| `download=True` | 没有数据时自动下载 |
| `transform` | 图像预处理流水线 |

### 单个样本的 shape

```python
inputs, labels = next(iter(train_loader))
# inputs.shape: (64, 1, 28, 28)  → batch × 通道 × 高 × 宽
# labels.shape: (64,)             → 每个是一个整数 0~9
```

---

## 六、x.view()：图像展平（课件会讲）

全连接层需要一维输入，28×28 图像要展平成 784 维：

```python
x = x.view(-1, 28 * 28)   # 或 x.view(-1, 784)
# (64, 1, 28, 28) → (64, 784)
```

| 参数 | 含义 |
|------|------|
| `-1` | 自动推断该维度（这里是 batch_size=64） |
| `784` | 28 × 28，每张图展平后的特征数 |

> `view` 是 reshape，不改变数据，只改变「怎么看」这个 Tensor。

---

## 七、模型结构预览

MNIST 10 分类，全连接网络（本课不用 CNN，第 10 集才学）：

```python
class Net(torch.nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.l1 = torch.nn.Linear(784, 512)
        self.l2 = torch.nn.Linear(512, 256)
        self.l3 = torch.nn.Linear(256, 128)
        self.l4 = torch.nn.Linear(128, 64)
        self.l5 = torch.nn.Linear(64, 10)    # 10 个类别

    def forward(self, x):
        x = x.view(-1, 784)
        x = torch.relu(self.l1(x))
        x = torch.relu(self.l2(x))
        x = torch.relu(self.l3(x))
        x = torch.relu(self.l4(x))
        x = self.l5(x)                     # 最后一层不过激活！
        return x
```

计算图：

```
x(64,1,28,28) → view → (64,784)
  → Linear(784,512) → ReLU
  → Linear(512,256) → ReLU
  → Linear(256,128) → ReLU
  → Linear(128,64)  → ReLU
  → Linear(64,10)   → logits(64,10)
  → CrossEntropyLoss
```

---

## 八、训练与测试循环预览

### 训练（与第 8 集相同的双层循环）

```python
def train(epoch):
    model.train()                    # 训练模式（本课影响小，但养成习惯）
    for batch_idx, (inputs, labels) in enumerate(train_loader):
        outputs = model(inputs)
        loss = criterion(outputs, labels)

        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

        if batch_idx % 100 == 0:
            print(f'epoch {epoch}, batch {batch_idx}, loss: {loss.item():.4f}')
```

### 测试（算准确率）

```python
def test():
    model.eval()                     # 评估模式
    correct = 0
    total = 0
    with torch.no_grad():            # 测试时不计算梯度
        for inputs, labels in test_loader:
            outputs = model(inputs)
            _, predicted = torch.max(outputs, dim=1)   # 取概率最大的类别
            total += labels.size(0)
            correct += (predicted == labels).sum().item()

    print(f'accuracy: {100 * correct / total:.2f}%')
```

### 预测怎么读

```python
outputs = model(inputs)       # shape: (64, 10)，10 个 logits
_, predicted = torch.max(outputs, dim=1)
# predicted: shape (64,)，每个是 0~9 的预测类别
```

| 函数 | 作用 |
|------|------|
| `torch.max(outputs, dim=1)` | 每行取最大值及其**列索引**（索引 = 预测类别） |
| `torch.no_grad()` | 测试时不建计算图，省内存 |

---

## 九、完整代码骨架（听课前可先扫一眼）

```python
import torch
from torch import optim
from torch.utils.data import DataLoader
from torchvision import datasets, transforms

batch_size = 64
transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.1307,), (0.3081,))
])

train_dataset = datasets.MNIST('./dataset/mnist', train=True, download=True, transform=transform)
test_dataset = datasets.MNIST('./dataset/mnist', train=False, download=True, transform=transform)
train_loader = DataLoader(train_dataset, batch_size=batch_size, shuffle=True)
test_loader = DataLoader(test_dataset, batch_size=batch_size, shuffle=False)

class Net(torch.nn.Module):
    def __init__(self):
        super(Net, self).__init__()
        self.l1 = torch.nn.Linear(784, 512)
        self.l2 = torch.nn.Linear(512, 256)
        self.l3 = torch.nn.Linear(256, 128)
        self.l4 = torch.nn.Linear(128, 64)
        self.l5 = torch.nn.Linear(64, 10)

    def forward(self, x):
        x = x.view(-1, 784)
        x = torch.relu(self.l1(x))
        x = torch.relu(self.l2(x))
        x = torch.relu(self.l3(x))
        x = torch.relu(self.l4(x))
        return self.l5(x)

model = Net()
criterion = torch.nn.CrossEntropyLoss()
optimizer = optim.SGD(model.parameters(), lr=0.01, momentum=0.5)

for epoch in range(10):
    # train ...
    # test ...
```

---

## 十、与第 8 集的对照

| 项目 | 第 8 集 | 第 9 集 |
|------|--------|--------|
| 任务 | 二分类（diabetes） | **多分类（MNIST 10 类）** |
| 数据加载 | 自定义 Dataset | **torchvision.datasets.MNIST** |
| 输入 shape | `(batch, 8)` | `(batch, 1, 28, 28)` → view 成 `(batch, 784)` |
| 输出维度 | 1 | **10** |
| 最后激活 | Sigmoid | **无（直接输出 logits）** |
| 损失 | BCELoss | **CrossEntropyLoss** |
| 标签 | float `(batch, 1)` | **long `(batch,)`** |
| 训练循环 | epoch × batch | **完全相同** |
| 评估指标 | loss | **准确率 accuracy** |

---

## 十一、听课时要重点关注的

| 关注点 | 为什么重要 |
|--------|------------|
| Softmax 的直觉 | 理解多分类概率分布 |
| CrossEntropyLoss 输入要求 | logits + long 标签，不是概率 |
| 最后一层不过 Softmax | 与第 6 集「输出要 Sigmoid」相反，别搞混 |
| `x.view(-1, 784)` | 图像 → 全连接层的桥梁 |
| `torch.max(dim=1)` | 从 10 维输出得到预测类别 |
| `model.train()` / `model.eval()` | 训练/测试模式切换 |
| `torch.no_grad()` | 测试时不计算梯度 |
| `momentum` | SGD 优化器的新参数，加速收敛 |

---

## 十二、预习动手任务（20 分钟）

在听第 9 集之前，建议完成：

- [ ] 回顾第 8 集 `DiabetesDataset` + `DataLoader` 双层循环
- [ ] 用 `datasets.MNIST(download=True)` 下载数据，print 一个 batch 的 shape
- [ ] 实验 `x.view(-1, 784)`，确认 `(64,1,28,28)` → `(64,784)`
- [ ] 跑通 CrossEntropyLoss 最小示例（3 分类 + LongTensor 标签）
- [ ] 思考：为什么多分类标签用 `long` 而不是 `float`？
- [ ] 可选：训练 1 个 epoch，看测试准确率是否 > 90%

---

## 十三、常见坑（提前避雷）

| 问题 | 原因 | 解决 |
|------|------|------|
| CrossEntropyLoss 报错 | 标签是 float | 改成 `torch.long` / `LongTensor` |
| 标签 shape 不对 | 用了 `(N, 1)` | 多分类要 `(N,)` 一维 |
| 最后一层又加了 Softmax | 与 CrossEntropyLoss 重复 | 去掉 Softmax，直接输出 logits |
| 用了 Sigmoid + CrossEntropyLoss | 二分类写法套到多分类 | 二分类 BCE / 多分类 CE，二选一 |
| `view` 报错 | 元素总数对不上 | 确认 1×28×28=784 |
| MNIST 下载失败 | 网络问题 | 手动下载或换 `root` 路径 |
| 准确率很低 | 忘了 `view` 或标签类型错 | 检查 shape 和 dtype |
| 测试集也 shuffle | 不需要 | 测试 `shuffle=False` |

---

## 十四、二分类 vs 多分类 决策表

| 场景 | 类别数 | 输出层 | 损失函数 | 标签 |
|------|--------|--------|----------|------|
| 二分类 | 2 | 1 维 + Sigmoid | BCELoss | float 0/1 |
| 二分类（工程） | 2 | 1 维 logits | BCEWithLogitsLoss | float 0/1 |
| **多分类** | C | **C 维 logits** | **CrossEntropyLoss** | **long 0~C-1** |

---

## 十五、与后续课程的关系

| 第 9 集学会 | 在哪用到 |
|------------|----------|
| Softmax / CrossEntropyLoss | 所有多分类任务 |
| MNIST + torchvision | 图像分类入门 |
| `x.view` 展平 | 全连接处理图像（第 10 集前） |
| 准确率评估 | 所有分类项目 |
| 双层训练循环 | 继续复用 |
| 全连接 MNIST | **第 10 集 CNN** 会大幅超越 |

---

## 十六、第 10 集预告（了解即可）

第 9 集用**全连接层**处理 MNIST，能到 ~97% 准确率。  
第 10 集会引入 **CNN（卷积神经网络）**：

- `Conv2d`、`MaxPool2d`
- 利用图像的**空间结构**，不再展平成长向量
- 同样数据集，准确率更高

第 9 集先把「多分类 + CrossEntropyLoss + MNIST 流程」跑通，第 10 集再换模型架构。

---

## 十七、一句话预习总结

> 第 9 集 = 第 8 集 DataLoader 双层循环不变 + **输出从 1 维变 C 维** + **Sigmoid/BCELoss 换成 CrossEntropyLoss** + **MNIST 数据集** + `x.view(-1, 784)` 展平图像；这是从二分类迈向多分类的关键一步。

---

*听课建议：先单独跑通 MNIST 加载和 CrossEntropyLoss 小例子；再写完整训练/测试函数，确认准确率能到 90%+ 后再调 epoch 和 lr。*
