# PyTorch 深度学习实践 · 第十二课 循环神经网络基础 总结

> **课程**：[刘二大人《PyTorch深度学习实践》](https://www.bilibili.com/video/BV1Y7411d7Ys?p=12)  
> **集数**：第 12 集 · 循环神经网络（基础篇）  
> **时长**：约 77 分钟  
> **整理日期**：2026-07-18  
> **前置**：第 9 集多分类（CrossEntropyLoss、`torch.max`）；`nn.Module` 五步训练循环

---

## 一、本课定位

第 9～11 集处理**图像**（空间结构 → CNN）。  
第 12 集进入**序列数据**（时间/顺序结构 → RNN）：文字、语音、天气、股价等「前后有关」的数据。

```
CNN（第 10～11 集）          RNN（第 12 集）
─────────────────           ─────────────────
输入：(N, C, H, W)           输入：(seq_len, batch, input_size)
空间邻域相关                 时间先后相关
Conv / Pool                  RNNCell / RNN
MNIST 分类                   hello → ohlol（逐步多分类）
```

**本课核心**：隐藏状态如何沿时间传递；分清 `RNNCell` 与 `RNN`；用 One-Hot 把字符变成向量，完成序列转换小任务。

---

## 二、为什么需要 RNN？

### 例子：用前 3 天天气预测第 4 天是否下雨

| 做法 | 问题 |
|------|------|
| 拼成一个长向量 → Linear | 丢掉先后顺序 |
| 每天单独一套网络 | 参数多，且学不到「同一规律按天复用」 |

**RNN 思路**：

1. **权值共享**：每个时间步用同一套 Cell 权重  
2. **隐藏状态**：把 \(h_{t-1}\) 和下一步 \(x_t\) 一起算，得到 \(h_t\)  
3. **记忆传递**：历史信息通过 \(h\) 沿时间传下去  

适用：自然语言、语音、金融序列、传感器时序等。

> Java 类比：同一个方法在 `for` 里反复调用，每次多带一个「记忆」参数 `hidden`。

---

## 三、RNN Cell 在算什么

### 数据流

```
h0（初始，常全 0）
  + x1 → Cell → h1
  + x2 → Cell → h2
  + x3 → Cell → h3
  ...
```

- Cell 本质：两个输入的线性变换 + `tanh`
- 每一步用的是**同一个** Cell（共享 W）

### 公式（理解即可）

\[
h_t = \tanh(W_{ih} x_t + b_{ih} + W_{hh} h_{t-1} + b_{hh})
\]

| 符号 | 含义 |
|------|------|
| \(x_t\) | 当前输入，`input_size` 维 |
| \(h_{t-1}\) | 上一时刻隐藏状态，`hidden_size` 维 |
| \(h_t\) | 当前隐藏状态（本课常直接当输出） |
| \(\tanh\) | 激活，输出约在 (-1, 1) |

`h0`：无先验时全 0；有先验（如图像生成文字）可用 CNN 特征当 `h0`。

---

## 四、两种实现方式

### 方式 A：`nn.RNNCell` —— 自己写循环

```python
cell = torch.nn.RNNCell(input_size=input_size, hidden_size=hidden_size)

# dataset: (seq_len, batch, input_size)
hidden = torch.zeros(batch_size, hidden_size)
for input_t in dataset:              # 每步 (batch, input_size)
    hidden = cell(input_t, hidden)   # → (batch, hidden_size)
```

| 张量 | shape |
|------|-------|
| 每步 input | `(batch, input_size)` |
| hidden | `(batch, hidden_size)` |

适合：搞懂「循环」本身；灵活控制每一步。

### 方式 B：`nn.RNN` —— 一次喂完整序列

```python
rnn = torch.nn.RNN(input_size, hidden_size, num_layers=1)

# inputs: (seq_len, batch, input_size)
# h0:     (num_layers, batch, hidden_size)
out, hidden = rnn(inputs, h0)
# out:    (seq_len, batch, hidden_size)   # 每一步的 h
# hidden: (num_layers, batch, hidden_size) # 最后时刻
```

| 参数 | 含义 |
|------|------|
| `input_size` | 每个时间步特征维 |
| `hidden_size` | 隐藏状态维 |
| `num_layers` | 纵向堆几层 RNN |
| `batch_first=True` | 输入改为 `(batch, seq_len, input_size)` |

**关系**：`RNN` ≈ 内部自动跑完整个序列上的 `RNNCell` 循环。

---

## 五、维度速查表（最易错）

默认（`batch_first=False`）：

| 张量 | shape |
|------|-------|
| 整段输入 | `(seq_len, batch, input_size)` |
| 初始 h0 | `(num_layers, batch, hidden_size)` |
| 输出 out | `(seq_len, batch, hidden_size)` |
| 最终 hidden | `(num_layers, batch, hidden_size)` |

`batch_first=True` 时：输入/输出前两维变为 `(batch, seq_len, …)`，**h0 形状不变**。

---

## 六、主任务：hello → ohlol

把字符串看成**逐步多分类**：

```
输入: h e l l o
输出: o h l o l
```

字符集 `{e, h, l, o}` → 4 类；每个时间步预测下一个（或对应位置）字符。

### 字符入网：One-Hot

```
e → [1,0,0,0]
h → [0,1,0,0]
l → [0,0,1,0]
o → [0,0,0,1]
```

```python
idx2char = ['e', 'h', 'l', 'o']
x_data = [1, 0, 2, 2, 3]   # hello
y_data = [3, 1, 2, 3, 2]   # ohlol
# One-Hot 后 inputs: (seq_len, batch, 4)
# labels: LongTensor，类别下标
```

- 损失：`CrossEntropyLoss`（与第 9 集相同）  
- 预测：每步 `torch.max` 取下标，再映射回字符  
- 优化器课件常用：`Adam(lr=0.1)`（玩具任务）

---

## 七、完整代码骨架

### RNNCell 版

```python
class Model(torch.nn.Module):
    def __init__(self, input_size, hidden_size, batch_size):
        super(Model, self).__init__()
        self.batch_size = batch_size
        self.hidden_size = hidden_size
        self.rnncell = torch.nn.RNNCell(input_size, hidden_size)

    def forward(self, input, hidden):
        return self.rnncell(input, hidden)

    def init_hidden(self):
        return torch.zeros(self.batch_size, self.hidden_size)

# 训练：
hidden = net.init_hidden()
loss = 0
for input_t, label_t in zip(inputs, labels):
    hidden = net(input_t, hidden)
    loss += criterion(hidden, label_t)   # 每步 CE，再累加
loss.backward()
optimizer.step()
```

### RNN 版

```python
class Model(torch.nn.Module):
    def __init__(self, input_size, hidden_size, batch_size, num_layers=1):
        super(Model, self).__init__()
        self.batch_size = batch_size
        self.hidden_size = hidden_size
        self.num_layers = num_layers
        self.rnn = torch.nn.RNN(input_size, hidden_size, num_layers)

    def forward(self, input):
        h0 = torch.zeros(self.num_layers, self.batch_size, self.hidden_size)
        out, _ = self.rnn(input, h0)
        return out.view(-1, self.hidden_size)  # 接到 CE

outputs = net(inputs)              # (seq*batch, hidden)
loss = criterion(outputs, labels)  # labels: (seq*batch,)
```

---

## 八、与 CNN / 全连接的对照

| 项目 | 全连接 / CNN | RNN |
|------|--------------|-----|
| 数据 | 表格 / 图像 | 序列 |
| 共享方式 | 空间卷积核共享 | **时间上 Cell 权重共享** |
| 关键 shape | `(N,C,H,W)` 或 `(N,D)` | `(seq, batch, feature)` |
| 损失粒度 | 常一个样本一个标签 | **每个时间步一个标签**（本课） |
| 「记忆」 | 无显式时间记忆 | **隐藏状态 \(h_t\)** |

---

## 九、常见坑

| 问题 | 原因 | 解决 |
|------|------|------|
| shape 对不上 | seq / batch 维放反 | 对照维度速查表，`print` 每步 shape |
| CE 维度错 | out 与 labels 没对齐 | RNN 版常用 `view(-1, C)` |
| 不重置 h0 | 用了上一条序列的记忆 | 每条序列 / 每轮开始前 `zeros` |
| 标签用 float | CE 要类别下标 | `LongTensor` |
| 先 Softmax 再 CE | 与第 9 集同样错误 | 直接 logits + CE |
| `input_size` 设错 | 与字典大小不一致 | One-Hot 维 = 字符种类数 |

---

## 十、本课你需要记住的

1. **RNN** = 共享权重的 Cell + 隐藏状态沿时间传递  
2. **`RNNCell`**：一步一步，自己写 `for`  
3. **`nn.RNN`**：一次吃整段序列，内部自动循环  
4. **维度**：默认 `(seq_len, batch, feature)`；`batch_first` 只换前两维  
5. **字符 → One-Hot → 逐步 CE**；预测用 `argmax` 映回字符  
6. **每条序列前重置 h0**（通常全 0）  
7. 第 13 集会把 RNN 用到更接近真实的分类任务；本课先把 Cell 与维度吃透  

---

## 十一、与后续课程的关系

| 第 12 集学会 | 在哪用到 |
|------------|----------|
| 隐藏状态 / 序列建模 | LSTM、GRU、Attention 的前置直觉 |
| `nn.RNN` API | 第 13 集人名分类等 |
| One-Hot | Embedding 的前身（课件可能提改进） |
| 序列 + CE | NLP、语音入门 |

对你的压缩路线：第 12 集是 **Phase 1 必看**；CNN 可跳，RNN 基础建议吃透后再进 NLP / Transformer。

---

## 十二、自测清单

- [ ] 能解释为什么序列任务不能简单拼成一个长向量  
- [ ] 能画出 `hello→ohlol` 五个时间步的 \(x_t / h_t\)  
- [ ] 能说出 `RNNCell` 与 `nn.RNN` 的输入输出 shape  
- [ ] 能手写 One-Hot 并把 `hello` 变成 `(5,1,4)`  
- [ ] 知道 `batch_first=True` 时哪两维对调、h0 是否变  
- [ ] 能独立跑通 Cell 版或 RNN 版，看到预测串接近 `ohlol`  
- [ ] 知道每条序列前要重置 hidden  

---

## 十三、一句话总结

> 第 12 集 = **用共享权重的 Cell 沿时间传递隐藏状态**；`RNNCell` 手写循环，`nn.RNN` 一次处理整段；用 One-Hot 把 `hello→ohlol` 变成逐步多分类。核心不是公式，而是 **`(seq, batch, feature)` 维度** 和 **`h_t` 如何接下一步**。

---

*下一课：第 13 集 · 循环神经网络（高级篇）—— 更接近真实任务的 RNN 分类器（如人名国家分类）*
